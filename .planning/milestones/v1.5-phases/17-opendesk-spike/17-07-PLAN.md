---
phase: 17-opendesk-spike
plan: 07
type: execute
wave: 7
depends_on: ["17-06"]
files_modified:
  - compose.spike-opendesk.yml
  - deploy/Caddyfile.spike-opendesk
  - docs/spike-opendesk.md
  - docs/contrib/user-oidc-925-kommentar.md
autonomous: true
requirements: [OD-02]

must_haves:
  truths:
    - "Der Loglevel steht auf debug, bevor der erste S5-Lauf startet, sonst verschluckt Nextcloud die Zeile, die S5 entscheidet"
    - "S5 trägt eine Log-Zeile als Messwert und nicht die 401 als Ursache"
    - "Der Bericht führt alle drei Ereignispfade und markiert den, den die Messung getroffen hat"
    - "Der Entwurf zu user_oidc#925 entsteht nur bei geglückter Live-Reproduktion und wird von keinem Agenten gesendet"
    - "D-07: S5 ist gemessen und nicht behauptet, weil Keycloak und user_oidc lokal dazukommen und die serverseitige Token-Erneuerung im OIDC-gebundenen Betrieb geprüft wird"
    - "D-08: Der Entwurf zu nextcloud/user_oidc#925 existiert nur mit geglückter Live-Reproduktion und bleibt sonst liegen, gesendet wird ausschließlich vom Owner"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "S5a, S5b, S5c in Abschnitt 2.1 oder als begründetes ungemessen in Abschnitt 2.5"
      contains: "S5"
    - path: "docs/contrib/user-oidc-925-kommentar.md"
      provides: "Entwurf des Kommentars, nur bei geglückter Repro, ausdrücklich unversendet"
      contains: "Entwurf"
  key_links:
    - from: "integration_openproject authorization_method=oidc"
      to: "user_oidc TokenService"
      via: "TokenEventFactory und die drei Ereignisse"
      pattern: "token_exchange"
---

<objective>
Stufe B der Messumgebung, der teuerste und ausdrücklich rückfallberechtigte Teil: Keycloak 26.7.0 und
`user_oidc` 8.11.0 kommen dazu, und S5 wird gemessen statt behauptet. Die Frage ist, ob die
serverseitige Token-Erneuerung von `integration_openproject` auch im OIDC-gebundenen Betrieb hält oder
nach Ablauf des zwischengespeicherten Tokens auf 401 fällt, und welcher der drei Ereignispfade dabei
bricht.

Purpose: S5 ist die Stelle, an der Weg 0 kippt, und der Unterschied zwischen "Weg 0 trägt" und "Weg 0
trägt nur im Modus oauth2". Nach K5 hat der Bruch drei Pfade statt einem, und einer davon bricht nicht.
Derselbe Aufbau liefert die Live-Reproduktion für `nextcloud/user_oidc#925` (D-08). S5 darf ungemessen
bleiben, ohne dass die Phase fehlschlägt; verworfen darf es nicht werden (D-03).
Output: S5a bis S5c als Messwerte mit Log-Zeilen oder als begründetes `ungemessen`, und bei geglückter
Repro ein unversendeter Entwurf im Repository.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@compose.spike-opendesk.yml
@deploy/Caddyfile.spike-opendesk
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stufe B aufbauen, Anbieter skriptbar, Loglevel vor dem ersten Lauf</name>
  <files>compose.spike-opendesk.yml, deploy/Caddyfile.spike-opendesk</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"S5 und der OIDC-Bruch: der minimale lokale
      Aufbau" (Zeilen 934 bis 1018), §K5 (Zeilen 358 bis 427), Annahmen A1 und A4, Pitfall 5.
    - `compose.spike-opendesk.yml`, Dienst `keycloak` und der Caddy-Block `:8082` als Muster für die
      Namensauflösung.
    - `deploy/Caddyfile.spike-opendesk` vollständig.
  </read_first>
  <action>
    Ergänze den Keycloak-Zugang nach demselben Muster, das die Namensfalle für OpenProject gelöst hat,
    weil `user_oidc` die Discovery-Adresse aus dem Nextcloud-Container auflöst und der Browser dieselbe
    Adresse sehen muss: einen Caddy-Zuhörblock `:8083` in `deploy/Caddyfile.spike-opendesk`, der auf
    `keycloak:8080` weiterleitet; in `compose.spike-opendesk.yml` die Veröffentlichung
    `127.0.0.1:8083:8083` am Caddy-Dienst, den `extra_hosts`-Eintrag `kc.localtest.me:172.29.43.10`
    am Nextcloud-Dienst, und am Keycloak-Dienst `KC_HOSTNAME=kc.localtest.me:8083`,
    `KC_HOSTNAME_STRICT=false`, dazu `KC_BOOTSTRAP_ADMIN_USERNAME` und
    `KC_BOOTSTRAP_ADMIN_PASSWORD` ohne Vorgabewert (WR-11), interpoliert aus
    `.env.spike-opendesk`.

    Fahre `docker compose -f compose.spike-opendesk.yml --profile op --profile oidc up -d`. Lege die
    Realm `spike` mit zwei Clients an: `nextcloud` (vertraulich, Redirect `http://127.0.0.1:8091/*`,
    Secret nach `.env.spike-opendesk` als `KC_CLIENT_SECRET`) und `openproject` (die Zielgruppe des
    Token-Austauschs), dazu die Nutzer `alice` und `bob` mit Passwörtern in `.env.spike-opendesk`. Der
    bequeme Weg `start-dev --import-realm` ist nach Annahme A1 `[ASSUMED]`: ein Fehlschlag hier ist ein
    Aufbaufehler und kein S5-Befund. Der Rückfall ist, die Realm einmal in der Keycloak-Oberfläche
    anzulegen und danach zu exportieren, damit ein zweiter Lauf reproduzierbar ist. Weil eine
    vorhandene Realm beim Import übersprungen wird, muss vor einem zweiten Lauf das Keycloak-Band mit
    `docker compose -f compose.spike-opendesk.yml down -v` verworfen werden, und danach ist Stufe A neu
    aufzubauen; plane den Lauf deshalb in einem Zug.

    Installiere `user_oidc` mit `occ app:install user_oidc` (erwartet 8.11.0, Plattformspanne
    `>=29.0.0 <36.0.0`; `integration_openproject` verlangt mindestens 7.2.0) und schreibe die mit
    `occ app:list` gelesene Fassung in den Berichtskopf, nicht die aus der Recherche.

    Richte den Anbieter skriptbar ein:
    `occ user_oidc:provider spike --clientid=nextcloud --clientsecret=$KC_CLIENT_SECRET
    --discoveryuri=http://kc.localtest.me:8083/realms/spike/.well-known/openid-configuration
    --scope="openid email profile"`, danach `occ user_oidc:provider:list` zur Kontrolle. Setze
    `occ config:app:set user_oidc store_login_token --value=1`. Halte im Bericht fest, dass der
    Quellcode diesen Wert als App-Konfigwert liest (`TokenService.php:316`), die Fehlermeldung aber
    "It can be enabled in config.php" sagt: diese Diskrepanz kostet beim Nachfahren Zeit.

    Stelle den Loglevel **vor** dem ersten S5-Lauf auf `debug` mit `occ log:manage --level 0`. Alle drei
    Meldungen, die die Pfade unterscheiden, sind `logger->debug`, und der Vorgabewert 2 verschluckt sie
    (Pitfall 5). Nimm in den Abschnitt "Was diese Messung nicht beweist" den Satz auf, dass diese
    Absenkung eine Messumgebung auf Loopback betrifft und keine Empfehlung für Produktion ist.

    Schalte `integration_openproject` in den OIDC-Modus:
    `occ config:app:set integration_openproject authorization_method --value=oidc`, dazu
    `sso_provider_type --value=external`, `oidc_provider --value=spike`,
    `targeted_audience_client_id --value=openproject`, `token_exchange --value=0`. Melde dich danach
    einmal über Keycloak in Nextcloud an und lies mit `occ user:list`, unter welcher Nutzer-Id
    `user_oidc` das Konto führt: sie kann von `alice` abweichen. Weicht sie ab, ist genau diese Id das
    Konto, das den persönlichen OpenProject-Durchlauf einmal braucht, bevor S5 messbar ist; notiere die
    Id im Bericht, weil jeder Messwert seinen Nutzernamen nennt.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w '%{http_code}\n' http://kc.localtest.me:8083/realms/spike/.well-known/openid-configuration; docker compose -f compose.spike-opendesk.yml exec -T nextcloud curl -sS -o /dev/null -w '%{http_code}\n' http://kc.localtest.me:8083/realms/spike/.well-known/openid-configuration; docker compose -f compose.spike-opendesk.yml exec -T --user www-data nextcloud php occ user_oidc:provider:list; docker compose -f compose.spike-opendesk.yml exec -T --user www-data nextcloud php occ config:app:get integration_openproject authorization_method | grep -q oidc</automated>
  </verify>
  <acceptance_criteria>
    - Die Discovery-Adresse antwortet mit 200 vom Host und aus dem Nextcloud-Container, unter demselben
      Namen `kc.localtest.me:8083`.
    - `occ user_oidc:provider:list` nennt den Anbieter `spike`; `occ config:app:get user_oidc
      store_login_token` ergibt `1`.
    - `occ config:app:get integration_openproject authorization_method` ergibt `oidc`.
    - `occ log:manage` meldet Level 0, bevor der erste S5-Lauf startet.
    - Alle Bildmarken bleiben gepinnt, keine `ports`-Zeile bindet etwas anderes als `127.0.0.1`, kein
      Geheimnis hat einen Vorgabewert in der Compose-Datei.
    - Ungemessen-Fallback, ausdrücklich vorgesehen (Pitfall 1, letzte Zeile): scheitert der
      Realm-Import, die Anbieter-Einrichtung oder die Anmeldung über Keycloak nach zwei Versuchen, wird
      Stufe B abgebrochen. Dann trägt Abschnitt 2.1 für S5a bis S5c wörtlich `ungemessen` mit Grund und
      letztem Fehlertext, Abschnitt 2.5 nennt denselben Grund, die Quellcodebelege aus K5 bleiben mit
      Datei, Zeile und Tag stehen und sind als hergeleitet markiert, und der Entwurf zu
      `user_oidc#925` bleibt liegen (D-08). Danach wird der Modus mit
      `occ config:app:set integration_openproject authorization_method --value=oauth2` zurückgestellt.
  </acceptance_criteria>
  <done>Stufe B steht, der Anbieter ist skriptbar eingerichtet, der Loglevel ist gesenkt und die App läuft im Modus oidc, oder der Abbruch ist mit Grund protokolliert.</done>
</task>

<task type="auto">
  <name>Task 2: S5a, S5b und S5c messen, die Log-Zeile ist der Messwert</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Was genau zu beobachten ist, damit S5 ein
      Messwert wird und keine Vermutung" (Zeilen 990 bis 1018) samt der Tabelle der vier beobachtbaren
      Zeilen, §K5 (Zeilen 358 bis 427), §"S0 bis S6" Zeilen S5a, S5b, S5c (Zeilen 802 bis 804).
    - Pitfall 5 (Zeilen 1404 bis 1414) und Pitfall 4 (Zeilen 1391 bis 1402).
  </read_first>
  <action>
    Fahre drei Läufe, jeder nach demselben Muster wie S4: `occ user:setting <oidc-nutzer>
    integration_openproject token_expires_at 0`, danach derselbe OCS-Aufruf unter reiner
    AppAPI-Impersonation
    (`GET /ocs/v2.php/apps/integration_openproject/api/v1/work-packages?searchQuery=SPIKE-OD-8471`),
    danach das Log frisch lesen
    (`docker compose -f compose.spike-opendesk.yml exec -T nextcloud tail -n 200 /var/www/html/data/nextcloud.log`)
    und die Zustandsschlüssel `token` und `token_expires_at` erneut lesen.

    S5a: `sso_provider_type=external`, `token_exchange=0`. Erwartung OCS-`statuscode` 401 und im Log
    `GetExternalTokenFailedException` beziehungsweise eine Zeile des
    `ExternalTokenRequestedListener`.
    S5b: `token_exchange=1`, `targeted_audience_client_id=openproject`, ein Lauf mit
    `store_login_token=0` und ein Lauf mit `=1`. Erwartung zwei unterschiedliche Meldungen:
    `Failed to exchange token, storing the login token is disabled` (`TokenService.php:318`) und
    `Failed to exchange token, no login token found in the session` (`TokenService.php:328`).
    S5c: die trennscharfe Beobachtung, ob überhaupt eine Listener-Zeile erscheint. Erscheint keine, war
    `IUserSession::isLoggedIn()` unter AppAPI-Impersonation falsch, der Bruch liegt vor der
    Sitzungsfrage, und die Diagnose ist eine andere als die in `research/SUMMARY.md` vermutete. Das ist
    ein vollwertiger Befund und kein Fehlschlag (Annahme A9). Beobachte auch die vierte Zeile aus der
    Recherche, `Token event has not been caught by 'user_oidc'` (`svc.php:1687`).

    Gegenprobe, ohne die jede 401 auch eine kaputte Keycloak-Kopplung wäre: derselbe Aufruf mit einer
    echten Browsersitzung des Nutzers, also mit dem Sitzungscookie aus einer Anmeldung über Keycloak,
    muss Daten liefern. Gelingt sie nicht, ist der Hauptlauf ausdrücklich als nicht gegengeprobt zu
    markieren.

    Schreibe je Lauf eine Tabellenzeile in Abschnitt 2.1 mit: Konfiguration (`sso_provider_type`,
    `token_exchange`, `store_login_token`), Aufruf, Nutzername, OCS-`statuscode`, die wörtliche
    Log-Zeile, der Zustand von `token` und `token_expires_at` danach, Urteil. Führe darunter alle drei
    Ereignispfade aus K5 in der Reihenfolge `nextcloud_hub` (bricht nicht, weil
    `InternalTokenRequestedListener` eine Nutzer-Id statt einer Sitzung nimmt), `external` ohne
    Austausch, `external` mit Austausch, und markiere den Pfad, den die Messung getroffen hat. Die
    Log-Zeile ist der Messwert, nicht die 401: ein S5-Abschnitt, der eine Ursache nennt, aber keine
    Log-Zeile zeigt, gilt in diesem Projekt nicht.

    Schreibe zusätzlich in Abschnitt 2.5 einen Satz, was von S5 auch bei gelungenen Läufen ungemessen
    bleibt: der lokale Aufbau baut den openDesk-Anmeldezwang über Keycloak nur nach, und die
    Betriebsart, die openDesk wirklich fährt, ist unbekannt und steht als Frage 7 in Abschnitt 4.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "S5a" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "S5c" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -qE "TokenService|GetExternalTokenFailedException|ungemessen" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "nextcloud_hub" &amp;&amp; ! grep -rniE "eyJ|refresh_token=[A-Za-z0-9_-]{10}" docs/spike-opendesk.md</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 2.1 trägt für S5a, S5b und S5c je eine Zeile mit Konfiguration, Nutzername,
      OCS-`statuscode`, wörtlicher Log-Zeile und Zustand danach, oder wörtlich `ungemessen` samt Grund.
    - Die drei Ereignispfade aus K5 stehen vollständig da, und genau einer ist als getroffen markiert;
      der Pfad `nextcloud_hub` steht mit dem Hinweis, dass er nicht bricht.
    - Die Gegenprobe mit echter Browsersitzung ist als gelungen oder als nicht durchführbar
      protokolliert; im zweiten Fall trägt der Hauptlauf den Vermerk "nicht gegengeprobt".
    - Abschnitt 2.5 nennt, was von S5 ungemessen bleibt, mit Verweis auf Frage 7.
    - Kein Token- und kein Secret-Wert im Bericht.
    - Ungemessen-Fallback: bleibt das Log ohne jede der vier Zeilen und lässt sich die Ursache nicht
      trennen, steht S5 als `ungemessen, Ursache nicht trennscharf beobachtbar` mit dem gelesenen
      Log-Auszug; eine vermutete Ursache wird nicht geschrieben.
  </acceptance_criteria>
  <done>S5 steht mit Log-Zeilen als Messwerten und markiertem Ereignispfad im Bericht, oder als ungemessen mit Grund.</done>
</task>

<task type="auto">
  <name>Task 3: Entwurf zu user_oidc#925, nur bei geglückter Reproduktion</name>
  <files>docs/contrib/user-oidc-925-kommentar.md, docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §K5, Absatz "Folge für D-08" (Zeilen 419 bis
      427): das Issue ist die Feature-Anfrage, aus der die heutige Implementierung entstanden ist, und
      nicht das Issue zur Bruchstelle.
    - `Desktop/openDesk-Anfragen-2026-08-28.md`, der zurückgehaltene Entwurf zu `user_oidc#925` samt
      Formulierung "Adding a case that I think this issue does not yet cover".
    - `.planning/phases/17-opendesk-spike/17-CONTEXT.md` D-08 und D-11.
    - `docs/contrib/227-pr-body.md` als Formvorbild für einen Beitragstext in diesem Repository.
  </read_first>
  <action>
    Prüfe zuerst die Bedingung: liegt aus Task 2 eine geglückte Live-Reproduktion vor, also eine
    wörtliche Log-Zeile, die einen der drei Bruchpfade belegt? Nur dann entsteht die Datei.

    Wenn ja: lege `docs/contrib/user-oidc-925-kommentar.md` an, englischer Text (der Kanal ist
    englisch), Kopfzeile mit `Status: Entwurf, nicht gesendet. Versand ausschließlich durch den Owner.`
    Inhalt in dieser Reihenfolge: die Einordnung (das Issue ist die Anfrage, aus der die heutige
    Implementierung entstanden ist, der Kommentar ergänzt einen Fall, den es noch nicht abdeckt: eine
    AppAPI-ExApp ohne PHP-Anteil); die reproduzierten Fassungen (Nextcloud, `user_oidc`,
    `integration_openproject`, Keycloak, alle aus `occ app:list` und `occ status`); die
    Reproduktionsschritte in der Kürze, in der ein Maintainer sie nachfährt; die wörtliche Log-Zeile;
    die Codestellen mit Datei, Zeile und Tag (`TokenService.php:316-333`, die Vorbedingung
    `if (!$this->userSession->isLoggedIn()) { return; }` in allen drei Listenern); die konkrete Frage,
    ob ein vorgesehener Weg existiert. Kein Token, kein Secret, kein Cookie, keine internen Adressen
    außer `localhost`-Formen.

    Wenn nein: lege die Datei **nicht** an. Schreibe stattdessen in Abschnitt 2.5 des Berichts einen
    Absatz, dass die Live-Reproduktion nicht gelungen ist, mit dem Grund, und dass der Entwurf nach
    D-08 und der Regel aus context_agent#230 zurückgehalten bleibt. Eine Behauptung über fremden Code
    ohne Live-Repro geht in diesem Projekt nicht in einen fremden Kanal.

    In beiden Fällen: senden darf diesen Entwurf ausschließlich der Owner. Kein `gh issue comment`,
    kein Browserversand, kein Playwright-Lauf.
  </action>
  <verify>
    <automated>if [ -f docs/contrib/user-oidc-925-kommentar.md ]; then grep -qi "Entwurf" docs/contrib/user-oidc-925-kommentar.md &amp;&amp; grep -q "TokenService.php" docs/contrib/user-oidc-925-kommentar.md &amp;&amp; ! grep -rniE "eyJ|Bearer [A-Za-z0-9]|client_secret" docs/contrib/user-oidc-925-kommentar.md; else grep -v '^#' docs/spike-opendesk.md | grep -qi "zurückgehalten"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Genau einer der beiden Fälle trifft zu und ist nachweisbar: entweder existiert
      `docs/contrib/user-oidc-925-kommentar.md` mit Statuszeile `Entwurf, nicht gesendet`, den gelesenen
      Fassungen, der wörtlichen Log-Zeile und den Codestellen mit Tag, oder der Bericht enthält den
      Absatz, dass der Entwurf zurückgehalten bleibt, samt Grund.
    - Existiert die Datei, enthält sie keine Zeichenkette `eyJ`, kein `Bearer <Wert>` und kein
      `client_secret`.
    - Kein Versandbefehl wurde ausgeführt: die Plan-SUMMARY nennt ausdrücklich, dass nichts gesendet
      wurde.
    - Ungemessen-Fallback: siehe Fall zwei, er ist der Rückfall und kein Fehler.
  </acceptance_criteria>
  <done>Der Entwurf liegt unversendet im Repository, oder der Bericht sagt begründet, warum er zurückgehalten bleibt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keycloak zu Nextcloud | Ein Identitätsanbieter entscheidet, wer in Nextcloud angemeldet ist; sein Client-Secret liegt in der Umgebung |
| Nextcloud-Log zu Bericht | Der auf debug gesenkte Log kann Token-Bruchstücke und Nutzerdaten tragen |
| Bericht und Entwurf zu Öffentlichkeit | Beide Dateien liegen in einem öffentlichen Repository, der Entwurf ist für einen fremden Kanal gedacht |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | Log-Auszüge in docs/spike-opendesk.md und im Entwurf | hoch | mitigate | Vor dem Übernehmen jede Log-Zeile auf Token-Bruchstücke prüfen; nur die Meldung samt Klassennamen übernehmen, nie den Kontext mit Kopfzeilen; Gate greift nach `eyJ`, `Bearer `, `refresh_token=`, `client_secret` in beiden Dateien |
| T-17-06 | Information Disclosure | occ log:manage --level 0 | mittel | mitigate | Die Absenkung gilt einer Wegwerf-Instanz auf Loopback; der Bericht sagt ausdrücklich, dass daraus keine Empfehlung für Produktion wird, und der Zustand verschwindet mit dem Nextcloud-Band |
| T-17-02 | Spoofing, Tampering | Keycloak, neuer Caddy-Port | mittel | mitigate | `127.0.0.1:8083` statt `0.0.0.0`, `KC_BOOTSTRAP_ADMIN_*` ohne Vorgabewert, Realm und Clients nur für diese Messung, `down -v` danach |
| T-17-05 | Repudiation | S5-Ursachenzuordnung | mittel | mitigate | Die Log-Zeile ist der Messwert; ohne Log-Zeile bleibt die Ursache ungemessen, und die drei Pfade stehen vollständig mit Datei und Zeile im Bericht |
| T-17-03 | Elevation of Privilege | Token-Austausch, Zielgruppe openproject | mittel | mitigate | Kein eigener Keycloak-Client für OpenProject als Dienstkonto (Out of Scope in REQUIREMENTS.md); der Client `openproject` ist ausschließlich Zielgruppe des Austauschs, und der Bericht sagt das |
| T-17-04 | Tampering | Produktionsbaum | mittel | mitigate | Geschrieben werden nur die Spike-Topologie, der Bericht und ein Entwurf unter `docs/contrib/`; `src/` bleibt unberührt |
| T-17-SC | Tampering | keycloak 26.7.0, user_oidc 8.11.0 | niedrig | mitigate | Bildmarke gepinnt und in 17-RESEARCH.md geprüft; `user_oidc` kommt aus dem offiziellen App Store unter `nextcloud/`-Eigentümerschaft; keine Installation aus npm, PyPI oder crates |
</threat_model>

<verification>
- Die Discovery-Adresse antwortet unter demselben Namen aus Host und Container mit 200.
- Abschnitt 2.1 trägt S5a bis S5c mit Log-Zeile oder mit begründetem `ungemessen`.
- Entweder existiert der Entwurf unversendet, oder der Bericht begründet, warum er liegen bleibt.
- `git status --short src/ appinfo/ pyproject.toml uv.lock` leer, `uv run pytest -q` grün.
</verification>

<success_criteria>
Die Frage, an der Weg 0 kippt, ist entweder gemessen (mit Log-Zeile, markiertem Ereignispfad und
Gegenprobe) oder als `ungemessen` mit Grund geführt. Der Entwurf zu `user_oidc#925` existiert nur bei
geglückter Reproduktion und wurde von keinem Agenten gesendet.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-07-SUMMARY.md` when done
</output>
