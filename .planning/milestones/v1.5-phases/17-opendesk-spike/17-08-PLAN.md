---
phase: 17-opendesk-spike
plan: 08
type: execute
wave: 8
depends_on: ["17-07"]
files_modified:
  - docs/spike-opendesk.md
  - docs/contrib/opendesk-forum-antwort-christianlupus.md
  - docs/contrib/openproject-community-konto-anfrage.md
  - C:/Users/Student/Desktop/ISV-Call-Dossier-2026-09-14.md
autonomous: false
requirements: [OD-03]

must_haves:
  truths:
    - "Die Fragenliste für den 14.09. enthält die vier Pflichtfragen aus OD-01 wörtlich abgedeckt"
    - "Jede Frage trägt einen Grund, damit sie nicht wie Neugier klingt"
    - "Die drei von dieser Recherche beantworteten Fragen sind entfernt und nicht mitgeführt"
    - "Die Liste steht an zwei Stellen: versioniert im Bericht und im Dossier für den Termin"
    - "Zwei Entwürfe liegen unversendet im Repository, gesendet wird ausschließlich vom Owner"
    - "D-09: Die Fragenliste steht als Abschnitt des Berichts docs/spike-opendesk.md und ist damit versioniert"
    - "D-10: Dieselbe Fragenliste steht zusätzlich im Dossier Desktop/ISV-Call-Dossier-2026-09-14.md"
    - "D-11: Die zwei Entwürfe an christianlupus und an die OpenProject-Community liegen unversendet im Repository, gesendet wird ausschließlich vom Owner"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "Abschnitt 4 mit neun Fragen samt Grund und der Tabelle der entfernten Fragen"
      contains: "## 4. Fragenliste"
    - path: "docs/contrib/opendesk-forum-antwort-christianlupus.md"
      provides: "kurze Antwort im Nextcloud-Forum, Entwurf, unversendet"
      contains: "Entwurf"
    - path: "docs/contrib/openproject-community-konto-anfrage.md"
      provides: "Konto-Anfrage an die OpenProject-Community, Entwurf, unversendet"
      contains: "Entwurf"
  key_links:
    - from: "docs/spike-opendesk.md Abschnitt 4"
      to: "Desktop/ISV-Call-Dossier-2026-09-14.md"
      via: "dieselbe Fragenliste an beiden Stellen (D-10)"
      pattern: "ISV-Call"
---

<objective>
OD-03 wird erfüllt: die Fragenliste für den ISV-Call am 14.09. entsteht, versioniert als Abschnitt 4
des Spike-Berichts und zusätzlich im Dossier für den Termin (D-10). Dazu entstehen die zwei Entwürfe
aus D-11, die der Owner selbst sendet.

Purpose: Die Liste ist das Verhandlungsergebnis dieser Phase. Sie enthält genau das, was Recherche und
Messung nicht klären konnten, sie führt keine Frage mehr, die diese Recherche beantwortet hat, und
jede Frage trägt ihren Grund, damit im Gespräch die Folge einer Antwort sofort sichtbar ist.
Output: Abschnitt 4 im Bericht, derselbe Stand im Dossier, zwei unversendete Entwürfe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@.planning/REQUIREMENTS.md
@docs/spike-opendesk.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abschnitt 4 schreiben und denselben Stand ins Dossier übernehmen</name>
  <files>docs/spike-opendesk.md, C:/Users/Student/Desktop/ISV-Call-Dossier-2026-09-14.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"OD-03: was auf die Fragenliste gehört, und
      was nicht mehr" (Zeilen 1519 bis 1573): die vier Pflichtfragen, die fünf neuen Fragen, die drei
      entfallenen.
    - `.planning/REQUIREMENTS.md` OD-03 im Wortlaut (Zeile 31) und der Absatz über die nicht
      entschiedenen Verhandlungsfragen (Zeile 19).
    - `C:/Users/Student/Desktop/ISV-Call-Dossier-2026-09-14.md` vollständig: die bestehende Gliederung
      und die drei nicht-technischen Fragen aus dem Validierungsplan, die unberührt bleiben.
    - Die eigenen Abschnitte 1.4 und 2.5 des Berichts, damit jeder dort genannte offene Punkt in
      Abschnitt 4 eine Frage hat.
  </read_first>
  <action>
    Schreibe `## 4. Fragenliste für den ISV-Call am 14.09. (OD-03)` als numerierte Liste mit neun
    Fragen. Jede Frage besteht aus der Frage selbst und einem Absatz `Grund:`, der die Folge einer
    Antwort nennt. Reihenfolge und Inhalt:

    1 bis 4 sind die Pflichtfragen aus OD-01 und REQUIREMENTS.md und müssen wörtlich abgedeckt sein:
    das ZenDiS-Aufnahmeverfahren (wer entscheidet, nach welchen Kriterien, in welchem Zeitrahmen; die
    Folge: heißt die Antwort "nur als Teil der Distribution", heißt die nächste Roadmap-Phase nicht
    OpenProject-Werkzeuge, sondern Aufnahmefähigkeit herstellen); der Installationsweg in openDesk (ist
    eine External App vorgesehen und auf welchem Weg, mit dem Beleg, dass AppAPI im Deployment-Projekt
    an keiner Stelle vorkommt und der App Store aus ist); die AGPL-Konsequenz für die
    Enterprise-Positionierung (ein Audit-Log in einem AGPL-Repository kann kein exklusives
    kommerzielles Unterscheidungsmerkmal sein, Verhandlungs- und keine Recherchefrage); die
    Abschaltung von Talk und Kontakten (`spreed: enabled: false`, `contacts: enabled: false`, dazu
    `comments` und `circles` aus; zwei der neun Werkzeugfamilien liegen dort dunkel, und die Frage ist,
    was das für die Store-Beschreibung und einen openDesk-Zuschnitt bedeutet: eigene Fassung,
    Fähigkeitsprüfung zur Laufzeit oder gar nicht bewerben).

    5 bis 9 sind die von der Phasenrecherche neu erzeugten: der Termin von openDesk auf Nextcloud 34
    oder höher (mit dem Aufhänger `kubernetes-install` ab `app_api` 34, vier Freigabearten, vier
    CI-Workflows, auf 33.0.7 nicht vorhanden); ob das openDesk-Nextcloud-Image `app_api` enthält und
    eingeschaltet hat; in welchem Modus `integration_openproject` in openDesk läuft, `oauth2` oder
    `oidc`, und hier den in dieser Phase gemessenen Unterschied als Aufhänger nennen (Ergebnis von S4
    gegen S5, mit dem Verweis auf Abschnitt 2.1; ist S5 ungemessen geblieben, sagt die Frage das und
    stützt sich auf den Quellcodekommentar "Token exchange needs to be initiated from the UI");
    ob es einen vorgesehenen Weg gibt, wie eine AppAPI-ExApp ohne PHP-Anteil ein audience-korrektes
    Token für eine Schwesterkomponente bekommt, ohne Browsersitzung; warum die AS-Metadaten von
    OpenProject `code_challenge_methods_supported` nicht bewerben, obwohl `force_pkce` im
    Doorkeeper-Initializer steht (mit dem Vermerk, dass diese Frage eher in den OpenProject-Kanal
    gehört als in den ISV-Call).

    Darunter eine Tabelle `Nicht mehr auf der Liste` mit den drei Fragen der Ausgangsrecherche und dem
    Grund ihres Entfallens (AppAPI-Aktivierung, jetzt präziser als Frage 6; PKCE-Annahme, aus der
    Quelle beantwortet, Rest ist Frage 9; Capability, aus der Quelle beantwortet). Eine Frage, die
    diese Phase beantwortet hat, wird entfernt und nicht mitgeführt.

    Übernimm danach denselben Stand in `C:/Users/Student/Desktop/ISV-Call-Dossier-2026-09-14.md`: einen
    neuen Abschnitt "Technische Fragen aus Phase 17 (Stand <Datum>)" mit denselben neun Fragen und
    Gründen, plus einer Zeile, wo der Beleg steht (`docs/spike-opendesk.md`, Abschnitt und Repository).
    Die drei nicht-technischen Fragen des Dossiers (Verkaufsmechanik, Referenz-ISVs, Kanal des
    Enterprise-Flags) bleiben unberührt, sie kommen aus dem Validierungsplan. Diese Datei liegt
    außerhalb des Repositories und wird nicht committet; nenne das in der Plan-SUMMARY.
  </action>
  <verify>
    <automated>grep -c '^[0-9]\+\.' docs/spike-opendesk.md; for s in "ZenDiS" "AGPL" "spreed" "kubernetes-install" "code_challenge_methods_supported" "Nicht mehr auf der Liste"; do grep -v '^#' docs/spike-opendesk.md | grep -q "$s" || { echo "FEHLT: $s"; exit 1; }; done; grep -c "Grund:" docs/spike-opendesk.md; grep -q "Phase 17" "/c/Users/Student/Desktop/ISV-Call-Dossier-2026-09-14.md" &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 4 enthält neun numerierte Fragen, und `grep -c "Grund:"` ergibt mindestens neun.
    - Die vier Pflichtfragen aus OD-01 sind inhaltlich vollständig abgedeckt und namentlich erkennbar
      (`ZenDiS`, `Installationsweg`, `AGPL`, `spreed` beziehungsweise `Talk und Kontakte`).
    - Die Tabelle `Nicht mehr auf der Liste` nennt genau drei Fragen mit Grund.
    - Frage 7 verweist auf Abschnitt 2.1 und nennt entweder das gemessene Ergebnis von S5 oder, wenn
      S5 ungemessen blieb, den Quellcodekommentar als Aufhänger.
    - Das Dossier auf dem Desktop trägt einen Abschnitt mit Datum, denselben neun Fragen und dem
      Verweis auf `docs/spike-opendesk.md`; die drei nicht-technischen Fragen sind unverändert.
    - Ungemessen-Fallback: ist eine Frage durch die Messung beantwortet worden, wird sie aus der Liste
      entfernt und in der Tabelle `Nicht mehr auf der Liste` mit dem Messwert als Grund geführt.
      Blieben Messungen ungemessen, tragen die betroffenen Fragen ausdrücklich, dass die Antwort aus
      dieser Phase nicht kommt.
  </acceptance_criteria>
  <done>Die Fragenliste steht versioniert im Bericht und im Dossier, jede Frage trägt ihren Grund, und beantwortete Fragen sind entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: Die zwei Entwürfe der Rückkanäle schreiben, unversendet</name>
  <files>docs/contrib/opendesk-forum-antwort-christianlupus.md, docs/contrib/openproject-community-konto-anfrage.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-CONTEXT.md` D-11 im Wortlaut samt der Owner-Zusage vom
      28.08.
    - `Desktop/openDesk-Anfragen-2026-08-28.md`: der bereits gesendete Forumsbeitrag über die
      OCS-Routen und der Versandstatus der vier Anfragen.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Die OCS-Fläche" (Zeilen 606 bis 640) und
      Open Question 3 (Zeilen 1639 bis 1645).
    - `docs/contrib/227-pr-body.md` als Formvorbild.
  </read_first>
  <action>
    Schreibe zwei Entwürfe, beide englisch, weil beide Kanäle englisch sind, beide mit der Kopfzeile
    `Status: Entwurf, nicht gesendet. Versand ausschließlich durch den Owner.`

    Erstens `docs/contrib/opendesk-forum-antwort-christianlupus.md`: eine kurze Antwort an
    `christianlupus` im Nextcloud-Forum. Inhalt in drei Absätzen: Dank für den Hinweis auf die
    OpenProject-Community; der Stand der eigenen Messung in zwei Sätzen (die 17 OCS-Routen sind aus
    `appinfo/routes.php` an Tag `v3.1.1` ausgezählt, die drei Lücken benannt: keine Route für ein
    einzelnes Arbeitspaket per Id, keine für Kommentare, keine für "meine Arbeit", dafür
    `work-packages/{id}/file-links`); die eigentliche Frage, präzise gestellt: sind diese Routen als
    Schnittstelle für andere Apps gedacht oder Innenleben des eigenen Frontends, also gilt für sie eine
    Stabilitätszusage. Kein Werbetext, kein Link auf das eigene Produkt außer als Kontext, wenn er die
    Frage verständlich macht.

    Zweitens `docs/contrib/openproject-community-konto-anfrage.md`: eine Anfrage an die
    OpenProject-Community um ein Konto, weil die Selbstregistrierung mit HTTP 400
    "Registration not allowed" antwortet. Inhalt: wer fragt und in welchem Zusammenhang (ein
    AGPL-lizenzierter MCP-Connector für Nextcloud, openDesk-Umfeld), warum ein Konto gebraucht wird
    (eine technische Frage zur Stabilität der OCS-Fläche von `integration_openproject` im
    Community-Forum stellen), der gemessene HTTP-Status der Selbstregistrierung mit Datum, und die
    Bitte um den vorgesehenen Weg.

    Beide Entwürfe enthalten keine Behauptung über fremden Code ohne Datei, Zeile und Tag, und keine
    Aussage, die diese Phase nicht gemessen hat. Sende nichts: kein `gh`, kein Browser, kein
    Playwright. Der Owner sendet (D-11, Regel aus context_agent#230 und feedback_owner_sends_outreach).
  </action>
  <verify>
    <automated>for f in docs/contrib/opendesk-forum-antwort-christianlupus.md docs/contrib/openproject-community-konto-anfrage.md; do test -f "$f" || { echo "FEHLT: $f"; exit 1; }; grep -qi "nicht gesendet" "$f" || { echo "KEINE STATUSZEILE: $f"; exit 1; }; done; grep -q "v3.1.1" docs/contrib/opendesk-forum-antwort-christianlupus.md &amp;&amp; grep -q "400" docs/contrib/openproject-community-konto-anfrage.md &amp;&amp; ! grep -rniE "eyJ|client_secret|Bearer [A-Za-z0-9]" docs/contrib/</automated>
  </verify>
  <acceptance_criteria>
    - Beide Dateien existieren und tragen die Statuszeile mit `nicht gesendet` und dem Hinweis, dass
      ausschließlich der Owner sendet.
    - Der Forumsentwurf nennt Tag `v3.1.1`, die drei Lücken und `file-links`, und stellt die
      Stabilitätsfrage als Frage.
    - Der Konto-Entwurf nennt den gemessenen HTTP-Status 400 der Selbstregistrierung mit Datum.
    - Kein Geheimnis in `docs/contrib/`; kein Versandbefehl wurde ausgeführt.
    - Ungemessen-Fallback: entfällt, dieser Task schreibt Text. Fehlt ein Beleg für eine Aussage im
      Entwurf, wird die Aussage gestrichen und nicht abgeschwächt.
  </acceptance_criteria>
  <done>Zwei Entwürfe liegen unversendet im Repository, beide belegen ihre technischen Aussagen mit Tag und Datei.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner liest Fragenliste und Entwürfe</name>
  <files>docs/spike-opendesk.md, docs/contrib/opendesk-forum-antwort-christianlupus.md, docs/contrib/openproject-community-konto-anfrage.md, docs/contrib/user-oidc-925-kommentar.md</files>
  <read_first>
    - `docs/spike-opendesk.md` Abschnitt 4.
    - die beiden Entwürfe aus Task 2 und, falls vorhanden, der Entwurf aus Plan 17-07.
  </read_first>
  <action>
    Halte an, lege dem Owner Abschnitt 4 und die Entwürfe aus `docs/contrib/` zum Lesen vor und warte
    auf das Freigabesignal. Arbeite benannte Änderungen an Liste und Entwürfen ein, bevor der Plan
    abgeschlossen wird, und halte den Moment der Freigabe mit UTC-Zeitstempel in der Plan-SUMMARY fest
    (Lehre aus Phase 16). Sende nichts: kein `gh issue comment`, kein Browser, kein Playwright. Prüfe
    zusätzlich, dass der Entwurf zu `user_oidc#925` nur dann existiert, wenn Plan 17-07 eine geglückte
    Live-Reproduktion protokolliert hat (D-08); existiert er ohne Repro, wird er entfernt.
  </action>
  <what-built>
    Abschnitt 4 des Berichts trägt neun Fragen mit Gründen, das Dossier auf dem Desktop trägt denselben
    Stand, und in `docs/contrib/` liegen zwei Entwürfe (Forumsantwort und Konto-Anfrage), dazu bei
    geglückter Reproduktion der Entwurf zu `user_oidc#925` aus Plan 17-07. Es wurde nichts gesendet
    und nichts veröffentlicht.
  </what-built>
  <how-to-verify>
    1. Lies `docs/spike-opendesk.md` Abschnitt 4. Fehlt eine Frage, die im Gespräch am 14.09. gebraucht
       wird, oder steht eine darin, die die Messung beantwortet hat, sage welche.
    2. Lies die zwei Entwürfe in `docs/contrib/`. Prüfe die Wortwahl: es sind Fragen, kein Werbetext.
    3. Prüfe, ob der Entwurf zu `user_oidc#925` existiert und ob das zum Ergebnis von S5 passt: er
       darf nur existieren, wenn die Live-Reproduktion geglückt ist (D-08).
    4. Senden ist dein Schritt und keiner dieser Pläne: Forum, OpenProject-Community und GitHub-Issue
       gehen ausschließlich von dir raus.
  </how-to-verify>
  <verify>
    <human-check>Owner bestätigt Fragenliste und Entwürfe oder benennt Änderungen.</human-check>
    <automated>ls docs/contrib/ &amp;&amp; grep -c '^[0-9]\+\.' docs/spike-opendesk.md</automated>
  </verify>
  <acceptance_criteria>
    - Die Freigabe oder die benannten Änderungen liegen als Text vor und stehen in der Plan-SUMMARY,
      mit UTC-Zeitstempel des Moments der Freigabe (Lehre aus Phase 16).
    - Benannte Änderungen sind vor dem Abschluss des Plans eingearbeitet.
    - Es wurde nichts gesendet; die SUMMARY sagt das ausdrücklich.
    - Ungemessen-Fallback: bleibt die Freigabe aus, wird der Plan mit dem Vermerk abgeschlossen, dass
      Liste und Entwürfe unfreigegeben im Repository liegen, und nichts wird gesendet. Der Bericht
      bleibt inhaltlich unverändert.
  </acceptance_criteria>
  <resume-signal>Tippe "freigegeben" oder beschreibe die Änderungen an Liste und Entwürfen.</resume-signal>
  <done>Der Owner hat Fragenliste und Entwürfe gelesen, Änderungen sind eingearbeitet, und gesendet wurde nichts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository zu fremdem Kanal | Zwei Entwürfe sind für Forum und Community gedacht und dürfen erst nach Owner-Freigabe und nur von ihm hinausgehen |
| Bericht zu Öffentlichkeit | Abschnitt 4 nennt Verhandlungsfragen, die die eigene Position offenlegen |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | docs/contrib/, Abschnitt 4 | hoch | mitigate | Kein Geheimnis und kein Nutzerdatum in den Entwürfen; das Gate greift über `docs/contrib/` nach `eyJ`, `Bearer `, `client_secret` |
| T-17-07 | Repudiation | Versand durch einen Agenten | mittel | mitigate | Kein `gh`, kein Browser, kein Playwright in diesen Tasks; die Statuszeile jedes Entwurfs sagt `nicht gesendet`, und die SUMMARY bestätigt es. Owner sendet (D-11) |
| T-17-05 | Repudiation | technische Aussagen in fremden Kanälen | mittel | mitigate | Keine Behauptung über fremden Code ohne Datei, Zeile und Tag; der Entwurf zu `user_oidc#925` existiert nur mit Live-Repro (Regel aus context_agent#230) |
| T-17-04 | Tampering | Produktionsbaum | mittel | mitigate | Geschrieben werden nur `docs/`-Dateien und eine Datei außerhalb des Repositories; `src/` bleibt unberührt |
</threat_model>

<verification>
- Abschnitt 4 trägt neun Fragen mit je einem Grund und die Tabelle der drei entfallenen Fragen.
- Das Dossier auf dem Desktop trägt denselben Stand mit Datum und Belegverweis.
- Beide Entwürfe existieren mit Statuszeile `nicht gesendet`.
- `git status --short src/ appinfo/ pyproject.toml uv.lock` leer.
</verification>

<success_criteria>
OD-03 ist erfüllt: die Fragenliste liegt versioniert im Bericht und im Dossier, sie enthält die vier
Pflichtfragen und keine beantwortete Frage mehr, und die zwei Rückkanäle stehen als unversendete
Entwürfe bereit.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-08-SUMMARY.md` when done
</output>
