---
phase: 17-opendesk-spike
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - compose.spike-opendesk.yml
  - deploy/Caddyfile.spike-opendesk
  - scripts/bootstrap_spike_opendesk.sh
  - .env.spike-opendesk.example
  - .gitignore
  - docs/spike-opendesk.md
autonomous: true
requirements: [OD-01, OD-02]

must_haves:
  truths:
    - "Die Messumgebung Stufe A steht mit gepinnten Fassungen und veröffentlicht keinen Port außerhalb von 127.0.0.1"
    - "Der Berichtskopf nennt die aus der laufenden Instanz gelesenen Fassungen, nicht die der Recherche"
    - "S0 sagt gemessen, ob diese ExApp auf Nextcloud 33.0.7 installiert und antwortet, mit Gegenprobe falsches APP_SECRET"
    - "D-06: Die SSRF-Grenze aus v1.1 ist gegen einen internen Docker-Dienstnamen gemessen, nicht hergeleitet, samt Gegenprobe mit einem öffentlichen Namen"
    - "D-02: Die Messumgebung ist lokal in Docker gepinnt auf Nextcloud 33.0.7 und OpenProject 17.7.2, kein latest"
    - "D-03: Es entsteht keine gemietete Box, und was lokal nicht messbar war, steht als ungemessen im Bericht und nie als verworfen"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "compose.spike-opendesk.yml"
      provides: "Stufe A und B als eine Topologie mit Profilen op und oidc, eigenes Projekt, eigenes Subnetz, eigene Ports"
      contains: "nextcloud:33.0.7-apache"
    - path: "scripts/bootstrap_spike_opendesk.sh"
      provides: "AppAPI, Deploy-Daemon, Image, ExApp-Registrierung gegen die Spike-Topologie, idempotent"
    - path: ".env.spike-opendesk.example"
      provides: "Variablennamen der Verbindungsdatei, ohne einen einzigen echten Wert"
    - path: "docs/spike-opendesk.md"
      provides: "Kopf mit gelesenen Fassungen, S0 in Abschnitt 1.3, SSRF-Messung in Abschnitt 2.3"
      contains: "## 2. Nutzeridentität gegen OpenProject (OD-02)"
  key_links:
    - from: "compose.spike-opendesk.yml"
      to: "deploy/Caddyfile.spike-opendesk"
      via: "read-only Bind-Mount des Caddy-Dienstes"
      pattern: "Caddyfile.spike-opendesk"
    - from: "scripts/bootstrap_spike_opendesk.sh"
      to: ".env.spike-opendesk"
      via: "geschriebene Verbindungsdatei, git-ignoriert"
      pattern: "\\.env\\.spike-opendesk"
---

<objective>
Stufe A der Messumgebung entsteht in ihrem ersten Teil: Nextcloud 33.0.7 in der HaRP-Topologie mit
dieser ExApp, alles gepinnt und nur auf Loopback. Darauf fallen zwei Messwerte: S0 (hält die
Ein-Klick-Kette auf 33.0.7, dem openDesk-Stand) und die SSRF-Messung aus D-06.

Purpose: S0 ist der Messteil von OD-01 Hürde 3 und ersetzt einen geerbten Nachweis auf der falschen
Hauptversion. Die SSRF-Messung hängt an keinem der beiden Zugriffswege und ist deshalb hier am
billigsten. Der Schnitt vor OpenProject und vor Keycloak ist Absicht (Pitfall 1): Stufe A muss
protokolliert sein, bevor der teure Teil beginnt.
Output: vier neue Dateien der Messumgebung, ein gefüllter Berichtskopf, Abschnitt 1.3 mit S0 und
Abschnitt 2.3 mit der SSRF-Antwort.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@compose.exapp.yml
@deploy/Caddyfile
@docs/spike-dav.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike-Topologie anlegen, gepinnt, auf Loopback, mit Profilen</name>
  <files>compose.spike-opendesk.yml, deploy/Caddyfile.spike-opendesk, scripts/bootstrap_spike_opendesk.sh, .env.spike-opendesk.example, .gitignore</files>
  <read_first>
    - `compose.exapp.yml` vollständig: die sieben Einzelprobleme, die diese Datei schon löst
      (Caddy-Regel für `/exapps/*`, `OVERWRITEHOST`, `TRUSTED_PROXIES` auf eine Adresse,
      feste Caddy-Adresse, `NEXTCLOUD_TRUSTED_DOMAINS` samt `caddy`, HaRP-Shared-Key ohne
      Vorgabewert, lokale Registry, `ip_range` außerhalb der statischen Hälfte).
    - `deploy/Caddyfile` vollständig.
    - `scripts/bootstrap_exapp.sh` Zeilen 1 bis 120 (Flags, nicht überschreibbare Variablen,
      `ENV_FILE`) und die Zeilen um `app_api:daemon:register` und `app_api:app:register`.
    - `.env.exapp.example` als Muster für eine Beispieldatei ohne echte Werte.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Messumgebung: Docker-Einzelheiten"
      (Zeilen 1102 bis 1200) und §"Don't Hand-Roll" (Zeilen 1303 bis 1316).
  </read_first>
  <action>
    Prüfe zuerst den Speicher der WSL2-VM (`wsl.exe --version` und die Datei
    `C:\Users\Student\.wslconfig`, Schlüssel `memory=`). Weniger als 8 GB ist der teuerste vermeidbare
    Fehlweg der Phase (A3): dann steht als erste Zeile im Protokoll, wieviel Speicher zur Verfügung
    stand, und der Wert wird in `.planning/phases/17-opendesk-spike/17-02-SUMMARY.md` genannt.

    Lege `compose.spike-opendesk.yml` als Kopie von `compose.exapp.yml` an, mit genau diesen
    Abweichungen und je Abweichung einem Kommentar, der den Grund nennt:
    `name: nc-mcp-spike-od`; Netzname `nc-mcp-spike-od-net`, Subnetz `172.29.43.0/24`,
    `ip_range: 172.29.43.128/25`, feste Caddy-Adresse `172.29.43.10`, damit die Topologie neben
    `compose.exapp.yml` bestehen kann; Nextcloud-Bildmarke `nextcloud:33.0.7-apache` statt
    `34.0.3-apache`, weil die openDesk-Zielumgebung auf 33.0.7 gepinnt ist (D-02); Containernamen mit
    dem Präfix `nc-mcp-spike-od-`; `TRUSTED_PROXIES` und `HP_TRUSTED_PROXY_IPS` auf `172.29.43.10`;
    Caddy veröffentlicht `127.0.0.1:${NC_SPIKE_OD_PORT:-8091}:80` für Nextcloud und
    `127.0.0.1:8082:8082` für OpenProject (gleiche Portnummer innen und außen, das ist die Auflösung
    der Namensfalle); Registry auf `127.0.0.1:5001:5000`; der Dienst `greenmail` entfällt.
    Nextcloud bekommt `extra_hosts` mit `op.localtest.me:172.29.43.10`, weil
    `integration_openproject` OpenProject aus dem Nextcloud-Container heraus unter
    `openproject_instance_url` aufruft und der Name aus dem Browser und aus dem Container gleich
    lauten muss. Schreibe als Kommentar dazu, dass der ExApp-Container diesen Eintrag nicht bekommt,
    weil ihn der Deploy-Daemon erzeugt, und dass die Egress-Kontrollmessung deshalb den
    Compose-Dienstnamen `openproject` benutzt und nicht `op.localtest.me`.

    Hänge zwei Dienste mit Profilen an, damit der Stufenschnitt in der Datei sichtbar ist:
    `openproject` mit `profiles: ["op"]`, Bildmarke `openproject/openproject:17.7.2`, ohne
    veröffentlichten Port (Caddy ist die Vordertür), Umgebung `SECRET_KEY_BASE` ohne Vorgabewert nach
    dem `HP_SHARED_KEY`-Muster (WR-11), `OPENPROJECT_HOST__NAME=op.localtest.me:8082`,
    `OPENPROJECT_HTTPS=false`, `OPENPROJECT_DEFAULT__LANGUAGE=en`, benannte Bände für
    `/var/openproject/pgdata` und `/var/openproject/assets`;
    `keycloak` mit `profiles: ["oidc"]`, Bildmarke `quay.io/keycloak/keycloak:26.7.0`, ohne
    veröffentlichten Port, Startbefehl `start-dev`, dazu ein Kommentar, dass `--import-realm` eine
    vorhandene Realm überspringt und die Topologie zwischen zwei Läufen ihr Keycloak-Band verwerfen
    muss. Setze in keinem Dienst ein Geheimnis mit Vorgabewert und in keinem Dienst
    `OPENPROJECT_AUTHENTICATION_GLOBAL__BASIC__AUTH_USER` oder einen API-Schlüssel (Pitfall 2).

    Lege `deploy/Caddyfile.spike-opendesk` als Kopie von `deploy/Caddyfile` an und ergänze einen
    zweiten Zuhörblock `:8082`, der auf `openproject:80` weiterleitet, mit den Headern, die Rails
    braucht (`X-Forwarded-Proto http`, `Host` durchreichen). Kommentiere, warum die Portnummer innen
    und außen gleich sein muss.

    Lege `scripts/bootstrap_spike_opendesk.sh` als Kopie von `scripts/bootstrap_exapp.sh` an und
    ändere ausschließlich die Topologie-Konstanten: `COMPOSE_FILE="compose.spike-opendesk.yml"`,
    `PROJECT_NAME="nc-mcp-spike-od"`, `HARP_CONTAINER` auf `nc-mcp-spike-od-harp`,
    `ENV_FILE=".env.spike-opendesk"`, `HOST_PORT="${NC_SPIKE_OD_PORT:-8091}"`,
    `NETWORK_NAME="nc-mcp-spike-od-net"`, die Registry-Adresse auf `127.0.0.1:5001`. Die Regel WR-07
    bleibt: `COMPOSE_FILE` ist nicht über das Environment überschreibbar. Die Flags `--staging` und
    der zugehörige Zweig entfallen, `--manual` bleibt. Setze in den Kopfkommentar, dass dieses Skript
    eine Wegwerf-Messumgebung für Phase 17 bedient und niemals eine Installation.

    Lege `.env.spike-opendesk.example` nach dem Muster von `.env.exapp.example` an: nur
    Variablennamen und Kommentare, kein einziger echter Wert, die Geheimniszeilen auskommentiert.
    Nenne dort auch die Variablen, die die späteren Pläne brauchen: `SECRET_KEY_BASE`,
    `OP_ADMIN_PASSWORD`, `OP_USER_A_PASSWORD`, `OP_USER_B_PASSWORD`, `OP_API_TOKEN`,
    `OP_OAUTH_CLIENT_ID`, `OP_OAUTH_CLIENT_SECRET`, `NC_OP_CLIENT_ID`, `NC_OP_CLIENT_SECRET`,
    `KC_CLIENT_SECRET`. Ergänze `.gitignore` um die Zeile `.env.spike-opendesk` mit einem
    zweizeiligen Kommentar, was die Datei trägt.

    Fahre die Topologie ohne Profile hoch (`docker compose -f compose.spike-opendesk.yml up -d
    --wait`, davor `export HP_SHARED_KEY="$(openssl rand -hex 32)"`) und danach
    `bash scripts/bootstrap_spike_opendesk.sh`. Ist ein Port besetzt, weil die Topologie aus
    `compose.exapp.yml` läuft, halte sie mit `docker compose -f compose.exapp.yml stop` an; niemals
    `down -v`, weil das fremde Messdaten vernichtet.
  </action>
  <verify>
    <automated>grep -q "nextcloud:33.0.7-apache" compose.spike-opendesk.yml &amp;&amp; grep -q "openproject/openproject:17.7.2" compose.spike-opendesk.yml &amp;&amp; grep -q "quay.io/keycloak/keycloak:26.7.0" compose.spike-opendesk.yml &amp;&amp; ! grep -nE ':(latest|[0-9]+)-?apache?"?$' compose.spike-opendesk.yml | grep -v "33.0.7"; grep -c "127.0.0.1:" compose.spike-opendesk.yml &amp;&amp; ! grep -q "0.0.0.0:" compose.spike-opendesk.yml &amp;&amp; grep -q "^\.env\.spike-opendesk$" .gitignore &amp;&amp; docker compose -f compose.spike-opendesk.yml ps --format '{{.Service}} {{.State}}'</automated>
  </verify>
  <acceptance_criteria>
    - `compose.spike-opendesk.yml` nennt genau die drei gepinnten Bildmarken `nextcloud:33.0.7-apache`,
      `openproject/openproject:17.7.2`, `quay.io/keycloak/keycloak:26.7.0` und kein `latest`.
    - Jede `ports`-Zeile beginnt mit `127.0.0.1:`; keine Zeile bindet `0.0.0.0`.
    - `openproject` steht unter `profiles: ["op"]`, `keycloak` unter `profiles: ["oidc"]`; ein `up`
      ohne Profil startet keinen von beiden.
    - `.gitignore` enthält eine Zeile `.env.spike-opendesk`; `git status --short` zeigt diese Datei
      nicht als unverfolgt an, nachdem der Bootstrap sie geschrieben hat.
    - `docker compose -f compose.spike-opendesk.yml ps` zeigt `caddy`, `nextcloud`, `appapi-harp`
      und `registry` im Zustand `running`; `docker compose ... exec -T --user www-data nextcloud php
      occ status` meldet `installed: true`.
    - Ungemessen-Fallback: bricht der Bootstrap am ExApp- oder HaRP-Heartbeat (die häufigste Ursache
      sind `OVERWRITEHOST` oder die Caddy-Regel), wird der Fehlertext samt
      `docker logs nc-mcp-spike-od-harp` in die Plan-SUMMARY genommen; ein zweiter Fehlschlag ist
      selbst der OD-01-Befund und wird in Abschnitt 1.3 als `S0: ungemessen, Registrierung auf
      33.0.7 gescheitert` mit dem Fehlertext geführt.
    - Reichweite dieses Fallbacks, eng gefasst: auf `ungemessen` geht daraufhin **nur S0**. Die
      SSRF-Messung aus Task 3 geht zusätzlich auf `ungemessen`, aber ausschließlich dann, wenn der
      ExApp-Container selbst nicht ansprechbar ist. Der Ausführende prüft das mit
      `docker ps --filter name=mcp_connector --format '{{.Names}} {{.State}}'`: leere Ausgabe oder
      ein Zustand ungleich `running` bedeutet nicht ansprechbar, sonst wird Task 3 normal gemessen.
    - Die Pläne 17-03 und 17-04 hängen **nicht** am Heartbeat und werden von diesem Fallback nicht
      erfasst. 17-03 erreicht OpenProject über Caddy und das Docker-Netz, also über genau die
      Dienste, die dieser Task vor dem Heartbeat schon laufend belegt hat; 17-04 (Weg 1) braucht
      Nextcloud überhaupt nicht (17-RESEARCH.md §"Pitfall 1", Kostentabelle Zeile 1340: "Weg 1
      ist der unabhängigste Teil und braucht Nextcloud nicht") und ist der ausdrückliche
      Ausweichweg, wenn Weg 0 hängt. Bevor über die beiden entschieden wird, prüft der Ausführende
      genau zwei Bedingungen:
      erstens `docker compose -f compose.spike-opendesk.yml ps --format '{{.Service}} {{.State}}' | grep -E '^caddy +running'`
      liefert eine Zeile, zweitens `docker network inspect nc-mcp-spike-od-net -f '{{.Name}}'`
      antwortet mit `nc-mcp-spike-od-net` und Exit-Code 0. Sind beide Bedingungen erfüllt, laufen
      17-03 und 17-04 unverändert weiter, unabhängig vom Ergebnis des Heartbeats. Nur wenn eine der
      beiden Bedingungen verletzt ist (`caddy` nicht `running` oder das Netz fehlt), werden 17-03 und
      17-04 auf `ungemessen` gesetzt, und der Grund wird mit dem Ausgabetext der verletzten Bedingung
      in die Plan-SUMMARY genommen.
    - Die Pläne 17-05, 17-06 und 17-07 brauchen die registrierte ExApp als Aufrufer und gehen nach
      einem zweiten Heartbeat-Fehlschlag auf `ungemessen`.
  </acceptance_criteria>
  <done>Die Spike-Topologie steht gepinnt, ausschließlich auf Loopback und mit sichtbarem Stufenschnitt; die Verbindungsdatei ist git-ignoriert.</done>
</task>

<task type="auto">
  <name>Task 2: S0 messen und den Berichtskopf mit gelesenen Fassungen füllen</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `docs/spike-dav.md` §"The two controls": die zwei Kontrollen (kein App-Passwort im Prozess,
      falsches `APP_SECRET` liefert 401) und die Regel, dass Headerwerte mit `APP_SECRET` nie
      gedruckt werden.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"S0 bis S6" Zeile S0 (Zeile 797) und
      §"Versionsmatrix für Stufe A" (Zeilen 814 bis 823), Annahme A5.
    - `tests/integration/test_http_tool_call.py`: der bestehende Weg, einen Werkzeugaufruf gegen die
      laufende Topologie zu fahren.
    - `docs/spike-opendesk.md` Kopfblock aus Plan 17-01.
  </read_first>
  <action>
    Lies alle Fassungen aus der laufenden Instanz, nicht aus der Recherche:
    `occ status` für Server und Build, `occ app:list` für `app_api` und, sobald installiert,
    `integration_openproject`, `occ app_api:app:list` für den Zustand der ExApp, sowie die tatsächlich
    gelaufene HaRP-Fassung über `docker image inspect ghcr.io/nextcloud/nextcloud-appapi-harp:release`
    (Digest und Erstellungsdatum, weil der Tag gleitend ist). Trage die Werte in den Kopfblock des
    Berichts ein und ersetze dabei jedes `noch nicht gemessen (Plan 17-02)`, das jetzt einen Wert hat.
    Felder, die zu diesem Zeitpunkt keinen Wert haben (`user_oidc`, Keycloak, OpenProject), tragen
    `noch nicht gemessen (Plan 17-03 bzw. 17-07)`.

    Miss S0 in drei Schritten und schreibe die Werte in Abschnitt 1.3 unter der Überschrift
    "S0: hält die Kette auf 33.0.7":
    erstens `occ app_api:app:list` zeigt diese ExApp mit `enabled` und der Fassung aus
    `appinfo/info.xml`;
    zweitens ein Werkzeugaufruf gegen die laufende Topologie: fahre
    `set -a && . ./.env.spike-opendesk && set +a` und danach
    `uv run pytest tests/integration/test_http_tool_call.py -m integration -q`. Verlangt die Datei
    etwas, das die Spike-Topologie nicht hat, ist der Rückfall ein Aufruf ohne neue Datei: `curl` auf
    `http://127.0.0.1:8091/ocs/v2.php/cloud/user` mit den Headern `OCS-APIRequest: true`,
    `EX-APP-ID`, `EX-APP-VERSION` und `AUTHORIZATION-APP-API` (Base64 von `alice:<APP_SECRET>`),
    Erwartung 200 und `ocs.data.id` gleich `alice`;
    drittens die Gegenprobe: derselbe Aufruf mit einem `APP_SECRET` aus 64 Nullen muss mit einem
    Status ungleich 200 abgewiesen werden, erwartet 401. Ohne diese Zeile beweist der zweite Schritt
    nichts.

    Halte in Abschnitt 1.3 zusätzlich den Satz fest, der die Aussage von OD-01 abschließt: dieser
    Nachweis steht jetzt auf 33.0.7 und ersetzt den geerbten Nachweis auf 34.0.2 und 34.0.3. Nenne die
    `app_api`-Fassung ausdrücklich, weil Annahme A5 daran hängt: liegt sie über 33.0.0, ändert das die
    Kubernetes-Aussage aus Abschnitt 1.2 und der Bericht muss das sagen.

    Protokolliere nach der Geheimnisregel: Statuscodes, `ocs.data.id`, Fassungen, Digest-Präfix. Kein
    `APP_SECRET`, kein Wert von `AUTHORIZATION-APP-API`, keine Kopfzeile mit Base64.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "33.0.7 (Build" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "S0" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "401" &amp;&amp; ! grep -rniE "eyJ|Bearer [A-Za-z0-9]|refresh_token=|client_secret=|AUTHORIZATION-APP-API: " docs/spike-opendesk.md</automated>
  </verify>
  <acceptance_criteria>
    - Der Kopfblock nennt Nextcloud mit Fassung und Build aus `occ status`, die `app_api`-Fassung aus
      `occ app:list` und die HaRP-Fassung mit Digest-Präfix.
    - Abschnitt 1.3 trägt S0 mit drei Zeilen: Zustand der ExApp, ein Werkzeugaufruf mit Status und
      dem Nutzernamen, unter dem er lief, und die Gegenprobe mit 64 Nullen und Status 401.
    - Der Satz, der den geerbten 34.0.x-Nachweis durch den 33.0.7-Nachweis ersetzt, steht im Text.
    - Kein Treffer für `eyJ`, `Bearer `, `refresh_token=`, `client_secret=` oder einen Headerwert von
      `AUTHORIZATION-APP-API` in der Datei.
    - Ungemessen-Fallback: antwortet der Werkzeugaufruf mit HTML oder einem 3xx auf `/login`, steht
      S0 als `nicht erreicht` mit der Antwortform da, nicht als Fehler; scheitert die Registrierung
      ganz, steht `S0: ungemessen` mit dem Fehlertext und dem Datum, und Abschnitt 1.3 sagt
      ausdrücklich, dass die Ein-Klick-Kette auf 33.0.7 damit ungeprüft bleibt.
  </acceptance_criteria>
  <done>Der Berichtskopf trägt gelesene Fassungen, und S0 steht als Messwert mit Gegenprobe oder als begründetes Ungemessen im Bericht.</done>
</task>

<task type="auto">
  <name>Task 3: SSRF-Grenze gegen einen internen Dienstnamen messen (D-06)</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `src/mcp_connector/oauth/cimd.py` Zeilen 1 bis 9 (Modul-Docstring), 168 bis 201
      (`target_allowed`), 258 bis 307 (`resolve_addresses`). Nur lesen, nicht ändern.
    - `tests/unit/test_oauth_cimd.py` Zeilen 179 bis 222: der bestehende Negativkatalog und die drei
      gemessenen Lücken.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"SSRF-Messung (D-06)" (Zeilen 1022 bis
      1098).
  </read_first>
  <action>
    Miss die Grenze im laufenden ExApp-Container, mit demselben Resolver, den die Produktion benutzt,
    und ohne eine Zeile Produktionscode zu ändern. Finde den Container mit
    `docker ps --filter name=mcp_connector --format '{{.Names}}'` und fahre die drei Schritte über
    `docker exec <name> python -c ...`:
    erstens `cimd._system_addresses("openproject", 80)` und `cimd._system_addresses("nextcloud", 80)`,
    Erwartung private Adressen aus `172.29.43.0/24`;
    zweitens dieselben Adressliterale durch `cimd.target_allowed()`, Erwartung `False`;
    drittens `cimd.resolve_addresses("openproject", 80)`, Erwartung `None` samt der Log-Zeile
    "a document target was refused: an address of it is not public".
    Gegenprobe im selben Lauf: `cimd.resolve_addresses("one.one.one.one", 443)` oder ein anderer
    öffentlicher Name muss eine Adressliste liefern. Ohne diese Zeile ist das `None` auch mit einem
    kaputten Resolver im Container erklärbar. Kennt der Container kein interaktives `python`, ist der
    Rückfall `docker run --rm --network nc-mcp-spike-od-net <exapp-image> python -c ...`.

    Schreibe Abschnitt 2.3 mit vier Teilen: die drei Messzeilen samt Adressen und Rückgabewerten, die
    Gegenprobe, die Einordnung und der Entwurfsbefund. Die Einordnung ist der eigentliche Wert und
    muss wörtlich vorkommen: die Grenze sitzt nicht auf dem Weg zu einem fremden Host, sondern auf dem
    Weg zum Client-Id-Metadatendokument eines fremden OAuth-Clients; der in dieser Phase gemessene
    Zugriffsweg auf OpenProject berührt sie überhaupt nicht, weil es keine zweite Basis-URL gibt. Der
    Entwurfsbefund für OD-04: wer `target_allowed` für eine zweite Basis-URL wiederverwendet, sperrt
    jede Cluster-Installation aus; wer sie nicht wiederverwendet, braucht eine eigene, ausdrücklich
    begründete Prüfung für eine URL, die aus den Admin-Einstellungen und niemals aus einer Anfrage
    kommt. Nenne dazu, dass `resolve_addresses` den ganzen Namen verwirft, sobald eine seiner Adressen
    abgelehnt wird, und dass der Negativkatalog aus `tests/unit/test_oauth_cimd.py` verwendet wurde und
    nicht ein neu erfundener.

    Ändere `src/` nicht, lege keine neue Test-Datei an, füge keinen Import hinzu.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "Client-Id-Metadatendokument" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "172.29.43" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "Gegenprobe" &amp;&amp; git status --short src/ | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 2.3 nennt die aufgelösten Adressen der Nachbardienste aus `172.29.43.0/24`, den
      Rückgabewert `False` von `target_allowed` und den Rückgabewert `None` von `resolve_addresses`.
    - Die Gegenprobe mit einem öffentlichen Namen steht mit ihrer Adressliste im Abschnitt.
    - Der Satz zur Einordnung enthält die Zeichenkette `Client-Id-Metadatendokument` und die Aussage,
      dass der gemessene Zugriffsweg diese Grenze nicht berührt.
    - Der Entwurfsbefund für OD-04 steht als eigener Absatz da, ohne eine Codeänderung zu verlangen.
    - `git status --short src/` ist leer; `uv run pytest -q` bleibt grün.
    - Ungemessen-Fallback: ist der ExApp-Container nicht ansprechbar, steht Abschnitt 2.3 als
      `ungemessen, ExApp-Container nicht erreichbar` mit dem `docker ps`-Auszug, und die Einordnung
      (welcher Weg von der Grenze überhaupt berührt wird) bleibt als Quellcodebefund mit Datei und
      Zeile stehen, ausdrücklich als hergeleitet und nicht gemessen gekennzeichnet.
  </acceptance_criteria>
  <done>Die SSRF-Frage aus D-06 ist mit drei Messzeilen und einer Gegenprobe beantwortet und in ihre Reichweite eingeordnet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host zu Messumgebung | Veröffentlichte Ports der Wegwerf-Instanzen; die Zugangsdaten sind Wegwerfwerte und der Bruteforce-Schutz ist im Bootstrap aus |
| ExApp-Container zu Nextcloud | Der Container verarbeitet unvertraute Eingabe und darf keine Client-Adresse fälschen |
| Messumgebung zu Repository | Geheimnisse der Umgebung dürfen weder in eine Compose-Datei noch in den Bericht wandern |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | .env.spike-opendesk, docs/spike-opendesk.md | hoch | mitigate | Geheimnisse nur in die git-ignorierte `.env.spike-opendesk`; `.env.spike-opendesk.example` trägt ausschließlich Variablennamen; Protokoll nur mit Statuscodes, Fassungen und Präfixen; Gate im verify-Schritt greift nach `eyJ`, `Bearer `, `refresh_token=`, `client_secret=` |
| T-17-02 | Spoofing, Tampering | Caddy, Registry, Nextcloud | mittel | mitigate | Jede `ports`-Zeile bindet `127.0.0.1` (WR-06); die Registry ist unauthentifiziert und deshalb ausschließlich auf Loopback; `HP_SHARED_KEY` und `SECRET_KEY_BASE` ohne Vorgabewert (WR-11) |
| T-17-03 | Spoofing | TRUSTED_PROXIES, HP_TRUSTED_PROXY_IPS | mittel | mitigate | Genau eine Adresse (`172.29.43.10`), nie das Subnetz (WR-08); `ip_range` hält die dynamische Zuteilung aus der statischen Hälfte (IN-03) |
| T-17-04 | Tampering | src/, appinfo/, pyproject.toml, uv.lock | mittel | mitigate | Die SSRF-Messung liest `cimd.py` und ruft es im Container auf, ohne eine Datei zu ändern; `git status --short src/` ist Teil der Verifikation |
| T-17-05 | Repudiation | S0 | niedrig | mitigate | Jeder Messwert nennt den Nutzernamen; die Gegenprobe mit 64 Nullen steht neben dem Hauptlauf |
| T-17-SC | Tampering | Container-Bildmarken | mittel | mitigate | Alle drei neuen Bildmarken sind gepinnt und in 17-RESEARCH.md §Package Legitimacy Audit geprüft; keine Paketinstallation aus npm, PyPI oder crates; `pyproject.toml` und `uv.lock` bleiben unverändert. HaRP bleibt der gleitende Tag der bestehenden Lage, die gelaufene Fassung steht mit Digest im Berichtskopf |
</threat_model>

<verification>
- `docker compose -f compose.spike-opendesk.yml ps` zeigt vier Dienste `running`, keinen `openproject`
  und keinen `keycloak`.
- `occ status` meldet 33.0.7, der Berichtskopf nennt denselben Wert.
- `uv run pytest -q` grün, `git status --short src/ appinfo/ pyproject.toml uv.lock` leer.
- Kein Treffer für `eyJ`, `Bearer `, `refresh_token=`, `client_secret=` in den geänderten Dateien.
</verification>

<success_criteria>
Stufe A steht in ihrem ersten Teil, gepinnt und auf Loopback; S0 und die SSRF-Frage stehen als
Messwerte mit Gegenprobe im Bericht, oder als Ungemessen mit Grund. Der Produktionsbaum ist
unverändert.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-02-SUMMARY.md` when done
</output>
