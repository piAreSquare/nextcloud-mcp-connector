---
phase: 17-opendesk-spike
plan: 03
type: execute
wave: 3
# depends_on nennt 17-02 allein wegen der dort angelegten Topologie-Dateien, nicht wegen der
# ExApp-Registrierung: dieser Plan erreicht OpenProject über Caddy und das Docker-Netz und läuft
# deshalb auch dann weiter, wenn der ExApp- oder HaRP-Heartbeat in 17-02 gescheitert ist. Auf
# "ungemessen" geht dieser Plan nur, wenn "caddy" nicht läuft oder "nc-mcp-spike-od-net" fehlt.
depends_on: ["17-02"]
files_modified:
  - docs/spike-opendesk.md
autonomous: false
requirements: [OD-02]

must_haves:
  truths:
    - "OpenProject 17.7.2 läuft in der Spike-Topologie und ist unter op.localtest.me:8082 aus Browser und Nextcloud-Container unter demselben Namen erreichbar"
    - "Zwei OpenProject-Nutzerkonten und ein privates Projekt mit genau einem Arbeitspaket unter eindeutigem Suchwort existieren, Mitglied ist nur Konto B"
    - "Die OAuth-Anwendung für Weg 1 ist ein öffentlicher Client, das Feld Client Credentials User ist leer, und der Bericht sagt das"
    - "Der Bericht kennzeichnet den Admin-Zugang als Aufbau und nicht als Teil der gemessenen Kette"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "Aufbaunotiz in Abschnitt 5 samt Fassungen von OpenProject im Kopfblock"
      contains: "Aufbau, nicht Messung"
  key_links:
    - from: "Nextcloud-Container"
      to: "op.localtest.me:8082"
      via: "extra_hosts auf die feste Caddy-Adresse"
      pattern: "op\\.localtest\\.me"
---

<objective>
Stufe A wird vollständig: OpenProject 17.7.2 kommt dazu, der Grundzustand für beide Zugriffswege
entsteht (zwei Konten, ein privates Projekt, ein Arbeitspaket mit eindeutigem Suchwort), und die
Schritte, die OpenProject ausschließlich über seine Oberfläche anbietet, erledigt der Owner in einem
einzigen Browserbesuch.

Purpose: Der Zwei-Konten-Negativbeweis ist auf beiden Wegen Pflicht (D-05) und braucht Daten, die die
Seed-Daten nicht hergeben (Pitfall 3). Ein leerer Negativbeweis sieht grün aus und beweist nichts.
Output: laufendes OpenProject, gefüllte Kopfzeile im Bericht, Aufbaunotiz in Abschnitt 5, alle
Zugangswerte in der git-ignorierten `.env.spike-opendesk`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@compose.spike-opendesk.yml
@docs/spike-dav.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenProject 17.7.2 hochfahren und die Fassung belegen</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Umgebungsvariablen von OpenProject beim
      ersten Start" (Zeilen 1155 bis 1168), §"Speicher- und Platzerwartung" (Zeilen 1170 bis 1184),
      §"Die Namensfalle" (Zeilen 1121 bis 1153), Pitfall 1 Zeile "OpenProject 17.7.2 erster Start".
    - `compose.spike-opendesk.yml`, Dienst `openproject` und der Caddy-Block `:8082`.
  </read_first>
  <action>
    Erzeuge `SECRET_KEY_BASE` einmalig mit `openssl rand -hex 64` und schreibe den Wert in
    `.env.spike-opendesk`, nicht in die Compose-Datei. Der Wert muss über Neustarts gleich bleiben,
    sonst sind Sitzungen und verschlüsselte Datenbankinhalte unlesbar.

    Fahre `docker compose -f compose.spike-opendesk.yml --profile op up -d` und verfolge
    `docker compose -f compose.spike-opendesk.yml logs -f openproject` bis die Seed-Phase durch ist.
    Der erste Start dauert erwartbar 30 bis 90 Minuten, davon meist Warten. Zwei Fehlerbilder sind
    vorgesehen: der OOM-Killer beim Seeden (dann WSL2-Speicher erhöhen und genau einmal wiederholen)
    und ein fehlendes `OPENPROJECT_HTTPS=false` (dann leitet alles auf HTTPS um).

    Belege die Fassung dreifach und schreibe sie in den Kopfblock des Berichts:
    `docker image inspect openproject/openproject:17.7.2 --format '{{index .RepoDigests 0}}'` für den
    Digest, `curl -sS http://op.localtest.me:8082/api/v3` für `coreVersion` (antwortet der Aufruf
    unauthentifiziert mit 401, hole den Wert nach der Anmeldung in Task 2 nach), und die Fußzeile der
    Anmeldeseite.

    Prüfe die Namensfalle in beide Richtungen, weil sie sonst einen halben Tag kostet:
    aus dem Host `curl -sS -o /dev/null -w '%{http_code}' http://op.localtest.me:8082/login`
    (erwartet 200) und aus dem Nextcloud-Container
    `docker compose -f compose.spike-opendesk.yml exec -T nextcloud curl -sS -o /dev/null -w '%{http_code}' http://op.localtest.me:8082/login`
    (erwartet ebenfalls 200). Antwortet der zweite Aufruf nicht, fehlt der `extra_hosts`-Eintrag oder
    er zeigt auf die falsche Adresse; ohne diese beiden gleichlautenden Antworten ist Weg 0 nicht
    einrichtbar.

    Löse in diesem Task keinen Schreibaufruf gegen eine OCS-Route von `integration_openproject` aus und
    setze keine Umgebungsvariable, die einen globalen Basic-Auth-Nutzer oder einen API-Schlüssel für
    alle einführt (Pitfall 2).
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w '%{http_code}\n' http://op.localtest.me:8082/login &amp;&amp; docker compose -f compose.spike-opendesk.yml exec -T nextcloud curl -sS -o /dev/null -w '%{http_code}\n' http://op.localtest.me:8082/login &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "17.7.2"</automated>
  </verify>
  <acceptance_criteria>
    - Beide `curl`-Aufrufe auf `http://op.localtest.me:8082/login`, einer vom Host und einer aus dem
      Nextcloud-Container, antworten mit 200.
    - Der Kopfblock des Berichts nennt OpenProject `17.7.2` mit Digest-Präfix und der aus der Instanz
      gelesenen `coreVersion` oder, solange die Anmeldung fehlt, mit dem Vermerk, dass `coreVersion`
      in Task 2 nachgetragen wird.
    - `docker compose -f compose.spike-opendesk.yml ps openproject` zeigt `running`.
    - Ungemessen-Fallback: bricht der Start zweimal (OOM oder Seed-Abbruch), entfällt der ganze
      OpenProject-Teil. Dann trägt Abschnitt 2 wörtlich `OD-02: ungemessen, Aufbau nicht erreichbar`
      samt dem letzten Log-Auszug und der WSL2-Speicherzahl, die Pläne 17-04 bis 17-07 werden auf
      ungemessen gesetzt, und Abschnitt 2.5 nennt den Grund. Weg 0 und Weg 1 stehen dann als
      ungemessen und niemals als verworfen (D-03).
  </acceptance_criteria>
  <done>OpenProject läuft, seine Fassung ist belegt, und derselbe Name führt aus Browser und aus dem Nextcloud-Container an dieselbe Instanz.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Die vier Schritte, die OpenProject nur über die Oberfläche anbietet</name>
  <files>.env.spike-opendesk (git-ignoriert, nicht im Repository)</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"OAuth-Anwendung in OpenProject 17.7.x
      anlegen" (Zeilen 867 bis 891), insbesondere die drei Planungsanweisungen und Annahme A7.
    - Pitfall 2 (Zeilen 1346 bis 1368): warum das Feld "Client Credentials User" leer bleibt.
  </read_first>
  <action>
    Halte an, lege dem Owner die vier Oberflächenschritte aus `how-to-verify` wörtlich vor und warte auf
    das Freigabesignal. Prüfe danach automatisiert, dass `OP_ADMIN_PASSWORD`, `OP_API_TOKEN`,
    `OP_OAUTH_CLIENT_ID` und `OP_OAUTH_CLIENT_SECRET` in `.env.spike-opendesk` stehen und dass
    `GET /api/v3/users/me` mit `Basic apikey:<OP_API_TOKEN>` mit 200 antwortet. Schreibe keinen dieser
    Werte in eine Datei im Repository und in kein Protokoll. Versuche nicht, die OAuth-Anwendung über
    einen `rails runner` gegen `Doorkeeper::Application` zu seeden: das ist Annahme A7 und ausdrücklich
    untersagt, weil es eine Behauptung über fremden Code ohne Beleg wäre.
  </action>
  <what-built>
    OpenProject 17.7.2 läuft unter `http://op.localtest.me:8082`, die Vorgabe-Anmeldung ist
    `admin`/`admin`, und `.env.spike-opendesk` liegt bereit und ist git-ignoriert. Alles, was per API
    oder `occ` möglich ist, ist automatisiert. Für die vier folgenden Schritte gibt es in
    OpenProject 17.7.2 keinen dokumentierten Weg außer der Oberfläche: ein `rails runner` gegen
    `Doorkeeper::Application` wäre eine Behauptung über fremden Code ohne Beleg und ist nach
    Annahme A7 ausdrücklich nicht zu benutzen.
  </what-built>
  <how-to-verify>
    1. Öffne `http://op.localtest.me:8082/login` und melde dich mit `admin`/`admin` an. OpenProject
       verlangt beim ersten Mal ein neues Passwort. Trage das gewählte Passwort als
       `OP_ADMIN_PASSWORD=` in `.env.spike-opendesk` ein.
    2. Öffne `http://op.localtest.me:8082/my/access_token` und erzeuge einen API-Schlüssel. Der Wert
       ist genau einmal sichtbar. Trage ihn als `OP_API_TOKEN=` ein.
    3. Öffne Administration, Authentication, OAuth applications und lege eine Anwendung an:
       Name `nc-mcp-spike-weg1`, Redirect URI `http://127.0.0.1:8099/callback`, Scopes `api_v3`.
       Das Häkchen "Confidential" bleibt **aus**, denn nur ein nicht vertraulicher Client fällt unter
       `force_pkce`, und genau dessen Verhalten wird gemessen. Das Feld "Client Credentials User"
       bleibt **leer**.
    4. Kopiere Client-Id und Client-Secret sofort. OpenProject legt das Secret gehasht ab
       (`hash_application_secrets`), es ist danach nicht mehr lesbar. Trage die Werte als
       `OP_OAUTH_CLIENT_ID=` und `OP_OAUTH_CLIENT_SECRET=` ein.

    Trage nichts davon in eine Datei im Repository ein. `.env.spike-opendesk` ist die einzige
    zulässige Ablage; sie steht in `.gitignore`.
  </how-to-verify>
  <verify>
    <automated>set -a &amp;&amp; . ./.env.spike-opendesk &amp;&amp; set +a; for v in OP_ADMIN_PASSWORD OP_API_TOKEN OP_OAUTH_CLIENT_ID OP_OAUTH_CLIENT_SECRET; do eval "test -n \"\$$v\"" || { echo "FEHLT: $v"; exit 1; }; done; curl -sS -u "apikey:$OP_API_TOKEN" -o /dev/null -w '%{http_code}\n' http://op.localtest.me:8082/api/v3/users/me</automated>
  </verify>
  <acceptance_criteria>
    - Die vier Variablen `OP_ADMIN_PASSWORD`, `OP_API_TOKEN`, `OP_OAUTH_CLIENT_ID` und
      `OP_OAUTH_CLIENT_SECRET` sind in `.env.spike-opendesk` gesetzt und nicht leer.
    - `GET /api/v3/users/me` mit `Basic apikey:<OP_API_TOKEN>` antwortet 200.
    - `git status --short` zeigt `.env.spike-opendesk` nicht an, und `git grep -n OP_OAUTH_CLIENT_SECRET`
      findet den Namen nur in `.env.spike-opendesk.example` und in Plan- oder Berichtstext, niemals mit
      einem Wert dahinter.
    - Ungemessen-Fallback: bleibt die Anmeldung oder die Anwendung aus (Owner nicht verfügbar, UI
      nicht bedienbar), wird Weg 1 in Abschnitt 2.2 als `ungemessen, OAuth-Anwendung nicht angelegt`
      mit Datum geführt, und Plan 17-04 wird übersprungen. Weg 0 bleibt davon unberührt und
      messbar.
  </acceptance_criteria>
  <resume-signal>Tippe "freigegeben", wenn die vier Werte in `.env.spike-opendesk` stehen, oder beschreibe, was nicht möglich war.</resume-signal>
  <done>Die vier ausschließlich über die Oberfläche erreichbaren Werte liegen in der git-ignorierten Verbindungsdatei.</done>
</task>

<task type="auto">
  <name>Task 3: Grundzustand für den Zwei-Konten-Negativbeweis anlegen</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Nutzerkonten in OpenProject anlegen, für
      beide Wege" (Zeilen 919 bis 930) und §"S0 bis S6" Zeile S3 (Zeile 800).
    - `docs/spike-dav.md` §"The negative case": die Form, in der ein Negativbeweis hier steht.
    - Pitfall 3 (Zeilen 1370 bis 1389), Absatz "Die Datenlage".
  </read_first>
  <action>
    Lege den Grundzustand über die API v3 mit `Basic apikey:<OP_API_TOKEN>` an. Alles in diesem Task
    ist Aufbau und keine Messung, und der Bericht muss das sagen.

    1. Zwei Nutzer über `POST /api/v3/users`, Status `active`, mit Passwörtern von mindestens zwölf
       Zeichen: Login `opa` (Vorname Alice) und `opb` (Vorname Bob). Passwörter nach
       `.env.spike-opendesk` als `OP_USER_A_PASSWORD` und `OP_USER_B_PASSWORD`. Erwartete Antwort 201.
    2. Ein privates Projekt über `POST /api/v3/projects`, Name `Spike Privat B`, Identifier
       `spike-privat-b`, `public` auf `false`. Erwartete Antwort 201.
    3. Die Rollen-Id von `Member` über `GET /api/v3/roles` lesen und über
       `POST /api/v3/memberships` **nur** `opb` in dieses Projekt aufnehmen. `opa` bleibt draußen, und
       das ist der ganze Punkt: ohne diese Asymmetrie ist der Negativbeweis leer und sieht trotzdem
       grün aus.
    4. Ein Arbeitspaket über `POST /api/v3/work_packages` im privaten Projekt, Betreff mit einem
       eindeutigen Suchwort, das nirgends sonst vorkommt: `SPIKE-OD-8471 privat`. Notiere die
       zurückgegebene Id als `OP_WP_ID` in `.env.spike-opendesk`; sie ist eine vom Server vergebene Id
       und darf im Bericht stehen.
    5. Gegenprobe des Aufbaus, ohne die Schritt 3 nichts beweist: `GET /api/v3/work_packages/<OP_WP_ID>`
       mit `Basic apikey:<OP_API_TOKEN>` antwortet 200 (der Admin sieht es). Diese Zeile ist Aufbau,
       nicht der Negativbeweis; der Negativbeweis läuft in Plan 17-04 und 17-06 unter den Konten
       selbst.

    Trage in Abschnitt 5 des Berichts eine Aufbaunotiz mit der wörtlichen Überschrift "Aufbau, nicht
    Messung": welche Konten, welches Projekt, welches Arbeitspaket, welche Id, und der Satz, dass der
    Admin-Zugang, mit dem das entstanden ist, nicht Teil der gemessenen Kette ist (dieselbe
    Formulierung führt `docs/spike-dav.md` über die Deck-Boards). Nenne dort auch ausdrücklich, dass
    das Feld "Client Credentials User" der OAuth-Anwendung leer geblieben ist, weil ein Prüfer genau
    danach fragt.

    Löse keine der schreibenden OCS-Routen von `integration_openproject` aus
    (`POST /api/v1/work-packages`, `POST /api/v1/create/work-packages`,
    `DELETE /api/v1/file-links/{id}`, `DELETE /api/v1/work-packages/{id}/notifications`). Der Aufbau
    läuft ausschließlich über die API v3 von OpenProject.
  </action>
  <verify>
    <automated>set -a &amp;&amp; . ./.env.spike-opendesk &amp;&amp; set +a; curl -sS -u "apikey:$OP_API_TOKEN" http://op.localtest.me:8082/api/v3/work_packages/$OP_WP_ID | grep -q "SPIKE-OD-8471" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "Aufbau, nicht Messung" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "Client Credentials User"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/v3/work_packages/<OP_WP_ID>` als Admin antwortet 200 und der Betreff enthält
      `SPIKE-OD-8471`.
    - `GET /api/v3/projects/spike-privat-b` meldet `public: false`; die Mitgliederliste des Projekts
      enthält `opb` und nicht `opa`.
    - `OP_USER_A_PASSWORD`, `OP_USER_B_PASSWORD` und `OP_WP_ID` stehen in `.env.spike-opendesk`.
    - Abschnitt 5 trägt die Überschrift `Aufbau, nicht Messung`, nennt die Arbeitspaket-Id und den
      Satz, dass das Feld "Client Credentials User" leer geblieben ist.
    - Ungemessen-Fallback: lehnt `POST /api/v3/users` ab (403, fehlende Berechtigung des Tokens),
      wird der Grundzustand einmal über die Oberfläche angelegt und die Abweichung in der Aufbaunotiz
      genannt. Ist auch das nicht möglich, stehen S3 und der Zwei-Konten-Negativbeweis beider Wege in
      Abschnitt 2.5 als `ungemessen, kein zweites Konto` mit dem HTTP-Status als Grund.
  </acceptance_criteria>
  <done>Zwei Konten, ein privates Projekt und ein Arbeitspaket unter eindeutigem Suchwort existieren, und der Bericht kennzeichnet sie als Aufbau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser des Owners zu OpenProject | Die einzigen Schritte, für die es keinen API-Weg gibt, laufen über eine angemeldete Sitzung |
| OpenProject zu Repository | Client-Id, Client-Secret, API-Schlüssel und drei Passwörter entstehen und dürfen das Repository nie erreichen |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | .env.spike-opendesk | hoch | mitigate | Alle sieben neuen Werte ausschließlich in die git-ignorierte Verbindungsdatei; `git status --short` und `git grep` als Prüfung im acceptance-Schritt; der Bericht nennt nur die vom Server vergebene Arbeitspaket-Id |
| T-17-03 | Elevation of Privilege | OAuth-Anwendung, OpenProject-Umgebung | mittel | mitigate | Häkchen "Confidential" aus, Feld "Client Credentials User" leer, kein `OPENPROJECT_AUTHENTICATION_GLOBAL__BASIC__AUTH_USER`, kein Dienstkonto; der Ausschluss steht als Satz im Bericht (Pitfall 2) |
| T-17-02 | Spoofing, Tampering | OpenProject auf Loopback | mittel | mitigate | Kein veröffentlichter Port am Dienst selbst, Zugang nur über Caddy auf `127.0.0.1:8082`; das Vorgabe-Passwort `admin` wird im ersten Schritt ersetzt |
| T-17-05 | Repudiation | Aufbau gegen Messung | niedrig | mitigate | Abschnitt 5 trennt Aufbau und Messung namentlich; der Admin-Zugang wird ausdrücklich aus der gemessenen Kette herausgenommen |
| T-17-04 | Tampering | Produktionsbaum | mittel | mitigate | Dieser Plan schreibt nur in `docs/spike-opendesk.md` und in eine git-ignorierte Datei |
| T-17-SC | Tampering | openproject/openproject:17.7.2 | niedrig | mitigate | Gepinnte Bildmarke samt Digest im Berichtskopf, geprüft in 17-RESEARCH.md §Package Legitimacy Audit; keine Paketinstallation im Umfang |
</threat_model>

<verification>
- Beide `curl`-Aufrufe auf `op.localtest.me:8082` (Host und Nextcloud-Container) antworten 200.
- `GET /api/v3/users/me` mit dem API-Schlüssel antwortet 200.
- Das private Projekt enthält `opb` und nicht `opa`, das Arbeitspaket trägt `SPIKE-OD-8471`.
- `git status --short` zeigt keine Datei mit einem Geheimnis; `git status --short src/` ist leer.
</verification>

<success_criteria>
Stufe A ist vollständig: beide Systeme laufen gepinnt, der Grundzustand für den
Zwei-Konten-Negativbeweis existiert, die vier Oberflächenschritte sind erledigt, und der Bericht
trennt Aufbau von Messung.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-03-SUMMARY.md` when done
</output>
