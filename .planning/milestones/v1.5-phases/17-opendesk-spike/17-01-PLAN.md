---
phase: 17-opendesk-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spike-opendesk.md
autonomous: true
requirements: [OD-01]

must_haves:
  truths:
    - "Ein Leser von docs/spike-opendesk.md erfährt die Installierbarkeit, bevor er eine API-Frage liest"
    - "Jede der drei Hürden trägt Repository, Tag, Datei, Zeile und ein wörtliches Zitat oder ist ausdrücklich als offene ISV-Call-Frage markiert"
    - "Das Entscheidungskriterium steht im Text, bevor der erste Messwert entsteht"
    - "Das Wort verworfen kommt als Urteil im Bericht nicht vor, ungemessen ist definiert"
    - "D-01: Jede der drei Hürden von OD-01 ist aus Quellen belegt, ohne Kubernetes-Cluster, oder ausdrücklich als offene ISV-Call-Frage markiert"
    - "D-09: Der Bericht liegt als docs/spike-opendesk.md nach dem Muster von docs/spike-dav.md vor, in der Reihenfolge Installierbarkeit, Auth, API-Form, mit den Rohmesswerten als eigenem Abschnitt"
    - "D-10: Das Berichtsgerüst legt den Abschnitt für die ISV-Fragenliste an, damit die Liste versioniert im Repository steht"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "Berichtskopf, vorab festgelegte Entscheidungskriterien, Abschnitt 1 (OD-01) vollständig, leere Gliederung 2 bis 5"
      contains: "## 1. Installierbarkeit (OD-01)"
      min_lines: 120
  key_links:
    - from: "docs/spike-opendesk.md Abschnitt 1.4"
      to: "docs/spike-opendesk.md Abschnitt 4"
      via: "namentlicher Verweis je offener Punkt"
      pattern: "Abschnitt 4"
---

<objective>
OD-01 wird ausschließlich aus Quellen beantwortet, ohne Docker und ohne Kubernetes-Cluster (D-01,
D-03), und der Spike-Bericht `docs/spike-opendesk.md` entsteht mit Kopf, vorab festgelegten
Entscheidungskriterien und vollständigem Abschnitt 1.

Purpose: Die Installierbarkeit steht über jeder API-Frage (Erfolgskriterium 1 der ROADMAP und D-09).
Die Arbeit ist billig, fast abschließend aus Quellen möglich und gibt die Reihenfolge des ganzen
Berichts vor. Zugleich muss das Entscheidungskriterium im Text stehen, bevor in Plan 17-02 der erste
Messwert entsteht, sonst könnten die Zahlen es nachträglich verschieben.
Output: `docs/spike-opendesk.md` mit Abschnitt 1 fertig und den Abschnitten 2 bis 5 als benannte,
noch leere Gliederung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@docs/spike-mail.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bericht anlegen mit Kopf, Entscheidungskriterien und leerer Gliederung</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `docs/spike-mail.md`, Zeilen 1 bis 33: Kopfblock und der Abschnitt "Entscheidungskriterium, vorab festgelegt". Das ist das Formvorbild, deutscher Fließtext.
    - `docs/spike-dav.md`, Zeilen 1 bis 20: Kopfblock und Scope-Formulierung.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Vorschlag für den Aufbau von `docs/spike-opendesk.md`" (Zeilen 1469 bis 1515) und §"Nachweisform: was in diesem Projekt als Beleg gilt" (Zeilen 1267 bis 1300).
    - `.planning/phases/17-opendesk-spike/17-CONTEXT.md` D-03, D-09, D-10.
  </read_first>
  <action>
    Lege `docs/spike-opendesk.md` neu an, deutscher Fließtext wie `docs/spike-mail.md`, echte Umlaute,
    keine Gedankenstriche, keine Emojis.

    Titel: `# openDesk-Spike (OD-01, OD-02, OD-03)`.

    Kopfblock mit genau diesen Feldern, je Feld eine Zeile: Status, Entscheidungsdatum, Nextcloud,
    AppAPI, `integration_openproject`, `user_oidc`, OpenProject, Keycloak, Deploy-Daemon, Scope. Jedes
    Feld, dessen Wert erst aus einer laufenden Instanz kommt, trägt wörtlich
    `noch nicht gemessen (Plan 17-02)`. Status trägt `in Arbeit`. Der Scope-Satz nennt, was gemessen
    wird (Installierbarkeit aus Quellen, zwei Zugriffswege lokal in Docker) und was ausdrücklich nicht
    gemessen wird (kein Kubernetes-Cluster, keine openDesk-Installation, kein Produktionscode).
    Darunter der Satz aus `spike-mail.md`, dass die Fassungen vor dem Schreiben aus der laufenden
    Instanz gelesen werden (`occ status`, `occ app:list`) und nicht aus der Recherche übernommen.

    Danach `## Entscheidungskriterien, vorab festgelegt` mit drei Bestandteilen:
    erstens die Antwortform-Tabelle für Weg 0 (OCS-Umschlag als JSON mit beliebigem `statuscode`
    gleich erreicht, HTML-Körper gleich nicht erreicht, 3xx mit `Location` auf `/login` gleich nicht
    erreicht) samt dem Satz, dass kein Schritt dieses Berichts auf Statuscode 200 prüft, weil eine 401
    aus `validatePreRequestConditions()` antwortender App-Code ist;
    zweitens die Definition von `gemessen` (ein Aufruf, ein Messwert, mindestens eine Gegenprobe und
    der Nutzername, unter dem der Aufruf lief);
    drittens die Definition von `ungemessen` (die Messung war nicht möglich, der Grund steht dabei) und
    der ausdrückliche Satz, dass `verworfen` in diesem Bericht kein zulässiges Urteil ist (D-03,
    ROADMAP-Erfolgskriterium 3).

    Danach die Gliederung nach D-09 mit den Überschriften `## 1. Installierbarkeit (OD-01)`,
    `## 2. Nutzeridentität gegen OpenProject (OD-02)`, `## 3. API-Form (Vorarbeit für OD-04, kein
    Requirement dieser Phase)`, `## 4. Fragenliste für den ISV-Call am 14.09. (OD-03)`,
    `## 5. Rohmesswerte`, `## Was diese Messung nicht beweist`, dazu die Unterüberschriften 1.1 bis 1.4
    und 2.1 bis 2.5 aus der Vorlage der Recherche. Unter jede noch nicht gefüllte Überschrift genau eine
    Zeile der Form `noch nicht gemessen, Plan 17-NN`, mit der Zuordnung: 1.1 bis 1.3 Belege Plan 17-01,
    1.3 Messteil S0 Plan 17-02, 2.1 Plan 17-05 und 17-06, 2.2 Plan 17-04, 2.3 Plan 17-02, 2.4 und 2.5
    Plan 17-09, 3 Plan 17-09, 4 Plan 17-08, 5 wird von allen Plänen gefüllt.

    Ganz oben in Abschnitt 5 ein Absatz mit der Geheimnisregel (Pitfall 7): protokolliert werden
    Statuscodes, Feldnamen, Zahlen, Längen und Präfixe; niemals ein `access_token`, ein
    `refresh_token`, ein Autorisierungscode, ein `client_secret` oder ein Wert des Headers
    `AUTHORIZATION-APP-API`, weil dieser Base64 von `<user>:<APP_SECRET>` ist. `expires_in` ist eine
    Zahl und darf stehen.

    Schreibe in diesem Task keinen Messwert und keine Aussage über eine der drei Hürden.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' docs/spike-opendesk.md)" = "7" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -c 'noch nicht gemessen' &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -c 'AUTHORIZATION-APP-API'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/spike-opendesk.md` existiert und trägt genau sieben `## `-Überschriften in der Reihenfolge
      Entscheidungskriterien, 1. Installierbarkeit, 2. Nutzeridentität, 3. API-Form, 4. Fragenliste,
      5. Rohmesswerte, Was diese Messung nicht beweist.
    - Der Kopfblock nennt alle zehn Felder; die Felder Nextcloud, AppAPI, `integration_openproject`,
      `user_oidc`, OpenProject und Keycloak tragen wörtlich `noch nicht gemessen (Plan 17-02)`.
    - Der Abschnitt Entscheidungskriterien enthält die Zeichenkette `OCS-Umschlag`, die Zeichenkette
      `HTML-Körper` und den Satz, dass `verworfen` kein zulässiges Urteil ist.
    - Abschnitt 5 enthält die Zeichenkette `AUTHORIZATION-APP-API` in der Geheimnisregel.
    - Kein Gedankenstrich (U+2014, U+2013) in der Datei, keine ASCII-Ersatzschreibung von Umlauten.
  </acceptance_criteria>
  <done>Der Bericht existiert mit Kopf, vorab festgelegten Kriterien und benannter, leerer Gliederung; kein Abschnitt behauptet einen Messwert.</done>
</task>

<task type="auto">
  <name>Task 2: Die drei Hürden belegen, jede mit Tag, Datei, Zeile und Zitat</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §K1 (Zeilen 176 bis 231), §K6 (Zeilen 429 bis 460), §"OD-01: Installierbarkeit aus Quellen" (Zeilen 464 bis 601).
    - `appinfo/info.xml` dieses Repositories, die Zeile mit `<nextcloud min-version=`.
    - `docs/spike-dav.md` §"The two controls" als Muster dafür, wie ein Beleg im Bericht aussieht.
  </read_first>
  <action>
    Hole jeden Beleg vor dem Schreiben selbst und schreibe nur, was der Abruf bestätigt. Die Abrufe,
    alle ohne Anmeldung erreichbar:

    Hürde 1, abgeschalteter App Store: Rohpfad
    `https://gitlab.opencode.de/bmi/opendesk/deployment/opendesk/-/raw/v1.18.0/helmfile/apps/nextcloud/values-nextcloud-management.yaml.gotmpl`.
    Belege wörtlich `appstore: enabled: false` samt Zeilennummer und dazu die vier Nebenbefunde
    `spreed: enabled: false`, `contacts: enabled: false`, `comments: enabled: false`,
    `circles: enabled: false` sowie `adminAudit`. Zwei der neun Werkzeugfamilien liegen in openDesk
    dunkel: dieser Satz gehört hierher und ein Verweis darauf in Abschnitt 4.

    Hürde 2, Deploy-Daemon und Kubernetes: `https://raw.githubusercontent.com/nextcloud/app_api/stable33/lib/Command/Daemon/RegisterDaemon.php`
    und derselbe Pfad an `stable34`, dazu der HTTP-Status von
    `https://raw.githubusercontent.com/nextcloud/app_api/stable33/lib/DeployActions/KubernetesActions.php`
    (erwartet 404) gegen `stable34` (erwartet 200). Führe die korrigierte Fassung aus K1: nicht
    "AppAPI kann kein Kubernetes", sondern auf `app_api` 33 existiert `kubernetes-install` nicht und auf
    `app_api` 34 existiert es, mit `DEPLOY_ID = 'kubernetes-install'` und über HaRP. Nenne den
    Sichtbarkeitsvorbehalt aus K1 (keine Vorlage für `kubernetes-install` in
    `src/constants/daemonTemplates.js` von `stable34`, nur über `occ app_api:daemon:register --k8s`
    erreichbar). Belege außerdem mit K6, dass AppAPI im Deployment-Projekt an keiner Stelle vorkommt:
    lade `https://gitlab.opencode.de/bmi/opendesk/deployment/opendesk/-/archive/v1.18.0/opendesk-v1.18.0.tar.gz`,
    entpacke in ein Verzeichnis außerhalb des Repositories und fahre
    `grep -ril "app_api\|appapi\|external.app\|exapp"` (erwartet: keine Treffer) sowie
    `grep -ril "authorization_method\|integration_openproject\|integrationOpenproject"` (erwartet: drei
    Dateien). Schreibe die Trefferzahlen als Messwert des Griffs mit Datum in den Bericht. Der eine
    verbleibende Vorbehalt, das openDesk-Nextcloud-Image wird aus einem nicht mitgelesenen Projekt
    gebaut, steht ausdrücklich dabei und geht als Frage 1a in Abschnitt 1.4.

    Hürde 3, Versionspin: Rohpfad
    `https://gitlab.opencode.de/bmi/opendesk/deployment/opendesk/-/raw/v1.18.0/helmfile/environments/default/images.yaml.gotmpl`.
    Belege die drei Pins wörtlich samt Zeilennummer und Digest-Präfix: Nextcloud `33.0.7`,
    Keycloak `26.7.0`, OpenProject `17.7.2` als Spiegel von `openproject/open_desk`. Führe die
    dreiteilige Aussage aus der Recherche: erstens `appinfo/info.xml` dieser App erlaubt
    `min-version="32" max-version="34"`, der Pin macht die App also nicht unzulässig; zweitens alle
    Ein-Klick- und Erreichbarkeitsnachweise dieses Projekts stehen auf 34.0.x (`docs/spike-dav.md`
    34.0.2, `docs/spike-mail.md` 34.0.3, `compose.exapp.yml` pinnt `nextcloud:34.0.3-apache`), die
    Zielumgebung ist damit ungetestet und nicht unzulässig; drittens Versionspin und Kubernetes-Hürde
    sind dieselbe Hürde und die Lösung beider ist derselbe Schritt. Kündige an, dass der Messteil
    dieser Hürde als Behauptung S0 in Plan 17-02 folgt, und lasse die Zeile
    `noch nicht gemessen, Plan 17-02` für S0 stehen.

    Verwende die Formulierung "manual-install ist laut Nextcloud-Doku ausdrücklich für Entwicklung"
    nicht: sie ist auf der heutigen Seite nicht mehr auffindbar. Zulässig sind die aufgezählten Werte
    der `occ`-Hilfe, weil die im Quellcode stehen.
  </action>
  <verify>
    <automated>for s in "appstore" "kubernetes-install" "stable33" "stable34" "33.0.7" "17.7.2" "26.7.0" "min-version" "v1.18.0"; do grep -v '^#' docs/spike-opendesk.md | grep -q "$s" || { echo "FEHLT: $s"; exit 1; }; done; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 1.1 zitiert `appstore: enabled: false` mit Datei, Tag `v1.18.0` und Zeilennummer und
      nennt `spreed: enabled: false` und `contacts: enabled: false` als Nebenbefund.
    - Abschnitt 1.2 nennt beide Hilfetexte von `RegisterDaemon.php:37` (Zweig `stable33` ohne und
      `stable34` mit `kubernetes-install`), den HTTP-Status 404 gegen 200 für
      `KubernetesActions.php` und den Sichtbarkeitsvorbehalt zu `daemonTemplates.js`.
    - Abschnitt 1.2 nennt das Ergebnis des Archivgriffs mit Datum: null Treffer für die vier
      AppAPI-Muster, drei Dateien für `authorization_method`.
    - Abschnitt 1.3 zitiert die drei Bildmarken-Pins mit Zeilennummer und führt die dreiteilige
      Aussage ungetestet gegen unzulässig; die S0-Zeile `noch nicht gemessen, Plan 17-02` steht noch.
    - Die Datei enthält die Formulierung "ausdrücklich für Entwicklung" nicht.
    - Ungemessen-Fallback: kann ein Abruf nicht durchgeführt werden (Netz, 4xx, 5xx), steht an der
      Stelle wörtlich `ungemessen` mit dem HTTP-Status und dem Datum des Versuchs, und der Punkt wird
      in Abschnitt 1.4 als offene ISV-Call-Frage aufgenommen. Kein Beleg wird aus der Recherche
      übernommen, ohne dass der Abruf ihn bestätigt hat.
  </acceptance_criteria>
  <done>Abschnitt 1.1 bis 1.3 tragen je Hürde Repository, Tag, Datei, Zeile und ein wörtliches Zitat, oder eine mit Grund versehene Ungemessen-Zeile.</done>
</task>

<task type="auto">
  <name>Task 3: Abschnitt 1.4, was offen bleibt, namentlich und mit Verweis</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Was von OD-01 offen bleibt und in den Bericht als ISV-Frage gehört" (Zeilen 593 bis 601) und §"OD-03: was auf die Fragenliste gehört" (Zeilen 1519 bis 1573).
    - Der eigene Abschnitt 1.1 bis 1.3 aus Task 2.
  </read_first>
  <action>
    Schreibe Abschnitt 1.4 als Tabelle mit den Spalten Offener Punkt, Warum aus Quellen nicht
    entscheidbar, Verweis. Vier Zeilen, wörtlich aus der Recherche übernommen und je mit dem Verweis
    auf die passende Frage in Abschnitt 4: `app_api` im openDesk-Nextcloud-Image enthalten und
    eingeschaltet (Frage 6), Betriebsart von `integration_openproject` in openDesk `oauth2` gegen
    `oidc` (Frage 7, mit dem ausdrücklichen Hinweis, dass der Bootstrap-Job nur ein Indiz und kein
    Beleg ist), Aufnahme einer Dritt-ExApp neben der Suite (Frage 1), Termin von openDesk auf
    Nextcloud 34 oder höher (Frage 5).

    Schließe Abschnitt 1 mit einem Absatz, der die Antwort auf OD-01 in drei Sätzen zusammenfasst:
    belegt ist, dass die Ein-Klick-Erzählung in einer openDesk-Installation nicht existiert; belegt
    ist, dass auf dem gepinnten Stand kein Kubernetes-Deploy-Daemon existiert und eine Hauptversion
    darüber einer existiert; offen bleibt der Installationsweg, und das ist eine Terminfrage und keine
    Absage. Halte die Trennung sichtbar, die Pitfall 3 verlangt: aus openDesk-Quellen belegt gegen
    lokal gemessen. Abschnitt 1 enthält ausschließlich Belege aus Quellen, keinen lokalen Messwert.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -c 'Abschnitt 4' &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q 'Terminfrage' &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q 'Indiz' &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 1.4 hat vier Zeilen, jede mit einem Verweis auf eine numerierte Frage in Abschnitt 4.
    - Die Zeile zur Betriebsart trägt das Wort `Indiz` und behauptet keine Betriebsart.
    - Der Schlussabsatz von Abschnitt 1 nennt das Wort `Terminfrage` und trennt sprachlich zwischen
      "aus openDesk-Quellen belegt" und "lokal gemessen".
    - Ungemessen-Fallback: entfällt, dieser Task schreibt keine Messung. Ein offener Punkt, der
      weder belegt noch als Frage geführt werden kann, wird mit Grund in Abschnitt 1.4 aufgenommen
      statt weggelassen.
  </acceptance_criteria>
  <done>Abschnitt 1 ist vollständig und verweist für jeden offenen Punkt namentlich auf Abschnitt 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| externe Quelle zu Bericht | Von gitlab.opencode.de und raw.githubusercontent.com geladener Text wandert als Zitat in eine öffentliche Repository-Datei |
| Bericht zu Öffentlichkeit | `docs/spike-opendesk.md` liegt in einem öffentlichen Repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | docs/spike-opendesk.md | hoch | mitigate | Die Geheimnisregel steht als Absatz in Abschnitt 5, bevor der erste Messwert entsteht (Task 1). In diesem Plan entsteht kein Geheimnis, weil keine Instanz läuft |
| T-17-05 | Repudiation | Belege in Abschnitt 1 | mittel | mitigate | Kein Zitat ohne Repository, Tag, Datei und Zeile; jeder Abruf wird vor dem Schreiben selbst durchgeführt (Regel aus context_agent#230) |
| T-17-04 | Tampering | Produktionsbaum | mittel | mitigate | `files_modified` nennt ausschließlich `docs/spike-opendesk.md`; `src/`, `appinfo/`, `pyproject.toml` und `uv.lock` werden nicht angefasst. Nachweis am Phasenende in Plan 17-09 |
| T-17-SC | Tampering | Paketinstallationen | niedrig | accept | Diese Phase installiert kein Paket aus npm, PyPI oder crates; das Archiv aus Task 2 wird nur gelesen und liegt außerhalb des Repositories. Container-Bildmarken sind in 17-RESEARCH.md §Package Legitimacy Audit geprüft und gepinnt |
</threat_model>

<verification>
- `grep -c '^## ' docs/spike-opendesk.md` ergibt 7.
- Der Bericht nennt für jede der drei Hürden Tag, Datei und Zeile oder eine begründete
  Ungemessen-Zeile.
- `git status --short src/ appinfo/ pyproject.toml uv.lock` ist leer.
</verification>

<success_criteria>
OD-01 ist aus Quellen beantwortet, die drei Hürden stehen belegt im Bericht, die offenen Punkte sind
namentlich als ISV-Call-Fragen markiert, und die Entscheidungskriterien stehen im Text, bevor in
Plan 17-02 der erste Messwert entsteht.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-01-SUMMARY.md` when done
</output>
