---
phase: 17-opendesk-spike
plan: 09
type: execute
wave: 9
depends_on: ["17-08"]
files_modified:
  - docs/spike-opendesk.md
autonomous: true
requirements: [OD-01, OD-02, OD-03]

must_haves:
  truths:
    - "Welcher Weg trägt, steht als Folge der Messungen da und nicht als Argument"
    - "Jeder nicht gemessene Punkt steht als ungemessen mit dem Grund, kein Punkt steht als verworfen"
    - "Der Bericht ist reproduzierbar: die Befehlsfolge der Messumgebung steht darin"
    - "Kein Geheimnis steht im Bericht oder in einer der neuen Dateien"
    - "Der ausgelieferte Produktionsbaum ist nachweislich unverändert: 15712 Bytes über 21 Werkzeuge gegen ein Budget von 18000"
    - "D-02: Der Bericht nennt die gepinnten Fassungen der lokalen Docker-Messung, Nextcloud 33.0.7 und OpenProject 17.7.2, und kein latest"
    - "D-03: Kein Punkt steht als verworfen da, alles nicht Messbare als ungemessen mit Grund, und eine gemietete Box ist nicht entstanden"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "Abschnitt 2.4, 2.5, Rohmesswerte, Reproduktion, Ränder, Status und Entscheidungsdatum im Kopf"
      contains: "## Was diese Messung nicht beweist"
  key_links:
    - from: "docs/spike-opendesk.md Abschnitt 2.4"
      to: "docs/spike-opendesk.md Abschnitt 2.1 und 2.2"
      via: "Verweis auf die Messwerte, aus denen die Folgerung stammt"
      pattern: "Abschnitt 2\\.[12]"
---

<objective>
Der Bericht wird abgeschlossen: welcher Weg trägt (2.4), was ungemessen blieb und warum (2.5), die
Rohmesswerte, die Reproduktionsanleitung und die Ränder. Danach die zwei Nachweise, die die Phase
schuldet: kein Geheimnis in einer neuen Datei, und der ausgelieferte Produktionsbaum ist unverändert
(ROADMAP-Erfolgskriterium 5).

Purpose: OD-02 verlangt, dass die Entscheidung auf der Messung fällt und nicht auf einem Argument, und
Erfolgskriterium 3 verlangt, dass ein nicht gemessener Weg als `ungemessen` dasteht und nie als
`verworfen`. Erfolgskriterium 5 verlangt einen stillstehenden Produktionsbaum, und Pitfall 6 sagt, wie
er sich unbemerkt bewegt.
Output: fertiger Bericht, gestoppte und abgeräumte Messumgebung, zwei belegte Nachweise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@.planning/ROADMAP.md
@docs/spike-mail.md
@docs/spike-opendesk.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abschnitt 2.4, 2.5 und die Ränder schreiben</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - Die eigenen Abschnitte 1, 2.1, 2.2, 2.3 und 3 des Berichts vollständig, samt aller Zeilen, die
      `ungemessen` tragen.
    - `.planning/ROADMAP.md` §Phase 17, die fünf Erfolgskriterien (Zeilen 123 bis 129).
    - `docs/spike-mail.md` §"Decision" und §"Was diese Messung nicht beweist" als Formvorbild.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` Pitfall 3 (Zeilen 1370 bis 1389).
  </read_first>
  <action>
    Schreibe `### 2.4 Welcher Weg trägt, als Folge dieser Messungen`. Die Form ist bindend: jeder Satz
    dieses Abschnitts verweist auf eine Messzeile aus 2.1, 2.2 oder 2.3, und es steht kein Argument
    darin, das nicht auf einen Messwert zeigt. Führe die Folgerung in dieser Reihenfolge: was Weg 0
    gemessen leistet (S1 bis S4, S6), wo er gemessen kippt oder ungemessen bleibt (S5), was Weg 1
    gemessen leistet (PKCE, `expires_in`, Refresh ohne Sitzung, Negativbeweis) und was er kostet
    (Consent je Nutzer, zweiter OAuth-Client beim Betreiber), und was die SSRF-Messung für einen
    künftigen zweiten Zugriffsweg bedeutet. Schließe mit einem Satz, der ausdrücklich benennt, welche
    offene Frage die Folgerung noch tragen muss: die Betriebsart, die openDesk wirklich fährt
    (Frage 7), und die Stabilitätszusage der OCS-Fläche (offener Rückkanal aus D-11). Ein tragender
    Weg 0 und eine offene Stabilitätsfrage sind kein Widerspruch, beides muss dastehen.

    Schreibe `### 2.5 Was ungemessen blieb, und warum die Messung nicht möglich war` als Tabelle mit
    den Spalten Punkt, Zustand, Grund, was es bräuchte. Übernimm jede Zeile, die irgendwo im Bericht
    `ungemessen` trägt, in diese Tabelle, damit der Leser sie an einer Stelle findet. Prüfe die ganze
    Datei darauf, dass das Wort `verworfen` nicht als Urteil über einen Weg vorkommt.

    Schreibe `## Was diese Messung nicht beweist` mit mindestens diesen Rändern: die Messung lief gegen
    eine Wegwerf-Instanz mit SQLite und gegen die genannten Fassungen, eine andere Fassung darf sich
    anders verhalten; der lokale Aufbau reproduziert openDesk nicht, und zwar in fünf benannten Punkten
    (Keycloak als Anmeldezwang ohne lokales Formular, die Scope-Pflicht für ein OIDC-JWT seit
    OpenProject 16.0.0, die Datenlage der Seed-Daten, Kubernetes samt Helm und abgeschaltetem App
    Store, die tatsächliche Betriebsart von `integration_openproject`); die Egress-Kontrollmessung
    beweist über eine Behördeninstallation nichts; die Absenkung des Loglevels auf `debug` ist eine
    Eigenschaft der Messumgebung und keine Empfehlung; die Zahlen aus `community.openproject.org` sind
    Kontext und kein Messwert dieser Instanz.

    Setze zuletzt den Kopfblock auf den Endstand: `Status` auf `done` samt dem Ergebnis in einem
    Halbsatz, `Entscheidungsdatum` auf das Datum des letzten Messlaufs, und ersetze jede noch
    vorhandene Zeile `noch nicht gemessen, Plan 17-NN` entweder durch den Messwert oder durch eine
    Ungemessen-Zeile mit Grund. Es darf keine Zeile mit `noch nicht gemessen` in der Datei bleiben.
  </action>
  <verify>
    <automated>! grep -q "noch nicht gemessen" docs/spike-opendesk.md &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "### 2.4" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "### 2.5" &amp;&amp; grep -q "^## Was diese Messung nicht beweist" docs/spike-opendesk.md &amp;&amp; ! grep -v '^#' docs/spike-opendesk.md | grep -iE "Weg [01] (ist|wird) verworfen"</automated>
  </verify>
  <acceptance_criteria>
    - Keine Zeile der Datei enthält mehr `noch nicht gemessen`.
    - Abschnitt 2.4 verweist mindestens dreimal auf eine Messzeile in 2.1, 2.2 oder 2.3 und nennt die
      offenen Fragen, die die Folgerung noch tragen muss.
    - Abschnitt 2.5 listet jede Ungemessen-Zeile des Berichts mit Grund und mit der Angabe, was die
      Messung möglich gemacht hätte.
    - Kein Weg wird als `verworfen` bezeichnet.
    - Der Abschnitt "Was diese Messung nicht beweist" nennt die fünf Punkte, in denen der lokale Aufbau
      openDesk nicht reproduziert.
    - Der Kopfblock trägt `Status: done` mit Ergebnis-Halbsatz und ein Entscheidungsdatum.
    - Ungemessen-Fallback: blieb ein ganzer Weg ungemessen, trägt 2.4 den Satz, dass die Entscheidung
      auf der Grundlage dieser Phase nicht fällt, samt der Angabe, welche Messung dafür fehlt. Eine
      Entscheidung wird dann nicht behauptet.
  </acceptance_criteria>
  <done>Der Bericht folgert nur aus Messwerten, sammelt alle Ungemessen-Punkte an einer Stelle und benennt seine Ränder.</done>
</task>

<task type="auto">
  <name>Task 2: Rohmesswerte, Reproduktion und der Geheimnisgriff</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `docs/spike-mail.md` §"Reproduktion" (Zeilen 390 bis 429) als Formvorbild.
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` Pitfall 7 (Zeilen 1431 bis 1443) und
      §"Zustand, der entsteht, und wie er wieder weggeht" (Zeilen 1186 bis 1199).
    - Der eigene Abschnitt 5 des Berichts mit den bisher eingetragenen Rohwerten.
  </read_first>
  <action>
    Vervollständige `## 5. Rohmesswerte`: je Messung der Aufruf ohne Kopfzeilenwerte, der Statuscode,
    der Content-Type, die Antwortform und maximal 120 Zeichen Körper. Ordne die Rohwerte in der
    Reihenfolge der Behauptungen S0 bis S6, danach Weg 1, danach SSRF. Die Aufbaunotiz aus Plan 17-03
    bleibt oben stehen.

    Ergänze einen Abschnitt `## Reproduktion` nach dem Muster von `docs/spike-mail.md`: die
    Befehlsfolge, mit der die Messumgebung entsteht (`export HP_SHARED_KEY`,
    `docker compose -f compose.spike-opendesk.yml up -d --wait`,
    `bash scripts/bootstrap_spike_opendesk.sh`, `--profile op`, `--profile oidc`,
    `set -a && . ./.env.spike-opendesk && set +a`), die Schritte, die nur über eine Oberfläche gehen
    (mit Verweis auf die Pläne 17-03 und 17-05), und wie der Zustand wieder verschwindet
    (`docker compose -f compose.spike-opendesk.yml down -v`, für Keycloak zwingend, weil ein
    Realm-Import eine vorhandene Realm überspringt). Nenne, dass die Verbindungsdatei git-ignoriert ist
    und dass `.env.spike-opendesk.example` nur die Variablennamen führt.

    Fahre danach den Geheimnisgriff über alle in dieser Phase entstandenen oder geänderten Dateien und
    schreibe das Ergebnis in die Plan-SUMMARY:
    `git grep -nEi "eyJ|Bearer [A-Za-z0-9]|refresh_token=[A-Za-z0-9_-]{8}|client_secret=[A-Za-z0-9_-]{8}|code_verifier=|AUTHORIZATION-APP-API: [A-Za-z0-9+/]" -- docs compose.spike-opendesk.yml deploy scripts .gitignore`
    (erwartet: keine Treffer außer in erklärenden Sätzen ohne Wert) und zusätzlich
    `git grep -nEi "client_credentials|GLOBAL__BASIC__AUTH|apikey:" -- docs compose.spike-opendesk.yml deploy scripts`
    (erwartet: Treffer nur in Absätzen, die erklären, warum diese Wege ausgeschlossen sind, und in der
    als Aufbau gekennzeichneten Zeile zum API-Schlüssel). Findet der Griff einen echten Wert, wird er
    entfernt, der Bericht behält an der Stelle nur Länge und Präfix, und das Vorkommnis steht in der
    SUMMARY.

    Prüfe zuletzt die Vollständigkeit gegen die Nachweisform: hat jede Behauptung im Bericht eine
    Gegenprobe oder ausdrücklich den Vermerk, dass sie nicht gegengeprobt ist, nennt jeder Messwert den
    Nutzernamen, unter dem er lief, und trägt der Kopf die aus der laufenden Instanz gelesenen
    Fassungen. Was fehlt, wird nachgetragen oder als Ungemessen-Zeile in 2.5 aufgenommen.
  </action>
  <verify>
    <automated>grep -q "^## Reproduktion" docs/spike-opendesk.md &amp;&amp; grep -q "down -v" docs/spike-opendesk.md &amp;&amp; ! git grep -nEi "eyJ|refresh_token=[A-Za-z0-9_-]{8}|client_secret=[A-Za-z0-9_-]{8}|code_verifier=[A-Za-z0-9_-]{8}" -- docs compose.spike-opendesk.yml deploy scripts</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 5 trägt je Messung Aufruf, Status, Content-Type, Antwortform und höchstens 120 Zeichen
      Körper, in der Reihenfolge S0 bis S6, dann Weg 1, dann SSRF.
    - Der Abschnitt `## Reproduktion` nennt die vollständige Befehlsfolge samt Profilen und `down -v`.
    - Der Geheimnisgriff läuft ohne Treffer mit einem echten Wert; das Ergebnis beider Griffe steht in
      der Plan-SUMMARY.
    - Jeder Messwert im Bericht nennt einen Nutzernamen; Behauptungen ohne Gegenprobe tragen den
      Vermerk `nicht gegengeprobt`.
    - Ungemessen-Fallback: fehlt ein Rohwert, weil die Messung nicht lief, steht in Abschnitt 5 die
      Zeile `ungemessen` mit Verweis auf 2.5 statt einer erfundenen Zahl.
  </acceptance_criteria>
  <done>Die Rohmesswerte sind vollständig, die Messumgebung ist reproduzierbar beschrieben, und kein Geheimnis steht in einer der neuen Dateien.</done>
</task>

<task type="auto">
  <name>Task 3: Produktionsbaum-Nachweis und Messumgebung abräumen</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` Pitfall 6 (Zeilen 1416 bis 1429) samt der
      erwarteten Zahlen und §"Zustand, der entsteht" (Zeilen 1186 bis 1199).
    - `.planning/ROADMAP.md` §Phase 17, Erfolgskriterium 5.
    - `scripts/check_tool_budget.py`, nur lesen.
  </read_first>
  <action>
    Führe die vier Nachweise und schreibe ihre Ergebnisse als eigenen Absatz am Ende des Berichts
    (Überschrift "Der Produktionsbaum nach dieser Phase") und in die Plan-SUMMARY:
    1. `git status --short src/ appinfo/ pyproject.toml uv.lock` ist leer, und
       `git diff --stat <Stand vor der Phase>..HEAD -- src appinfo pyproject.toml uv.lock` ist leer.
       Den Stand vor der Phase liefert der letzte Commit vor `docs(17): research openDesk spike domain`.
    2. `uv run python scripts/check_tool_budget.py` meldet dieselben Zahlen wie am 2026-08-28:
       15712 Bytes über 21 Werkzeuge gegen ein Budget von 18000. Weicht eine Zahl ab, ist das ein
       Fehlschlag von Erfolgskriterium 5 und wird als solcher gemeldet, nicht kommentiert.
    3. `uv run pytest -q` ist grün. Die Spike-Messungen laufen außerhalb der Suite, es ist keine
       Test-Datei entstanden.
    4. `uv run ruff check .` und `uv run ruff format --check .` sind grün; `.planning/` ist von ruff
       ausgenommen, das bleibt so.

    Räume danach die Messumgebung ab: `docker compose -f compose.spike-opendesk.yml --profile op
    --profile oidc down -v`. Das `-v` ist Pflicht, weil ein zweiter Keycloak-Lauf sonst die
    Konfiguration des ersten misst. Prüfe mit `docker volume ls`, dass kein Band mit dem Präfix
    `nc-mcp-spike-od` übrig ist, und mit `docker ps`, dass kein Container der Topologie läuft. Wurde in
    Plan 17-02 die Topologie aus `compose.exapp.yml` angehalten, starte sie mit
    `docker compose -f compose.exapp.yml up -d --wait` wieder, damit die nächste Phase sie vorfindet.

    Schreibe zum Schluss in die Plan-SUMMARY eine Zeile je Anforderung: OD-01, OD-02 und OD-03 mit dem
    Ort des Nachweises im Bericht (Abschnitt und Überschrift) und dem Zustand `erfüllt` oder
    `teilweise, mit ungemessenen Punkten` samt Verweis auf 2.5. Eine Anforderung wird nicht als erfüllt
    gemeldet, wenn ihr Nachweis im Bericht fehlt.
  </action>
  <verify>
    <automated>git status --short src/ appinfo/ pyproject.toml uv.lock | wc -l | grep -q '^0$' &amp;&amp; uv run python scripts/check_tool_budget.py | grep -E "15712|21|18000" &amp;&amp; uv run pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; ! docker volume ls --format '{{.Name}}' | grep -q 'nc-mcp-spike-od'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short src/ appinfo/ pyproject.toml uv.lock` ist leer, und der Diff gegen den Stand
      vor der Phase ist für diese vier Pfade leer.
    - `scripts/check_tool_budget.py` meldet 15712 Bytes, 21 Werkzeuge, Budget 18000; keine Grenze wurde
      angehoben.
    - `uv run pytest -q` grün, `ruff check .` und `ruff format --check .` grün.
    - Kein Docker-Band und kein Container mit dem Präfix `nc-mcp-spike-od` existiert mehr.
    - Der Bericht trägt den Absatz "Der Produktionsbaum nach dieser Phase" mit allen vier Ergebnissen.
    - Die Plan-SUMMARY nennt je Anforderung OD-01, OD-02, OD-03 den Ort des Nachweises und den Zustand.
    - Ungemessen-Fallback: weicht eine der Zahlen aus Nachweis 2 ab oder ist der Diff nicht leer, wird
      das als Verletzung von Erfolgskriterium 5 gemeldet, die betroffene Änderung benannt und
      zurückgenommen, bevor der Plan abgeschlossen wird.
  </acceptance_criteria>
  <done>Der Produktionsbaum ist belegt unverändert, die Messumgebung ist restlos abgeräumt, und je Anforderung steht der Ort ihres Nachweises fest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Messumgebung zu Host | Bänder und Container mit Wegwerf-Zugangsdaten bleiben sonst liegen und hängen an der nächsten Phase |
| neue Dateien zu Öffentlichkeit | Bericht, Compose-Datei, Skript und Entwürfe werden mit dem Phasen-Commit öffentlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | alle in Phase 17 geänderten Dateien | hoch | mitigate | Zwei `git grep`-Griffe vor dem Abschluss: einer nach Token- und Secret-Mustern, einer nach `client_credentials`, `GLOBAL__BASIC__AUTH` und `apikey:`; ein echter Treffer wird entfernt und im SUMMARY vermerkt (Pitfall 7) |
| T-17-02 | Spoofing, Tampering | liegengebliebene Messumgebung | mittel | mitigate | `down -v` über beide Profile, Prüfung mit `docker volume ls` und `docker ps`; die Instanz mit Wegwerf-Passwort und abgeschaltetem Bruteforce-Schutz existiert danach nicht mehr |
| T-17-04 | Tampering | src/, appinfo/, pyproject.toml, uv.lock, Werkzeugbudget | mittel | mitigate | Vier Nachweise: leerer Status, leerer Diff gegen den Stand vor der Phase, unveränderte Budget-Zahlen 15712 von 18000 über 21 Werkzeuge, grüner Vorgabelauf der Testsuite |
| T-17-05 | Repudiation | Abschluss ohne Nachweis | mittel | mitigate | Je Anforderung eine SUMMARY-Zeile mit dem Ort des Nachweises; ohne Nachweis wird die Anforderung nicht als erfüllt gemeldet |
| T-17-06 | Information Disclosure | Loglevel debug | niedrig | mitigate | Der Zustand verschwindet mit dem Nextcloud-Band beim `down -v`; der Bericht sagt, dass die Absenkung keine Empfehlung ist |
| T-17-SC | Tampering | Paketstand | niedrig | mitigate | `pyproject.toml` und `uv.lock` sind Teil des Diff-Nachweises; diese Phase hat kein Paket gezogen |
</threat_model>

<verification>
- Keine Zeile `noch nicht gemessen` im Bericht, kein Weg als `verworfen` bezeichnet.
- `## Reproduktion` und "Der Produktionsbaum nach dieser Phase" stehen im Bericht.
- Beide `git grep`-Griffe ohne echten Treffer.
- `check_tool_budget.py` meldet 15712 Bytes, 21 Werkzeuge, Budget 18000.
- `uv run pytest -q`, `ruff check .`, `ruff format --check .` grün.
- Kein Band und kein Container mit Präfix `nc-mcp-spike-od`.
</verification>

<success_criteria>
Der Spike-Bericht ist fertig und folgert nur aus Messwerten, alle ungemessenen Punkte stehen mit Grund
an einer Stelle, kein Geheimnis ist im Repository gelandet, die Messumgebung ist abgeräumt, und der
ausgelieferte Produktionsbaum ist nachweislich unverändert.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-09-SUMMARY.md` when done
</output>
