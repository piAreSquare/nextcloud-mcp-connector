---
phase: 17-opendesk-spike
plan: 06
type: execute
wave: 6
depends_on: ["17-05"]
files_modified:
  - docs/spike-opendesk.md
autonomous: true
requirements: [OD-02]

must_haves:
  truths:
    - "Auf Weg 0 findet Konto alice das Arbeitspaket aus dem privaten Projekt von bob nicht, und bob findet es"
    - "Nach künstlichem Tokenablauf antwortet derselbe OCS-Aufruf im Modus oauth2 wieder mit Daten, ohne Browsersitzung, und token_expires_at steht danach in der Zukunft"
    - "Die Erneuerung ist als solche belegt, weil derselbe Aufruf mit unbrauchbarem refresh_token 401 liefert"
    - "Die Byte-Kosten einer Weg-0-Antwort stehen mit einer Bezugsgröße im Bericht, nicht als nackte Zahl"
    - "D-05: Der Zwei-Konten-Negativbeweis ist auch auf Weg 0 gefahren, damit er auf beiden Wegen vorliegt"
    - "D-12: In diesem Plan entsteht kein Code, files_modified nennt keinen Pfad unter src/, und Werkzeugoberfläche wie Budget-Gate bleiben unverändert"
  artifacts:
    - path: "docs/spike-opendesk.md"
      provides: "Abschnitt 2.1 mit S3, S4 und S6 samt Gegenproben, Abschnitt 3 mit der API-Form-Vorarbeit"
      contains: "S4"
  key_links:
    - from: "occ user:setting <user> integration_openproject token_expires_at 0"
      to: "GET /ocs/v2.php/apps/integration_openproject/api/v1/work-packages"
      via: "serverseitige Erneuerung in getAccessToken()"
      pattern: "token_expires_at"
---

<objective>
Die drei Weg-0-Messungen, die den Ausgang von OD-02 entscheiden, soweit sie ohne OIDC möglich sind:
S3 (Zwei-Konten-Negativbeweis auf Weg 0, D-05), S4 (Erneuerung ohne Browsersitzung im Modus `oauth2`)
und S6 (Byte-Kosten und Feldsatz einer Antwort).

Purpose: S4 ist die Behauptung, an der Weg 0 hängt. Der Quellcode sagt in einem Kommentar der
Upstream-Entwickler selbst, dass im `oauth2`-Modus serverseitig erneuert wird und im `oidc`-Modus
nicht; S4 belegt die erste Hälfte dieser Asymmetrie gemessen, S5 in Plan 17-07 die zweite. S6 ist
Vorarbeit für OD-04 und kostet in diesem Lauf fast nichts.
Output: Abschnitt 2.1 mit S3, S4, S6 und ihren Gegenproben, Abschnitt 3 mit der API-Form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-opendesk-spike/17-CONTEXT.md
@.planning/phases/17-opendesk-spike/17-RESEARCH.md
@docs/spike-dav.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: S3, der Zwei-Konten-Negativbeweis auf Weg 0</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"S0 bis S6" Zeile S3 (Zeile 800).
    - `docs/spike-dav.md` §"The negative case" und §"The confused deputy check".
    - `.planning/phases/17-opendesk-spike/17-CONTEXT.md` D-05: der Negativbeweis ist auf beiden Wegen
      Pflicht, nicht nur auf dem gewinnenden.
  </read_first>
  <action>
    Zwei Aufrufe unter reiner AppAPI-Impersonation gegen `http://127.0.0.1:8091`, mit den Headern aus
    Plan 17-05 und ohne App-Passwort im Prozess:
    `GET /ocs/v2.php/apps/integration_openproject/api/v1/work-packages?searchQuery=SPIKE-OD-8471`
    einmal als `alice` (verbunden mit dem OpenProject-Konto `opa`, kein Mitglied des privaten
    Projekts) und einmal als `bob` (verbunden mit `opb`, Mitglied).

    Erwartung: `alice` findet nichts, `bob` findet genau ein Arbeitspaket. Die Zeile von `bob` ist die
    Gegenprobe, ohne die die leere Antwort von `alice` auch ein Tippfehler im Suchwort wäre. Beide
    Zeilen tragen den Nutzernamen, den gemessenen Status, die Antwortform und die Trefferzahl.

    Führe die Kette, die dieser Weg wirklich hat, ausdrücklich im Text: auf Weg 0 entscheidet erst
    Nextcloud, wer der Nutzer ist (AppAPI-Impersonation), und danach OpenProject, was er sehen darf.
    Beide Glieder gehören in den Satz, sonst liest der Beweis wie eine Aussage über OpenProject allein.

    Schreibe die zwei Zeilen in die Behauptungstabelle von Abschnitt 2.1 und ergänze die Rohwerte
    (Aufruf, Status, Trefferzahl, Betreff des Treffers) in Abschnitt 5.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "searchQuery=SPIKE-OD-8471" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "S3" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "alice" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "bob"</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 2.1 trägt für S3 zwei Zeilen mit Nutzername, Status, Antwortform und Trefferzahl:
      `alice` null Treffer, `bob` ein Treffer.
    - Der Text nennt beide Glieder der Kette (Nextcloud entscheidet die Identität, OpenProject die
      Sichtbarkeit).
    - Abschnitt 5 trägt die Rohwerte beider Aufrufe, gekürzt nach der Geheimnisregel.
    - Ungemessen-Fallback: findet auch `bob` nichts, ist der Beweis leer und gilt nicht. Dann steht S3
      als `ungemessen, Suchweg liefert auch für das berechtigte Konto keinen Treffer` samt der
      Prüfung, ob das Arbeitspaket über die API v3 als `opb` sichtbar ist. Erst wenn diese Prüfung
      trägt, ist die leere Antwort von `alice` überhaupt ein Befund.
  </acceptance_criteria>
  <done>S3 steht mit Treffer und Nicht-Treffer, beide unter benanntem Konto, im Bericht.</done>
</task>

<task type="auto">
  <name>Task 2: S4, Erneuerung nach künstlichem Ablauf, mit der Gegenprobe die sie belegt</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"Die per-Nutzer-Zustandsschlüssel, die S4
      braucht" (Zeilen 719 bis 751) und §"Der entscheidende Quellcodebefund für S4, wörtlich"
      (Zeilen 753 bis 788).
    - Pitfall 4 (Zeilen 1391 bis 1402).
  </read_first>
  <action>
    Lies zuerst den Ausgangszustand von `bob`:
    `occ user:setting bob integration_openproject token_expires_at` und
    `occ user:setting bob integration_openproject token` (nur Länge und Präfix notieren, nie den
    Wert).

    Hauptlauf: setze den Ablauf künstlich in die Vergangenheit mit
    `occ user:setting bob integration_openproject token_expires_at 0`. Der Wert 0 ist sicher, weil
    `isAccessTokenExpired()` 60 Sekunden Sicherheitsabstand abzieht und 0 auch der Vorgabewert der
    Methode ist. Fahre danach denselben OCS-Aufruf wie in S3 als `bob`. Erwartung: Daten, und
    anschließend ein **neuer** `token_expires_at` in der Zukunft. Lies beide Zustandswerte nach dem
    Aufruf erneut und schreibe die drei Zahlen (vorher, gesetzt, nachher) in die Messzeile. Halte
    ausdrücklich fest, dass in diesem Aufruf keine Browsersitzung im Spiel war: der Aufruf lief unter
    reiner AppAPI-Impersonation, ohne Cookie und ohne App-Passwort.

    Gegenprobe, ohne die der Hauptlauf nichts beweist: setze
    `occ user:setting bob integration_openproject refresh_token <Unsinnswert>`, danach
    `occ user:setting bob integration_openproject token_expires_at 0`, und fahre denselben Aufruf
    erneut. Erwartung: OCS-`statuscode` 401, weil `requestOAuthAccessToken()` fehlschlägt und
    `getAccessToken()` eine leere Zeichenkette zurückgibt. Erst damit ist belegt, dass im Hauptlauf
    wirklich eine Erneuerung lief und nicht ein zwischengespeichertes Token weitergeholfen hat.

    Halte im Bericht fest, dass die Weg-0-Verbindung von `bob` nach dieser Gegenprobe absichtlich
    kaputt ist und dass `alice` unberührt bleibt, weil Plan 17-07 ein verbundenes Konto braucht.
    Nenne dazu den Quellcodebeleg aus dem Kommentar der Upstream-Entwickler
    (`OpenProjectAPIService.php:1764-1765`, "For OAuth2 setup, only try to refresh the expired token.
    Token exchange needs to be initiated from the UI.") und dass genau diese Asymmetrie der Unterschied
    zwischen S4 und S5 ist.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "token_expires_at" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "refresh_token" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "1764" &amp;&amp; docker compose -f compose.spike-opendesk.yml exec -T --user www-data nextcloud php occ user:setting alice integration_openproject token_expires_at</automated>
  </verify>
  <acceptance_criteria>
    - Die S4-Zeile trägt drei Werte von `token_expires_at` (vorher, künstlich 0, nachher) und den
      Status des Aufrufs dazwischen; der Wert nachher liegt in der Zukunft.
    - Der Satz, dass kein Cookie und kein App-Passwort im Aufruf waren, steht im Abschnitt.
    - Die Gegenprobe mit unbrauchbarem `refresh_token` steht mit OCS-`statuscode` 401 da.
    - Der Bericht nennt die Codestelle `OpenProjectAPIService.php:1764-1765` mit dem Zitat.
    - `occ user:setting alice integration_openproject token_expires_at` liefert weiter einen Wert:
      die Verbindung von `alice` ist unberührt.
    - Ungemessen-Fallback: antwortet der Hauptlauf mit 401, ist das der Befund und Weg 0 trägt im
      Modus `oauth2` nicht; dann steht in der Zeile der gemessene Status samt der Log-Zeile aus
      `docker compose logs nextcloud` und dem gelesenen Zustand danach. Ist der Zustandsschlüssel
      nicht setzbar, steht S4 als `ungemessen` mit dem Fehlertext von `occ`.
  </acceptance_criteria>
  <done>S4 steht mit drei Zustandswerten, einem Statuscode und der Gegenprobe im Bericht.</done>
</task>

<task type="auto">
  <name>Task 3: S6 und Abschnitt 3, die API-Form als Vorarbeit für OD-04</name>
  <files>docs/spike-opendesk.md</files>
  <read_first>
    - `.planning/phases/17-opendesk-spike/17-RESEARCH.md` §"S0 bis S6" Zeile S6 (Zeile 805) und
      §"Die OCS-Fläche" (Zeilen 606 bis 640), dazu Open Question 5 (Zeilen 1655 bis 1660).
    - `.planning/research/FEATURES.md`, Abschnitt zum Unterscheidungsmerkmal Arbeitspaket zu Datei,
      soweit vorhanden.
  </read_first>
  <action>
    Miss die Byte-Kosten an derselben Antwort wie in S3, als `bob` beziehungsweise, wenn dessen
    Verbindung aus Task 2 kaputt ist, als `alice` gegen ein Arbeitspaket, das `opa` sehen darf. Nenne
    das Konto in der Messzeile.
    1. Antwort minifiziert (ohne Leerraum) in Bytes zählen.
    2. Die Feldnamen der Antwort vollständig auflisten, so wie `docs/spike-mail.md` es für Mail tut.
    3. Bezugsgröße gegenrechnen, sonst ist die Zahl allein wertlos: dieselbe Instanz über
       `GET /api/v3/work_packages/$OP_WP_ID` roh (Zugang `Basic apikey:$OP_API_TOKEN`, ausdrücklich als
       Aufbauzugang gekennzeichnet) und, wenn möglich, mit `select`. Nenne die Vergleichswerte aus der
       Recherche (`community.openproject.org`: roh 3691 Bytes, mit `select` 216 Bytes) als Kontext und
       nicht als eigenen Messwert.

    Schreibe Abschnitt 3 des Berichts: Byte-Kosten, Feldsatz, die drei Lücken der OCS-Fläche (kein
    einzelnes Arbeitspaket per Id, keine Kommentare, kein "meine Arbeit") und der Befund, der sie
    teilweise ausgleicht (`GET /api/v1/work-packages/{id}/file-links` existiert und ist genau die Kette
    Arbeitspaket zu Datei). Treffe in diesem Abschnitt keine Entscheidung über den Werkzeugschnitt:
    die Entscheidung ist OD-04 und diese Phase erzeugt keinen Code (D-12). Halte am Ende von
    Abschnitt 3 einen Satz fest, dass die Diät hier der Server macht (`select`) und nicht eine
    Projektion in unserem Code.
  </action>
  <verify>
    <automated>grep -v '^#' docs/spike-opendesk.md | grep -q "Bytes" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "file-links" &amp;&amp; grep -v '^#' docs/spike-opendesk.md | grep -q "3691" &amp;&amp; grep -c '^## 3\.' docs/spike-opendesk.md</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 3 nennt die gemessene Bytezahl der minifizierten Antwort mit Einheit und dem Konto,
      unter dem sie entstand.
    - Die Feldnamen der Antwort stehen vollständig da.
    - Die rohe API-v3-Antwort derselben Instanz steht als Bezugsgröße daneben, der Aufbauzugang ist
      als solcher gekennzeichnet, und die Werte aus der Recherche stehen als Kontext.
    - Die drei Lücken und der `file-links`-Befund stehen im Abschnitt; keine Entscheidung über den
      Werkzeugschnitt wird getroffen.
    - Ungemessen-Fallback: liefert kein Konto eine Antwort mit einem Arbeitspaket, steht S6 als
      `ungemessen, keine Antwort mit Nutzdaten` mit Grund, und Abschnitt 3 führt ausschließlich die
      aus `appinfo/routes.php` an `v3.1.1` belegte Routenlage, ausdrücklich ohne Bytezahl.
  </acceptance_criteria>
  <done>S6 steht mit Bytezahl, Feldsatz und Bezugsgröße im Bericht, und Abschnitt 3 trägt die API-Form ohne Entscheidung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occ zu Nutzerzustand | Der künstliche Ablauf und die Gegenprobe verändern Zustandsschlüssel eines Nutzers in der Messumgebung |
| Antwortkörper zu Bericht | Die gemessene Antwort trägt Feldwerte aus OpenProject |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-17-01 | Information Disclosure | docs/spike-opendesk.md | hoch | mitigate | `token` und `refresh_token` erscheinen ausschließlich als Länge und Präfix; die Antwortkörper werden auf Feldnamen und Zahlen reduziert, Freitext nur soweit er aus dem selbst angelegten Aufbau stammt (`SPIKE-OD-8471`) |
| T-17-05 | Repudiation | S4 | mittel | mitigate | Ohne die Gegenprobe mit unbrauchbarem `refresh_token` gilt der Hauptlauf nicht als Erneuerungsnachweis; die drei Zustandswerte stehen als Zahlen im Bericht |
| T-17-03 | Elevation of Privilege | Zwei-Konten-Beweis | mittel | mitigate | Beide Läufe unter reiner AppAPI-Impersonation mit benanntem Nutzer; kein Dienstkonto, kein App-Passwort im Prozess |
| T-17-02 | Tampering | Zustand von bob | niedrig | accept | Die Weg-0-Verbindung von `bob` bleibt nach der Gegenprobe absichtlich kaputt; der Zustand verschwindet mit dem Nextcloud-Band beim `down -v`, und `alice` bleibt für Plan 17-07 unberührt |
| T-17-04 | Tampering | Produktionsbaum | mittel | mitigate | Nur `docs/spike-opendesk.md` wird geschrieben |
</threat_model>

<verification>
- Abschnitt 2.1 trägt S3, S4 und S6 mit je einer Gegenprobe.
- `occ user:setting alice integration_openproject token_expires_at` liefert weiter einen Wert.
- Kein Token- und kein Secret-Wert im Bericht.
- `git status --short src/ appinfo/ pyproject.toml uv.lock` leer.
</verification>

<success_criteria>
Die drei Weg-0-Messungen, die ohne OIDC möglich sind, stehen als Messwerte mit Gegenproben im Bericht,
und die API-Form ist als Vorarbeit für OD-04 festgehalten, ohne eine Entscheidung zu treffen.
</success_criteria>

<output>
Create `.planning/phases/17-opendesk-spike/17-06-SUMMARY.md` when done
</output>
