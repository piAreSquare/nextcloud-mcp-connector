---
phase: 18-audit-log-kern
plan: 10
type: execute
wave: 6
depends_on: ["18-08", "18-09"]
files_modified:
  - tests/unit/test_exapp_purge.py
  - tests/unit/test_audit_store.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "purge lässt das Audit-Log stehen; ein Test hält diese Behauptung"
    - "Entfernen über die Oberfläche, Verbindung trennen und Pausieren lassen das Audit-Log ebenfalls stehen; das ist keine getestete Behauptung, sondern eine hergeleitete Eigenschaft der Aufräumpfade, belegt durch einen grep, der zeigt, dass keiner von ihnen die Audit-Ablage kennt"
    - "Das Log ist nicht mit dem Datenschlüssel verschlüsselt und überlebt deshalb auch crypto.delete_key"
    - "Bei vollem Volume bleiben Token-Rotation und neue Verbindungen funktionsfähig"
    - "Die Werkzeugfläche hat sich in dieser Phase nicht bewegt: 15712 Bytes über 21 Werkzeuge, kein Grenzwert angehoben"
    - "Die Grenze aus D-18 steht beim Namen: occ app_api:app:unregister --rm-data entfernt das Volume und mit ihm das Log"
    - "Jedes der fünf Erfolgskriterien der Roadmap hat eine benannte Fundstelle oder eine gemessene Zeile"
  artifacts:
    - path: "tests/unit/test_exapp_purge.py"
      provides: "Fälle, die das Überleben der Audit-Datei behaupten"
      contains: "audit"
    - path: ".planning/phases/18-audit-log-kern/18-10-SUMMARY.md"
      provides: "Nachweistabelle je Erfolgskriterium, Budget-Stillstand, Gate-Lauf, A2-Messung"
  key_links:
    - from: "tests/unit/test_exapp_purge.py"
      to: "src/mcp_connector/audit/store.py"
      via: "eine Audit-Datei neben dem Speicher, die den Purge überlebt"
      pattern: "AUDIT_FILENAME"
---

<objective>
Der Abschluss der Phase: die Behauptungen, die bisher niemand hält, bekommen ihren Test, und
jedes der fünf Erfolgskriterien der Roadmap bekommt seine Fundstelle. Dazu der Nachweis, dass
sich das Werkzeugbudget nicht bewegt hat und alle Gates grün sind.

Purpose: D-v1.5-01 ist heute wahr, ohne dass jemand etwas geändert hätte, und genau deshalb
gefährlich: eine Behauptung, die niemand prüft, fällt beim nächsten Umbau lautlos. D-18 ist
die Gegenprobe dazu und wird nicht wegkonstruiert, sondern beim Namen genannt.
Output: zwei bis drei neue Testfälle, eine Messzeile zu den Schreibkosten und eine
Nachweistabelle in der SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-audit-log-kern/18-CONTEXT.md
@.planning/phases/18-audit-log-kern/18-RESEARCH.md
@.planning/phases/18-audit-log-kern/18-08-SUMMARY.md
@.planning/phases/18-audit-log-kern/18-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Das Audit-Log überlebt den Purge, und ein Test hält es</name>
  <files>tests/unit/test_exapp_purge.py</files>
  <behavior>
    - Nach `occ mcp_connector:purge --force` existiert die Datei `audit.sqlite3` noch, und ihre
      Zeilenzahl ist unverändert.
    - Die Ketten sind nach dem Purge unverletzt: `verify_chains()` meldet keinen Befund.
    - Der Purge leert weiterhin jede OAuth-Tabelle und löscht den Datenschlüssel; die
      bestehenden Fälle bleiben grün.
    - Das Audit-Log ist nach dem Löschen des Datenschlüssels weiterhin lesbar, weil es nicht
      damit verschlüsselt ist.
  </behavior>
  <read_first>
    - `tests/unit/test_exapp_purge.py` Zeilen 1 bis 27 (Threat-Katalog), 107 bis 154 (die
      `Deployment`-Hülle und `counts`), 164 bis 202 (Spionage-Fixture und `call`).
    - `src/mcp_connector/exapp/purge.py` Zeilen 144 bis 212: die drei Handlungen des Purge und
      dass keine davon eine zweite Datei im Verzeichnis berührt.
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §11 vollständig (Zeilen 1088 bis
      1148), besonders die Tabelle, welche Vorgänge das Log überlebt.
  </read_first>
  <action>
    Erweitere die `Deployment`-Hülle in `tests/unit/test_exapp_purge.py` um eine zweite Datei
    im selben Verzeichnis: eine `AuditStore` auf `tmp_path / audit.AUDIT_FILENAME`, in die vor
    dem Purge einige Zeilen geschrieben werden.

    Decke die vier Punkte aus `<behavior>` ab. Der Kern ist der Vergleich vorher und nachher:
    Zeilenzahl der Audit-Datei vor dem Purge, Purge laufen lassen, Zeilenzahl danach, und
    zusätzlich `verify_chains()` leer. Das Lesen geht an der Ablage vorbei mit einer eigenen
    `sqlite3`-Verbindung, wie `counts` es für die OAuth-Tabellen schon tut.

    Ergänze den Threat-Katalog im Modul-Docstring um eine Zeile zu T-18-22: eine Behauptung,
    die niemand prüft, fällt beim nächsten Umbau lautlos. Genau genommen ist das keine
    Änderung an der Aufräumlogik, sondern eine Behauptung, die bisher niemand hielt.

    Ändere `src/mcp_connector/exapp/purge.py` nicht. Sollte der Test rot laufen, ist das ein
    Befund und keine Einladung zu einer Anpassung des Handlers; er gehört dann in die SUMMARY
    und in die Phasenverifikation.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_purge.py -q &amp;&amp; git diff --stat src/mcp_connector/exapp/purge.py | wc -l | grep -qx "0" &amp;&amp; uv run --no-sync pytest tests/unit tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Vier neue oder erweiterte Fälle, grün.
    - `git diff --stat src/mcp_connector/exapp/purge.py` ist leer: das Überleben ist eine
      Eigenschaft des bestehenden Handlers und keine neue Sonderbehandlung.
    - Der Fall zum Datenschlüssel behauptet, dass die Audit-Datei nach `crypto.delete_key`
      weiterhin lesbar ist.
    - Alle bestehenden Fälle der Datei bleiben grün.
  </acceptance_criteria>
  <done>D-v1.5-01 ist nicht mehr nur wahr, sondern gehalten.</done>
</task>

<task type="auto">
  <name>Task 2: Schreibkosten messen und den OAuth-Speicher bei vollem Volume prüfen</name>
  <files>tests/unit/test_audit_store.py</files>
  <read_first>
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §8 Abschnitt "Was ein Eintrag im
      Schreibpfad kostet" (Zeilen 919 bis 936) samt der Einschränkung zu Windows und NTFS, und
      das Annahmenverzeichnis Zeile 1536 (A2).
    - `tests/unit/test_oauth_store.py`: die Fälle zur Nebenläufigkeit, als Vorbild für einen
      Fall, der zwei Speicher gleichzeitig benutzt.
    - `src/mcp_connector/audit/store.py`: `SIZE_LIMIT_BYTES`, `sweep`, `used_bytes`.
  </read_first>
  <action>
    Ergänze `tests/unit/test_audit_store.py` um einen Fall zu AUDIT-03, der die zweite Hälfte
    des Erfolgskriteriums 4 belegt: eine Audit-Ablage, die ihre Obergrenze erreicht hat, macht
    den OAuth-Speicher nicht schreibunfähig. Baue dazu im selben `tmp_path` beide Dateien, fülle
    die Audit-Ablage über ihre Obergrenze, lass den Aufräumlauf greifen, und behaupte danach
    zweierlei: der OAuth-Speicher nimmt weiterhin eine Schreiboperation an (eine Token-Rotation
    oder eine neue Verbindung, je nachdem was `test_oauth_store.py` am billigsten anbietet), und
    `used_bytes` der Audit-Ablage liegt unter der Grenze.

    Miss danach die Schreibkosten je Eintrag und schreibe die Zahl in die SUMMARY. Der Lauf ist
    ein Einzeiler über `uv run --no-sync python -c ...`: hundert Einträge über die
    öffentliche Schnittstelle schreiben, die Zeit teilen, das Ergebnis in Millisekunden nennen,
    zusammen mit der Plattform. Die Recherche misst 7,2 ms auf Windows und NTFS und nennt das
    ausdrücklich eine Obergrenze und keine Vorhersage für den Linux-Container (A2). Steht keine
    Container-Messung zur Verfügung, steht in der SUMMARY die lokale Zahl und daneben "im
    Container ungemessen"; das ist ein zulässiger Ausgang und kein Fehlschlag.

    Kein Test dieser Datei darf von der gemessenen Zeit abhängen: eine Zeitschwelle in einem
    Testfall wäre auf fremder Hardware eine Zufallszahl. Die Messung ist eine Beleg-Zeile, keine
    Zusicherung.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_audit_store.py -q &amp;&amp; ! grep -nE "perf_counter|time\.time\(\)" tests/unit/test_audit_store.py | grep -iE "assert" &amp;&amp; uv run --no-sync pytest tests/unit tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Ein Fall belegt, dass der OAuth-Speicher nach dem Erreichen der Audit-Obergrenze weiterhin
      schreibbar ist.
    - Kein Testfall behauptet eine Zeitschwelle.
    - Die SUMMARY trägt die gemessenen Millisekunden je Eintrag samt Plattform und, falls
      zutreffend, die Zeile "im Container ungemessen".
    - `uv run --no-sync pytest tests/unit tests/contract -q` grün.
  </acceptance_criteria>
  <done>Erfolgskriterium 4 ist in beiden Hälften belegt: Grenze und Frist greifen, und der Nachbar bleibt schreibfähig.</done>
</task>

<task type="auto">
  <name>Task 3: Gate-Lauf, Budget-Stillstand und die Nachweistabelle</name>
  <files>.planning/phases/18-audit-log-kern/18-10-SUMMARY.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 18: Audit-Log Kern": die fünf Erfolgskriterien wörtlich,
      besonders die präzisierte zweite Hälfte von Kriterium 5.
    - `.github/workflows/ci.yml`, Job `unit`: die sechs Gate-Kommandos in ihrer Reihenfolge.
    - Die SUMMARYs der Pläne 18-01 bis 18-09 dieser Phase.
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §10 (Zeilen 1054 bis 1084) und §11
      Abschnitt "Deinstallation" (Zeilen 1112 bis 1148).
  </read_first>
  <action>
    Fahre die sechs Gates in genau der Reihenfolge des CI-Jobs `unit` und schreibe je Gate eine
    Zeile mit dem Kommando und dem Ergebnis in die SUMMARY:
    `uv run --no-sync ruff check .`, `uv run --no-sync ruff format --check .`,
    `uv run --no-sync pyright`, `uv run --no-sync vulture src scripts vulture_whitelist.py`,
    `uv run --no-sync pytest tests/unit tests/contract`,
    `uv run --no-sync python scripts/check_tool_budget.py`, dazu `uv run --no-sync pytest -m matrix`.
    Der ausgeschlossene Lauf gehört ausdrücklich dazu, weil diese Phase Werkzeugcode angefasst
    hat: `-m matrix` läuft lokal, `-m integration` läuft im CI-Job `exapp` und wird mit dem
    Ergebnis aus Plan 18-09 nachgetragen.

    Halte den Budget-Stillstand als eigene Zeile fest: `tools/list: 15712 bytes, 21 tools, budget
    18000` vor dieser Phase und derselbe Wert danach. Kein Grenzwert wurde angehoben, kein
    Werkzeug kam dazu, `BUDGET_BYTES` und `MAX_TOOL_BYTES` sind unverändert. Belege das
    zusätzlich mit `git diff --stat` auf `scripts/check_tool_budget.py` und
    `tests/contract/test_tool_surface.py`: beide leer.

    Schreibe die Nachweistabelle mit einer Zeile je Erfolgskriterium der Roadmap, jede mit Datei
    und Testnamen oder Kommandoausgabe als Fundstelle:
    Kriterium 1 (Eintrag je Aufruf, Grund bei Ablehnung, kein Werkzeug kommt vorbei),
    Kriterium 2 (kein Wert und kein Inhalt, Erlaubnisliste, Vertragstest nach dem Muster des
    Budget-Gates),
    Kriterium 3 (Prüfkommando bestätigt oder benennt die erste Stelle, die veränderte Zeile
    wird gefunden),
    Kriterium 4 (eigene Ablage, Obergrenze, Frist ab 180 Tagen, Nachbar bleibt schreibfähig),
    Kriterium 5 (purge, Entfernen über die Oberfläche, Verbindung trennen, Pausieren lassen die
    Einträge stehen).

    Kriterium 5 hat zwei verschiedene Belegarten, und die Tabelle sagt bei jeder, welche es ist.
    `purge` ist **getestet** (Task 1). Entfernen über die Oberfläche, Verbindung trennen und
    Pausieren sind **hergeleitet**: keiner dieser drei Pfade kennt die Audit-Ablage. Belege das
    mit einem grep statt mit einem Satz und nimm seine Ausgabe als Fundstelle in die Tabelle auf:
    `grep -rniE "audit|AUDIT_FILENAME|audit\.sqlite3" src/mcp_connector/oauth/connections.py src/mcp_connector/exapp/purge.py src/mcp_connector/exapp/lifecycle.py` liefert keine Zeile. Das sind die drei Dateien, in denen Trennen und Pausieren
    (`oauth/connections.py`), der Purge (`exapp/purge.py`) und der `enabled=0`-Haken der
    Oberfläche (`exapp/lifecycle.py`) liegen. Trifft der grep doch, ist das ein Befund und gehört
    als solcher in die SUMMARY, nicht weggelassen. Nenne in derselben Zeile die Tabelle in
    `18-RESEARCH.md` §11 als Herleitung und `docs/uninstall.md:47-48` als Quelle für das
    Verhalten der Oberfläche.

    Schreibe zu Kriterium 5 die Grenze aus D-18 wörtlich dazu, ohne sie zu beschönigen:
    `occ app_api:app:unregister mcp_connector --rm-data` entfernt das Volume und mit ihm die
    Log-Datei, weil sie nach D-01 neben dem OAuth-Speicher liegt; dasselbe gilt für das
    Entfernen mit gesetztem Haken auf Nextcloud 32 und 33. Das ausdrückliche Löschen der Daten
    durch den Administrator ist kein Fall, gegen den diese Phase schützt. Belege beide Zeilen
    mit `docs/uninstall.md:19`, `:47`, `:48` und `:229-235`. Die Datei `docs/uninstall.md` wird
    in dieser Phase **nicht** geändert: der Textnachzug ist AUDIT-06 und gehört zu Phase 19.

    Nenne zuletzt, was diese Phase nicht geliefert hat und wer es liefert: Lesen und Exportieren
    (AUDIT-04), die sichtbare Beschriftung samt Mitbestimmungshinweis (AUDIT-05) und der
    Textnachzug (AUDIT-06), alle drei Phase 19; die Auslieferung als 0.1.12 (EXAPP-12) liegt
    außerhalb des Meilensteins.
  </action>
  <verify>
    <automated>uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright &amp;&amp; uv run --no-sync vulture src scripts vulture_whitelist.py &amp;&amp; uv run --no-sync pytest tests/unit tests/contract -q &amp;&amp; uv run --no-sync pytest -m matrix -q &amp;&amp; uv run --no-sync python scripts/check_tool_budget.py | tr -d '\r' | grep -q "15712 bytes, 21 tools" &amp;&amp; git diff --stat scripts/check_tool_budget.py tests/contract/test_tool_surface.py docs/uninstall.md docs/privacy.md | wc -l | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - Sieben Gate-Zeilen in der SUMMARY, jede mit Kommando und Ergebnis.
    - Budget-Zeile mit derselben Zahl vor und nach der Phase: 15712 von 18000 über 21
      Werkzeuge; `scripts/check_tool_budget.py` und `tests/contract/test_tool_surface.py` sind
      unverändert.
    - Nachweistabelle mit fünf Zeilen, jede mit einer Fundstelle aus dieser Phase (Datei plus
      Testname oder Kommandoausgabe), keine Zeile mit "erledigt" ohne Beleg.
    - Zeile 5 der Tabelle trennt getestet von hergeleitet: `purge` mit dem Testnamen aus
      `tests/unit/test_exapp_purge.py`, die drei übrigen Vorgänge mit der leeren grep-Ausgabe
      über `oauth/connections.py`, `exapp/purge.py` und `exapp/lifecycle.py` und dem Verweis auf
      `18-RESEARCH.md` §11. Kein Vorgang steht als getestet da, der es nicht ist.
    - Die D-18-Grenze steht wörtlich in der SUMMARY, mit den vier Zeilenverweisen auf
      `docs/uninstall.md`; die Datei selbst ist unverändert.
    - `docs/privacy.md`, `docs/uninstall.md`, `CHANGELOG.md`, `appinfo/info.xml`-Version und
      `pyproject.toml` sind in dieser Phase unverändert (Phase 19 und EXAPP-12).
  </acceptance_criteria>
  <done>Die Phase steht auf Belegen und nicht auf Behauptungen, und was sie nicht kann, steht beim Namen darin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Administrator zu Daten | Wer das Volume löscht, löscht das Log; das ist eine Handlung und kein Angriff |
| Phase zu Phase | Was hier nicht belegt ist, wird in Phase 19 als wahr weitergeschrieben |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-18-22 | Repudiation | Überleben des Logs beim Purge | mittel | mitigate | Ein Test hält die Behauptung, statt sie nur zu formulieren; `purge.py` bleibt unverändert |
| T-18-25 | Repudiation | Trennen, Pausieren, Entfernen über die Oberfläche | mittel | accept | Kein Test, sondern ein grep über die drei Aufräumpfade plus die Herleitung in 18-RESEARCH.md §11. Die Nachweistabelle weist das ausdrücklich als hergeleitet aus, damit Phase 19 es nicht als geprüft weiterschreibt |
| T-18-05 | Denial of Service | Volume mit beiden Dateien | hoch | mitigate | Ein Fall belegt, dass der OAuth-Speicher nach dem Erreichen der Audit-Obergrenze schreibbar bleibt |
| T-18-23 | Repudiation | Grenze aus D-18 | mittel | accept | `--rm-data` entfernt das Volume und mit ihm das Log. Wird beim Namen genannt statt wegkonstruiert; ein Pfad außerhalb des Volumes wurde in CONTEXT.md erwogen und verworfen |
| T-18-24 | Tampering | Werkzeugfläche und Gate-Grenzwerte | mittel | mitigate | Budget-Stillstand als Beleg vor und nach der Phase; `check_tool_budget.py` und `test_tool_surface.py` unverändert |
| T-18-SC | Tampering | pyproject.toml, uv.lock | mittel | mitigate | Diese Phase hat kein Paket installiert; beide Dateien sind unverändert |
</threat_model>

<verification>
- Sieben Gate-Läufe grün, jeder mit Kommando und Ergebnis in der SUMMARY.
- `uv run --no-sync python scripts/check_tool_budget.py`: 15712 Bytes, 21 Werkzeuge, Grenzwert
  18000 unverändert.
- `git status --short pyproject.toml uv.lock docs/ CHANGELOG.md` leer.
- Nachweistabelle mit fünf Zeilen und einer Fundstelle je Erfolgskriterium; Zeile 5 weist
  getestete und hergeleitete Vorgänge getrennt aus.
- `grep -rniE "audit|AUDIT_FILENAME|audit\.sqlite3" src/mcp_connector/oauth/connections.py src/mcp_connector/exapp/purge.py src/mcp_connector/exapp/lifecycle.py` ohne Treffer.
</verification>

<success_criteria>
Jedes der fünf Erfolgskriterien der Roadmap ist mit einer Fundstelle aus dieser Phase belegt,
die Gates sind grün, das Werkzeugbudget steht still, und die gemessene Grenze aus D-18 steht
beim Namen im Abschlussbericht.
</success_criteria>

<output>
Create `.planning/phases/18-audit-log-kern/18-10-SUMMARY.md` when done
</output>
