---
phase: 18-audit-log-kern
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/audit/allowlist.py
  - tests/contract/test_audit_surface.py
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "Eine Erlaubnisliste nennt je Werkzeug die Parameternamen, die im Log erscheinen dürfen (D-06)"
    - "Ein Werkzeug ohne Eintrag in der Liste lässt den Vertragstest fehlschlagen, es gibt keinen stillen leeren Eintrag"
    - "Ein Name in der Liste, den das Werkzeug nicht mehr hat, lässt den Vertragstest fehlschlagen"
    - "Kein Name aus der Sperrliste steht in einer Erlaubnisliste; die Sperrliste läuft nachweislich nicht leer"
  artifacts:
    - path: "src/mcp_connector/audit/allowlist.py"
      provides: "PARAM_ALLOWLIST je Werkzeug und FORBIDDEN_PARAMS als Sperrliste"
      contains: "PARAM_ALLOWLIST"
    - path: "tests/contract/test_audit_surface.py"
      provides: "Messung über alle 21 Werkzeuge nach dem Muster des Budget-Gates"
      contains: "PARAM_ALLOWLIST"
  key_links:
    - from: "tests/contract/test_audit_surface.py"
      to: "src/mcp_connector/audit/allowlist.py"
      via: "Vergleich der Liste gegen die gemessene Werkzeugfläche"
      pattern: "PARAM_ALLOWLIST"
---

<objective>
Die Erlaubnisliste aus D-06 entsteht samt dem Vertragstest, der sie hält. Der Test ist eine
Messung über alle registrierten Werkzeuge nach dem Muster von `scripts/check_tool_budget.py`,
keine Stichprobe, und er benennt jeden Treffer.

Purpose: Erfolgskriterium 2 der Roadmap hängt an dieser Datei. Sie entsteht vor dem
Erfassungspfad, damit der Rekorder in Plan 18-06 gegen eine schon geprüft Liste schneidet und
nicht umgekehrt.
Output: `audit/allowlist.py` mit `PARAM_ALLOWLIST` und `FORBIDDEN_PARAMS`, dazu
`tests/contract/test_audit_surface.py` mit drei Behauptungen und einer Gegenprobe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-audit-log-kern/18-CONTEXT.md
@.planning/phases/18-audit-log-kern/18-PATTERNS.md
@.planning/phases/18-audit-log-kern/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: audit/allowlist.py mit Erlaubnisliste und Sperrliste</name>
  <files>src/mcp_connector/audit/allowlist.py</files>
  <read_first>
    - `src/mcp_connector/exapp/config_values.py` Zeilen 103 bis 135: die Form einer benannten
      Aufzählung mit einem Kommentar, der die Reihenfolge begründet.
    - `tests/contract/test_tool_surface.py` Zeilen 34 bis 83: das eingefrorene Literal
      `EXPECTED_TOOLS` und die Sperrliste `FORBIDDEN_PROPERTIES` samt ihrer Begründungszeile.
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §2 (Zeilen 338 bis 437): die gemessene
      Parameterfläche aller 21 Werkzeuge und die drei Behauptungen, die der Gate halten muss.
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt
      "`src/mcp_connector/audit/allowlist.py`" (Zeilen 447 bis 478), besonders der Namenszwang
      aus dem Modulzustands-Gate.
  </read_first>
  <action>
    Lege `src/mcp_connector/audit/allowlist.py` an. Reine Daten, keine Funktion mit Logik, kein
    Import außer `typing`/`collections.abc`.

    `PARAM_ALLOWLIST: Mapping[str, frozenset[str]]` in Großbuchstaben. Der Name ist Pflicht und
    nicht Geschmack: `tests/contract/test_no_destructive_calls.py:635` nimmt `target.id.isupper()`
    als einzige Ausnahme vom Verbot des Modulzustands, ein `_param_allowlist` auf Modulebene
    macht dieses Gate rot. Schreibe den Grund als Kommentar darüber.

    Die Liste trägt genau diese 21 Einträge, alphabetisch, jeder mit den Namen aus der
    gemessenen Fläche in 18-RESEARCH.md §2 abzüglich der Sperrliste:
    `calendar_create_event`: all_day, calendar, end, start, timezone.
    `calendar_list_events`: calendar, end, limit, start, timezone.
    `contacts_search`: limit, query.
    `deck_browse`: board_id, level, limit, stack_id.
    `deck_create_card`: board_id, duedate, stack_id.
    `fetch`: id.
    `files_list`: cursor, limit, path.
    `files_read`: offset, path.
    `files_search`: cursor, folder, limit, query.
    `files_upload`: path.
    `mail_browse`: account_id, cursor, filter, level, limit, mailbox_id.
    `notes_create`: category.
    `notes_read`: note_id.
    `notes_search`: limit, query.
    `prepare_context`: detail, query.
    `search`: query.
    `tables_browse`: cursor, level, limit, table_id.
    `tables_create_row`: table_id.
    `talk_browse`: cursor, level, limit, token.
    `talk_send`: token.
    `unified_search`: limit, providers, query.

    `FORBIDDEN_PARAMS: frozenset[str]` mit `content`, `description`, `location`, `message`,
    `summary`, `title`, `values`. Die Begründungszeile darüber sagt, warum genau diese: es
    sind die Namen der Nutzlastfelder. Dass ein Inhalt mitgegeben wurde, ist bei einem
    Schreibwerkzeug ohnehin trivial wahr, der Name trägt also keine Auskunft und stünde nur
    da, um eines Tages versehentlich mit einem Wert zu wachsen (18-RESEARCH.md Zeilen 428 bis
    433). Nenne dabei ausdrücklich die drei klaren Fälle aus der Recherche
    (`content` bei `files_upload` und `notes_create`, `message` bei `talk_send`) und die vier,
    die aus demselben Grund dazukommen.

    Der Modul-Docstring sagt in zwei Sätzen, was die Datei ist und was sie nicht ist: sie nennt
    Namen, niemals Werte, und sie ist die Grenze, die der Vertragstest hält. Keine Zeile dieser
    Datei liest einen Wert, keine Funktion dieser Datei bekommt einen Aufrufparameter aus einer
    Anfrage.
  </action>
  <verify>
    <automated>uv run --no-sync ruff check src/mcp_connector/audit/allowlist.py &amp;&amp; uv run --no-sync python -c "from mcp_connector.audit import allowlist as a; print(len(a.PARAM_ALLOWLIST)); print(sorted(n for s in a.PARAM_ALLOWLIST.values() for n in s if n in a.FORBIDDEN_PARAMS))" | tr -d '\r' | head -2 | grep -qx "21" &amp;&amp; uv run --no-sync pytest tests/contract/test_no_destructive_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PARAM_ALLOWLIST` hat genau 21 Schlüssel, keiner davon mit einer leeren Menge.
    - Kein Name aus `FORBIDDEN_PARAMS` steht in irgendeiner Menge der Erlaubnisliste.
    - Der Name `PARAM_ALLOWLIST` ist durchgehend groß geschrieben;
      `uv run --no-sync pytest tests/contract/test_no_destructive_calls.py -q` bleibt grün.
    - `allowlist.py` importiert kein Modul dieses Projekts.
  </acceptance_criteria>
  <done>Die Erlaubnisliste steht als reine Daten da, mit dem Namen, den das Modulzustands-Gate verlangt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vertragstest über alle Werkzeuge nach dem Muster des Budget-Gates</name>
  <files>tests/contract/test_audit_surface.py</files>
  <behavior>
    - Vollständigkeit: `set(PARAM_ALLOWLIST)` ist gleich der Menge der registrierten
      Werkzeugnamen. Ein Werkzeug ohne Eintrag ist ein Fehlschlag mit seinem Namen im
      Fehlertext, kein stiller leerer Eintrag.
    - Keine Erfindungen: jeder Name einer Erlaubnisliste steht in den `properties` des
      zugehörigen Werkzeugschemas. Jeder Treffer wird als `werkzeug.name` gesammelt und
      gemeinsam gemeldet.
    - Keine Verräter: kein Name einer Erlaubnisliste steht in `FORBIDDEN_PARAMS`.
    - Gegenprobe zur Sperrliste: mindestens ein Name aus `FORBIDDEN_PARAMS` kommt in der
      gemessenen Werkzeugfläche tatsächlich vor. Sonst hält die Sperrliste nichts und
      niemand merkt es.
  </behavior>
  <read_first>
    - `scripts/check_tool_budget.py` Zeilen 120 bis 157: die Messung über alle Werkzeuge und
      der Fehlschlag, der jeden Treffer benennt statt nur die Tatsache.
    - `tests/contract/test_tool_surface.py` Zeilen 39 bis 59 (Mengenvergleich gegen das
      eingefrorene Literal), 79 bis 86 (Sperrliste und `_properties`), 709 bis 726 (dieselbe
      Behauptung als pytest-Gate mit gesammelten Befunden).
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt
      "`tests/contract/test_audit_surface.py`" (Zeilen 761 bis 838), besonders die Warnung
      Falle 9.
  </read_first>
  <action>
    Lege `tests/contract/test_audit_surface.py` an, `@pytest.mark.anyio`, mit der
    `anyio_backend`-Fixture aus `tests/conftest.py`. Hole die Fläche über
    `async with Client(mcp, raise_exceptions=True) as client: await client.list_tools()`, genau
    wie `check_tool_budget.py:120-122` und `test_tool_surface.py:84-86`. Registriere nichts am
    Modulsingleton `mcp`, auch nicht vorübergehend: `test_tool_surface.py:508-515` vergleicht
    die Werkzeugmenge gegen ein eingefrorenes Literal und `len(tools) == 21` und würde rot
    (Falle 9).

    Decke die vier Punkte aus `<behavior>` in vier Fällen ab. Jeder Fall sammelt seine Befunde
    in einer Liste `findings: list[str]` und behauptet am Ende `findings == []` mit einem Satz,
    der sagt, warum der Befund ein Problem ist, nicht nur dass er einer ist.

    Der Modul-Docstring nennt die Bedrohung, die diese Datei hält: T-18-01 (ein Parameterwert
    oder ein Ergebnisinhalt im Log) und T-18-02 (ein Aufrufer schreibt einen erfundenen
    Parameternamen ins Log), und den Satz aus AUDIT-01, dass der Test nach dem Muster des
    Budget-Gates fehlschlägt, sobald ein Werkzeug die Grenze überschreitet.

    Der Dekorator-Nachweis aus D-04 gehört **nicht** in diesen Plan: der Marker entsteht in
    Plan 18-06 und bekommt dort seinen Fall in derselben Datei.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/contract/test_audit_surface.py -q &amp;&amp; uv run --no-sync pytest tests/unit tests/contract -q &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
  </verify>
  <acceptance_criteria>
    - Vier Fälle, grün; die Namen der Fälle sagen die Behauptung, nicht die Mechanik.
    - Gegenprobe von Hand, in der SUMMARY belegt: ein Werkzeugname wird in einer Kopie der
      Erlaubnisliste entfernt, der Vollständigkeitsfall wird rot und nennt genau diesen Namen;
      danach wird die Änderung zurückgenommen.
    - Zweite Gegenprobe, ebenfalls belegt: ein erfundener Name in einer Erlaubnisliste macht den
      Erfindungsfall rot mit `werkzeug.name` im Fehlertext.
    - `uv run --no-sync pytest tests/unit tests/contract -q` grün, `len(tools) == 21` in
      `tests/contract/test_tool_surface.py` bleibt unberührt.
    - `uv run --no-sync python scripts/check_tool_budget.py` meldet unverändert 15712 Bytes
      über 21 Werkzeuge.
  </acceptance_criteria>
  <done>Die Grenze aus D-06 wird von einer Messung über alle Werkzeuge gehalten und nicht von Disziplin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aufrufer zu Erfassung | Die Argumente eines Werkzeugaufrufs sind Eingabe von außen, auch ihre Schlüsselnamen |
| Werkzeugfläche zu Erlaubnisliste | Zwei Listen, die auseinanderlaufen können; der Gate hält sie zusammen |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-18-01 | Information Disclosure | audit/allowlist.py | hoch | mitigate | Nur Namen, nie Werte; die Nutzlastnamen stehen auf der Sperrliste und können deshalb nicht eines Tages mit einem Wert wachsen |
| T-18-02 | Tampering | PARAM_ALLOWLIST | mittel | mitigate | Der Rekorder schneidet die gesetzten Schlüssel mit dieser Liste (Plan 18-06); ein erfundener Schlüsselname erreicht damit keine Zeile |
| T-18-14 | Tampering | tests/contract/test_tool_surface.py | niedrig | mitigate | Der neue Gate registriert nichts am Modulsingleton `mcp`; der Gegenbeweis läuft ohne Registrierung (Falle 9) |
| T-18-SC | Tampering | pyproject.toml, uv.lock | mittel | mitigate | Kein Paket, keine neue Abhängigkeit; beide Dateien bleiben unverändert |
</threat_model>

<verification>
- `uv run --no-sync pytest tests/unit tests/contract -q` grün.
- `uv run --no-sync ruff check .`, `ruff format --check .`, `pyright`,
  `vulture src scripts vulture_whitelist.py` grün.
- `uv run --no-sync python scripts/check_tool_budget.py`: 15712 Bytes, 21 Werkzeuge, Grenzwert
  18000 unverändert.
</verification>

<success_criteria>
Eine Erlaubnisliste je Werkzeug existiert, und ein Vertragstest über alle 21 Werkzeuge schlägt
fehl, sobald ein Werkzeug die Grenze aus D-06 überschreitet. Die Werkzeugfläche selbst ist
unverändert.
</success_criteria>

<output>
Create `.planning/phases/18-audit-log-kern/18-02-SUMMARY.md` when done
</output>
