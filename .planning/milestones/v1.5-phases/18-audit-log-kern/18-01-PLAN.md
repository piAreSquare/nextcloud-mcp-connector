---
phase: 18-audit-log-kern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/audit/__init__.py
  - src/mcp_connector/audit/store.py
  - tests/unit/test_audit_store.py
autonomous: true
requirements: [AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "Eine zweite SQLite-Datei liegt neben oauth.sqlite3 im selben Volume, mit eigener Verbindung und eigenem WAL (D-01)"
    - "Zwei aufeinanderfolgende Einträge einer Kette sind über prev_hash und hash verkettet, der erste zeigt auf GENESIS"
    - "Zwanzig gleichzeitig geschriebene Einträge desselben Nutzers ergeben eine Kette ohne Gabelung (BEGIN IMMEDIATE)"
    - "Nutzerketten und die Instanzkette liegen in derselben Tabelle und berühren sich nicht (D-02, D-03)"
    - "used_bytes fällt nach einem DELETE, page_count mal page_size fällt nicht"
    - "D-16: die Spalte actor steht von Anfang an im Schema, ebenso gap_chain und gap_hash für die Grabsteine aus Plan 18-04"
  artifacts:
    - path: "src/mcp_connector/audit/store.py"
      provides: "AuditStore: Schema, Pragmas, Kettenanhang in einer Transaktion, used_bytes"
      contains: "AUDIT_FILENAME"
    - path: "src/mcp_connector/audit/__init__.py"
      provides: "audit_opener(env) als Closure, AUDIT_STATE_ATTR"
      contains: "AUDIT_STATE_ATTR"
    - path: "tests/unit/test_audit_store.py"
      provides: "Schema, Pragmas, Kette, Nebenläufigkeit, used_bytes gegen eine echte Datei"
  key_links:
    - from: "src/mcp_connector/audit/__init__.py"
      to: "src/mcp_connector/config.py"
      via: "config.persistent_storage(env) / AUDIT_FILENAME"
      pattern: "persistent_storage"
    - from: "src/mcp_connector/audit/store.py"
      to: "hashlib"
      via: "sha256 über die kanonische Feldliste plus prev_hash"
      pattern: "hashlib\\.sha256"
---

<objective>
Die Ablage aus D-01 entsteht: eine zweite SQLite-Datei mit eigener Verbindung, dem Schema aus
18-RESEARCH.md §4, den drei Pragmas von `oauth/store.py` plus `auto_vacuum = INCREMENTAL`, und
dem Anhängen an eine Hash-Kette in genau einer Transaktion.

Purpose: Jeder weitere Plan dieser Phase schreibt oder liest über dieses Objekt. Die
Feldreihenfolge der Kanonisierung und die Kettenkennungen werden hier einmal festgelegt und
danach nicht mehr verhandelt.
Output: das Paket `src/mcp_connector/audit/` mit `store.py` und `__init__.py`, dazu die
Unit-Tests gegen eine echte Datei in `tmp_path`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-audit-log-kern/18-CONTEXT.md
@.planning/phases/18-audit-log-kern/18-PATTERNS.md
@.planning/phases/18-audit-log-kern/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: audit/store.py mit Schema, Pragmas und Kettenanhang</name>
  <files>src/mcp_connector/audit/store.py</files>
  <read_first>
    - `src/mcp_connector/oauth/store.py`: Zeilen 33-49 (Importblock und `type Work[T]`), 82-97
      (Dateiname und Lebensdaürn als benannte Konstanten mit Begründungszeile), 158-178
      (SCHEMA als Modulkonstante mit Spaltenkommentaren), 251-258 (`token_hash`), 394-411
      (Objekt statt Modulzustand, Schema-Flag am Objekt), 1344-1354 (die drei Hüllen),
      1356-1406 (`_call`, `BEGIN IMMEDIATE`, best-effort-`ROLLBACK`, `conn.close()` im
      `finally`), 1447-1466 (`_connect` und die drei Pragmas), 1469-1499
      (`_add_missing_columns`), 1530-1533 (Timeout-Konstanten).
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt "`src/mcp_connector/audit/store.py`"
      (Zeilen 58 bis 297): dort steht jede zu übernehmende Stelle wörtlich.
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §4 (Zeilen 488 bis 620, Schema,
      Kanonisierung, Nebenläufigkeit), §8 (Zeilen 837 bis 953, `used_bytes`, WAL, Kosten),
      Fallen 3 bis 5 (Zeilen 1291 bis 1313), Codebeispiele (Zeilen 1375 bis 1408).
  </read_first>
  <action>
    Lege `src/mcp_connector/audit/store.py` an. Nur Standardbibliothek: `asyncio`,
    `contextlib`, `hashlib`, `json`, `sqlite3`, `time`, `dataclasses`, `pathlib`, `typing`.
    Kein Import aus `server/`, kein Import aus `exapp/`, kein `crypto` (der Purge löscht den
    Datenschlüssel, ein damit verschlüsseltes Log wäre danach unlesbar, 18-RESEARCH.md §11).

    Benannte Konstanten, jede mit einer Begründungszeile im Stil von `oauth/store.py:85-97`:
    `AUDIT_FILENAME = "audit.sqlite3"` (der Code sagt `oauth.sqlite3`, nicht `store.sqlite3`;
    das neue Gegenstück heißt analog), `RETENTION_DAYS = 180` und
    `SIZE_LIMIT_BYTES = 100_000_000` (D-09), `SWEEP_EVERY = 500`,
    `SWEEP_USER_CHECK_EVERY = 20`, `USER_SILENCE_DAYS = 30`, `SWEEP_BATCH_ROWS = 5000`
    (WAL-Grund, 18-RESEARCH.md Zeilen 899 bis 909), `CLIENT_NAME_LIMIT = 80`,
    `_BUSY_TIMEOUT_MS = 5000` und `_BUSY_TIMEOUT_SECONDS`, `GENESIS = b"\x00" * 32`,
    `ACTOR_UNKNOWN = "unknown"` (D-16: die Spalte wird ab Werk mit "unbekannt" gefüllt, der
    Code schreibt seine Zeichenketten auf Englisch).
    Kettenkennungen und Arten als Konstanten plus zwei Hilfen: `CHAIN_INSTANCE = "i:instance"`,
    `user_chain(nc_user) -> f"u:{nc_user}"`, `KIND_CALL = "call"`, `KIND_TOMBSTONE = "tombstone"`,
    `KIND_SWITCH = "switch"`. Ergebnisklassen: `OUTCOME_OK = "ok"`,
    `OUTCOME_REJECTED = "rejected"`, `OUTCOME_FAILED = "failed"` (D-07).

    `SCHEMA` als Modulkonstante nach dem Muster von `oauth/store.py:158-178`, mit
    Spaltenkommentaren, die den Grund und nicht nur die Bedeutung tragen. Tabelle `entries`
    genau nach 18-RESEARCH.md Zeilen 494 bis 515, mit `seq INTEGER PRIMARY KEY AUTOINCREMENT`
    (die Begründung, dass ohne `AUTOINCREMENT` eine Nummer nach einem Grabstein-Lauf zweimal
    vorkäme, gehört als Kommentar dazu), zusätzlich der Spalte `actor TEXT` aus D-16 und den
    zwei Spalten, die ein Grabstein in Plan 18-04 braucht: `gap_chain TEXT` (die Kette, deren
    Lücke er erklärt) und `gap_hash TEXT` (der Endhash des gewichenen Blocks, als Hex, damit
    die Kanonisierung ohne Sonderbehandlung JSON-fähig bleibt). Dazu
    den beiden Indizes `entries_chain_seq` auf `(chain, seq)` und `entries_at` auf `(at)`.
    Keine Fremdschlüssel, also entfällt `PRAGMA foreign_keys`; schreibe den Grund als
    Kommentar an `_connect`.

    `_connect(path, *, schema=True)` nach `oauth/store.py:1447-1466`, mit genau dieser
    Reihenfolge: `path.parent.mkdir(...)`, `sqlite3.connect(path, isolation_level=None,
    timeout=_BUSY_TIMEOUT_SECONDS)`, `PRAGMA journal_mode = WAL`,
    `PRAGMA busy_timeout = {_BUSY_TIMEOUT_MS}`, dann `PRAGMA auto_vacuum = INCREMENTAL`, und
    erst danach `conn.executescript(SCHEMA)`. Die Reihenfolge ist load bearing (Falle 4: nach
    der ersten Tabelle bleibt der Modus auf 0 und ist nur noch mit einem vollen `VACUUM`
    änderbar); das gehört als Kommentar an genau diese Zeile.

    `class AuditStore` mit `__init__(self, path: Path)` (kein Schlüssel, nichts zu maskieren),
    `_schema_ready`-Flag am Objekt, `_call(work, commit)` wörtlich nach
    `oauth/store.py:1389-1406` inklusive der Zeile
    `schema=not self._schema_ready or not self._path.exists()`, und den drei Hüllen `_read`,
    `_write`, `_transaction` über `asyncio.to_thread`.

    Kanonisierung als Modulfunktion `_canonical(fields: tuple[Any, ...]) -> bytes`:
    `json.dumps(list(fields), separators=(",", ":"), ensure_ascii=False).encode("utf-8")`.
    Die Feldreihenfolge wird hier einmal festgelegt und ist ab jetzt unveränderlich; schreibe
    sie als Konstante `CANONICAL_FIELDS: tuple[str, ...]` mit genau dieser Reihenfolge hin:
    `seq, chain, kind, at, actor, nc_user, tool, client_id, auth_id, client_name, outcome,
    reason, duration_ms, params, removed, gap_chain, gap_hash`, also siebzehn Namen und damit
    jede Spalte außer `prev_hash` und `hash`. Der Zeilenhash ist
    `hashlib.sha256(_canonical(...) + prev_hash).digest()`, also 32 rohe Bytes als `BLOB`
    (18-RESEARCH.md Zeilen 544 bis 549). `seq` gehört in die Kanonisierung, sonst liesse sich
    eine Zeile innerhalb ihrer Kette umnummerieren.

    Eine Datenklasse `Entry` (frozen, slots) für die Felder eines Eintrags ohne `seq`,
    `prev_hash` und `hash`, damit die Aufrufer nicht mit einem Tupel aus fünfzehn Stellen
    hantieren. `params` reist als `list[str]` und wird beim Schreiben mit
    `json.dumps(sorted(params), separators=(",", ":"))` abgelegt.

    Zwei öffentliche Methoden:
    `append(self, entry: Entry) -> int` läuft über `_transaction` und tut in genau einer
    Transaktion: `BEGIN IMMEDIATE`, `SELECT hash FROM entries WHERE chain = ? ORDER BY seq DESC
    LIMIT 1`, `INSERT`, `COMMIT`. Kein Lesen und Schreiben in zwei Transaktionen (Falle 5).
    Der Rückgabewert ist der vergebene `seq`, weil D-11 sein Bündelungsintervall daran
    hängt.
    `last_entry(self, chain: str, *, kind: str | None = None) -> Entry | None` liest die
    jüngste Zeile einer Kette, wahlweise auf eine Art eingeschränkt. Der Schalter aus D-15
    braucht sie in Plan 18-07.

    `used_bytes(conn)` als Modulfunktion wörtlich nach dem Codebeispiel in 18-RESEARCH.md
    Zeilen 1379 bis 1391, samt dem Docstring, der die Messung nennt: nach dem Löschen der
    Hälfte aller Zeilen fällt weder `os.stat` noch `page_count * page_size`, nur
    `(page_count - freelist_count) * page_size`. Dazu eine `async def size(self) -> int`, die
    sie über `_read` anbietet.

    Der Modul-Docstring trägt die Grenzbeschreibung aus 18-RESEARCH.md Zeilen 589 bis 597:
    diese Kette schützt gegen die unbemerkte Änderung einer Zeile, nicht gegen jemanden, der
    die Datei schreiben und die Kette neu rechnen kann; es gibt keinen externen Anker. Die
    Wörter revisionssicher, AI-Act-konform, DSGVO-konform und SIEM-zertifiziert kommen in
    keinem Kommentar dieser Phase vor.
  </action>
  <verify>
    <automated>uv run --no-sync ruff check src/mcp_connector/audit/store.py &amp;&amp; uv run --no-sync ruff format --check src/mcp_connector/audit/store.py &amp;&amp; uv run --no-sync python -c "import sqlite3,tempfile,pathlib,asyncio; from mcp_connector.audit import store as s; d=pathlib.Path(tempfile.mkdtemp()); st=s.AuditStore(d/s.AUDIT_FILENAME); asyncio.run(st.size()); c=sqlite3.connect(d/s.AUDIT_FILENAME); print('auto_vacuum', c.execute('PRAGMA auto_vacuum').fetchone()[0]); print('cols', [r[1] for r in c.execute('PRAGMA table_info(entries)')])" | grep -q "auto_vacuum 2"</automated>
  </verify>
  <acceptance_criteria>
    - `PRAGMA auto_vacuum` meldet auf einer frisch angelegten Datei den Wert 2 (INCREMENTAL);
      im Quelltext steht die Pragma-Zeile vor `executescript(SCHEMA)`.
    - `PRAGMA table_info(entries)` nennt die neunzehn Spalten `seq, chain, kind, at, actor,
      nc_user, tool, client_id, auth_id, client_name, outcome, reason, duration_ms, params,
      removed, gap_chain, gap_hash, prev_hash, hash`.
    - `CANONICAL_FIELDS` hat 17 Einträge, beginnt mit `seq` und nennt weder `prev_hash` noch
      `hash`; `GENESIS` ist 32 Byte lang.
    - `append` enthält `BEGIN IMMEDIATE`, das `SELECT ... ORDER BY seq DESC LIMIT 1` und den
      `INSERT` in einem einzigen `work`-Rumpf.
    - `store.py` importiert weder `mcp_connector.server` noch `mcp_connector.exapp` noch
      `oauth.crypto`; `grep -c "^from\|^import" ` zeigt ausschließlich Standardbibliothek und
      `from .. import config` nicht (der Pfad kommt von außen).
    - `uv run --no-sync pyright src/mcp_connector/audit/store.py` ohne Fehler.
  </acceptance_criteria>
  <done>Die Ablage existiert, legt ihr Schema beim ersten Öffnen an und hängt Einträge verkettet in genau einer Transaktion an.</done>
</task>

<task type="auto">
  <name>Task 2: audit/__init__.py mit Fabrik und Zustandsname</name>
  <files>src/mcp_connector/audit/__init__.py</files>
  <read_first>
    - `src/mcp_connector/oauth/store.py` Zeilen 1409 bis 1444 (`store_opener` als Closure,
      Begründung gegen ein Modulglobal, D-20).
    - `src/mcp_connector/oauth/verifier.py` Zeilen 82 bis 90 (`OAUTH_STATE_ATTR`: die Konstante
      liegt bei ihrem Leser, damit die zwei Seiten nicht auseinanderlaufen).
    - `src/mcp_connector/config.py` Zeilen 240 bis 299 (`persistent_storage`, fail closed, die
      Schreibprobe).
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt
      "`src/mcp_connector/audit/__init__.py`" (Zeilen 300 bis 346) und Abschnitt "Berührte
      Gates" Nummer 2 (Zeilen 1260 bis 1296).
  </read_first>
  <action>
    Lege `src/mcp_connector/audit/__init__.py` an mit drei Dingen und sonst nichts.

    Erstens `AUDIT_STATE_ATTR = "audit_recorder"` mit demselben Kommentar-Muster wie
    `OAUTH_STATE_ATTR`: der Name, unter dem die Transportgrenze den Rekorder hinterlegt, und
    der Name, den der Erfassungspfad liest. Eine Konstante, damit die zwei Seiten nicht
    auseinanderlaufen.

    Zweitens `audit_opener(env: Mapping[str, str] | None = None) ->
    Callable[[], Awaitable[AuditStore]]` als Closure nach dem Muster von
    `oauth/store.py:1409-1444`, ohne den Schlüsselschritt: `opened: dict[str, AuditStore]` und
    `lock = asyncio.Lock()` liegen **innerhalb** der Funktion, niemals auf Modulebene. Der
    Grund gehört in den Docstring: der Gate in `tests/contract/test_no_destructive_calls.py`
    zählt die Ausnahmen für Modulzustand und lässt genau zwei zu (`:658`), das Audit-Modul
    meldet keinen dritten Cache an. Der Pfad ist
    `config.persistent_storage(env) / store.AUDIT_FILENAME`.

    Drittens ein `__all__` mit `AUDIT_STATE_ATTR`, `audit_opener` und den Re-Exports, die
    andere Module brauchen (`AuditStore`, `Entry`, die Kettenkennungen, die drei Ergebnis-
    klassen). Kein Import von `record` und kein Import von `allowlist` in dieser Datei: beide
    entstehen in späteren Plänen und würden hier nur einen Ring anlegen.
  </action>
  <verify>
    <automated>uv run --no-sync ruff check src/mcp_connector/audit/ &amp;&amp; uv run --no-sync python -c "from mcp_connector import audit; print(audit.AUDIT_STATE_ATTR); print(callable(audit.audit_opener))" | grep -q "audit_recorder" &amp;&amp; uv run --no-sync pytest tests/contract/test_no_destructive_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `from mcp_connector import audit` gelingt ohne laufende Anwendung; `audit.AUDIT_STATE_ATTR`
      ist `"audit_recorder"`.
    - `audit_opener(env)()` zweimal aufgerufen liefert dasselbe `AuditStore`-Objekt, zwei
      verschiedene Opener liefern zwei verschiedene.
    - Auf Modulebene von `audit/__init__.py` und `audit/store.py` steht kein veränderlicher
      Name in Kleinschreibung; `uv run --no-sync pytest tests/contract/test_no_destructive_calls.py -q`
      bleibt grün, ohne dass `ALLOWED_MODULE_STATE` einen dritten Eintrag bekommt.
    - `grep -c "ALLOWED_MODULE_STATE" tests/contract/test_no_destructive_calls.py` bleibt
      unverändert; die Datei wird in diesem Plan nicht angefasst.
  </acceptance_criteria>
  <done>Ein Opener je Anwendung, kein Modulzustand, und ein Name für den hinterlegten Rekorder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-Tests gegen eine echte Datei</name>
  <files>tests/unit/test_audit_store.py</files>
  <behavior>
    - Eine frisch geöffnete Datei hat `journal_mode = wal`, `auto_vacuum = 2` und die Tabelle
      `entries` mit den siebzehn Spalten.
    - Der erste Eintrag einer Kette trägt `prev_hash == GENESIS`; der zweite trägt den `hash`
      des ersten.
    - Der `hash` einer Zeile ist reproduzierbar: derselbe kanonische Rumpf plus `prev_hash`
      ergibt denselben Digest.
    - Zwanzig gleichzeitig über `asyncio.gather` geschriebene Einträge desselben Nutzers
      ergeben zwanzig Zeilen mit lückenloser Verkettung und keinem doppelten `prev_hash`.
    - Ein Eintrag in `CHAIN_INSTANCE` verlängert die Nutzerkette nicht und umgekehrt.
    - `last_entry(chain, kind=KIND_SWITCH)` findet die jüngste Schaltzeile und übergeht
      Zeilen der Art `call`.
    - `size()` fällt nach einem `DELETE` an der Ablage vorbei, `page_count * page_size` fällt
      nicht.
    - `params` liegt sortiert und als JSON-Liste in der Zeile; ein Wert steht in keiner Spalte.
  </behavior>
  <read_first>
    - `tests/unit/test_oauth_store.py` Zeilen 1 bis 10 (Docstring: warum eine echte Datei und
      kein Mock) und 43 bis 44 (die Öffnungshilfe).
    - `tests/conftest.py` Zeilen 13 bis 16 (`anyio_backend`; `pytest-asyncio` ist keine
      Abhängigkeit dieses Projekts).
    - `tests/unit/test_exapp_purge.py` Zeilen 143 bis 153 (`counts`: an der Ablage vorbei mit
      einer eigenen `sqlite3`-Verbindung lesen).
  </read_first>
  <action>
    Lege `tests/unit/test_audit_store.py` an, `pytestmark = pytest.mark.anyio` für die
    asynchronen Fälle, alles gegen eine echte Datei in `tmp_path`. Der Modul-Docstring nennt
    die Bedrohungen, die hier abgedeckt werden: T-18-03 (eine nachträglich veränderte Zeile),
    T-18-05 (eine Ablage, die ohne Grenze wächst) und T-18-01 (ein Wert im Log).

    Schreibe die Öffnungshilfe `open_store(tmp_path) -> store.AuditStore` nach dem Muster von
    `test_oauth_store.py:43-44` und eine Hilfe `rows(path)`, die mit einer eigenen
    `sqlite3`-Verbindung an der Ablage vorbei liest, nach dem Muster von
    `test_exapp_purge.py:143-153`.

    Decke die acht Verhaltenspunkte aus `<behavior>` ab, jeder Fall mit einem Namen, der die
    Behauptung nennt. Für den Nebenläufigkeitsfall `asyncio.gather` über zwanzig `append`
    desselben `user_chain("alice")` und danach die Behauptung, dass die Menge der `prev_hash`
    genau zwanzig verschiedene Werte hat. Für den `size`-Fall genug Zeilen einfügen, dass
    mehrere Seiten belegt sind (zweitausend reichen nach der Messung mit rund 227 Byte je
    Zeile), dann die Hälfte mit einer eigenen Verbindung löschen und beide Zahlen
    gegenüberstellen.

    Kein Fall in dieser Datei prüft Frist, Obergrenze oder das Prüfkommando: die entstehen in
    Plan 18-04 und bekommen dort ihre eigenen Fälle in derselben Datei.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_audit_store.py -q &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --no-sync pytest tests/unit/test_audit_store.py -q` läuft grün und enthält
      mindestens acht Fälle, je einen für die acht Punkte in `<behavior>`.
    - Der Nebenläufigkeitsfall schreibt über `asyncio.gather` und behauptet zwanzig
      verschiedene `prev_hash`-Werte.
    - Der `size`-Fall zeigt beide Zahlen: `size()` fällt, `page_count * page_size` nicht.
    - `uv run --no-sync ruff check .`, `ruff format --check .` und `pyright` sind grün.
    - `uv run --no-sync pytest tests/unit tests/contract -q` ist grün (kein bestehender Test
      wird durch das neue Paket rot).
  </acceptance_criteria>
  <done>Die Eigenschaften der Ablage sind behauptet und laufen gegen eine echte Datei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Werkzeugaufruf zu Ablage | Werte aus einer Anfrage dürfen die Schreibmethode erreichen, aber nie eine Spalte füllen |
| Dateisystem zu Ablage | Wer das Volume schreiben kann, kann Zeilen ändern; die Kette macht das sichtbar, verhindert es nicht |
| Ablage zu OAuth-Speicher | Zwei Dateien, zwei Verbindungen, ein Volume; die Ablage darf den Nachbarn nicht schreibunfähig machen |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-18-01 | Information Disclosure | audit/store.py, Spalte params | hoch | mitigate | Die Spalte nimmt nur eine sortierte JSON-Liste von Namen; keine Methode dieses Moduls nimmt einen Parameterwert oder einen Ergebnisinhalt entgegen (D-06) |
| T-18-03 | Tampering | Tabelle entries | mittel | mitigate | SHA-256 über `CANONICAL_FIELDS` plus `prev_hash`, `seq` eingeschlossen; die Prüfung dazu entsteht in Plan 18-04 |
| T-18-04 | Repudiation | Tabelle entries | hoch | accept | Wer die Datei schreiben kann, rechnet die Kette neu. Kein externer Anker, keine Signatur außerhalb des Volumes. Die Grenze steht als Absatz im Modul-Docstring (D-v1.5-02) |
| T-18-05 | Denial of Service | Volume, oauth.sqlite3 | hoch | mitigate | `auto_vacuum = INCREMENTAL` vor der ersten Tabelle und `used_bytes` als einzige Messgröße; die Obergrenze selbst greift in Plan 18-04 |
| T-18-12 | Information Disclosure | audit/store.py | mittel | mitigate | Kein Datenschlüssel, keine Verschlüsselung, kein `crypto`-Import: das Log trägt keine Geheimnisse und überlebt damit auch `crypto.delete_key` im Purge |
| T-18-SC | Tampering | pyproject.toml, uv.lock | mittel | mitigate | Diese Phase installiert kein Paket (18-RESEARCH.md §Paketprüfung); `pyproject.toml` und `uv.lock` bleiben unverändert und stehen in keinem `files_modified` |
</threat_model>

<verification>
- `uv run --no-sync pytest tests/unit tests/contract -q` grün.
- `uv run --no-sync ruff check .`, `ruff format --check .`, `pyright`,
  `vulture src scripts vulture_whitelist.py` grün.
- `git status --short pyproject.toml uv.lock appinfo/` ist leer.
- `uv run --no-sync python scripts/check_tool_budget.py` meldet weiterhin 15712 Bytes über 21
  Werkzeuge.
</verification>

<success_criteria>
Eine zweite SQLite-Datei mit eigenem Schema, eigenen Pragmas und einer Hash-Kette je Kettenkennung
steht bereit, ohne dass irgendein Aufrufer sie schon benutzt. Kein neues Paket, kein Modulzustand,
kein Werkzeug berührt.
</success_criteria>

<output>
Create `.planning/phases/18-audit-log-kern/18-01-SUMMARY.md` when done
</output>

