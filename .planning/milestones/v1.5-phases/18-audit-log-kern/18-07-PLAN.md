---
phase: 18-audit-log-kern
plan: 07
type: execute
wave: 4
depends_on: ["18-06"]
files_modified:
  - src/mcp_connector/config.py
  - src/mcp_connector/exapp/config_values.py
  - src/mcp_connector/exapp/admin_settings.py
  - src/mcp_connector/exapp/ui/strings.py
  - src/mcp_connector/entry_exapp.py
  - tests/unit/test_exapp_config_values.py
  - tests/unit/test_exapp_admin_settings.py
  - tests/unit/test_exapp_entry.py
autonomous: true
requirements: [AUDIT-01, AUDIT-03]

must_haves:
  truths:
    - "Ab Werk ist das Log aus: ohne Admin-Wert und ohne Umgebungsvariable wird keine Zeile geschrieben (D-14)"
    - "Ein 401 beim ersten Start ergibt {} und damit die Vorgabe aus dem Code, und die ist aus"
    - "Aufbewahrungsfrist und Obergrenze sind über die Umgebung einstellbar und erreichen 180 Tage (D-09)"
    - "Das Ein- und Ausschalten steht als eigene Zeile in der Instanzkette, mit Zeitpunkt, Richtung und actor unknown (D-15, D-16)"
    - "Ein Opener je Anwendung, gebaut an derselben Stelle wie der OAuth-Opener, kein Modulzustand"
    - "Der gebaute Rekorder trägt das aufgelöste env-Mapping, damit die Kontoprüfung aus D-12 in der laufenden Anwendung angeschlossen ist"
  artifacts:
    - path: "src/mcp_connector/config.py"
      provides: "ENV_AUDIT_LOG, ENV_AUDIT_RETENTION_DAYS, ENV_AUDIT_MAX_BYTES samt Lesern"
      contains: "ENV_AUDIT_LOG"
    - path: "src/mcp_connector/exapp/config_values.py"
      provides: "siebter Schlüssel in CONFIG_KEYS, KEY_TO_ENV und SWITCH_KEYS"
      contains: "audit_log"
    - path: "src/mcp_connector/entry_exapp.py"
      provides: "audit_opener, Recorder hinter dem Schalter, Schaltprotokoll beim Start"
      contains: "audit_opener"
  key_links:
    - from: "src/mcp_connector/entry_exapp.py"
      to: "src/mcp_connector/exapp/middleware.py"
      via: "Recorder als Konstruktorargument, nur wenn der Schalter an ist"
      pattern: "audit_recorder"
    - from: "src/mcp_connector/entry_exapp.py"
      to: "src/mcp_connector/audit/record.py"
      via: "note_switch beim Start, wenn der gelesene Zustand vom zuletzt protokollierten abweicht"
      pattern: "note_switch"
---

<objective>
Der Schalter aus D-14 entsteht, ab Werk aus, als siebter Konfigurationswert nach dem
bestehenden Muster. Die Anwendung baut den Audit-Opener an derselben Stelle wie den
OAuth-Opener, reicht den Rekorder an die Transportgrenze und protokolliert beim Start die
Schaltung selbst (D-15, D-16).

Purpose: Ohne diesen Plan läuft ein nutzerbezogenes Protokoll entweder gar nicht oder
ungefragt mit. Beides ist ausgeschlossen: es läuft genau dann, wenn ein Administrator es
eingeschaltet hat, und die Schaltung selbst hinterlässt eine Spur.
Output: drei Umgebungsvariablen samt Lesern, ein Schlüssel in drei Aufzählungen, ein
Formularfeld ohne Beschriftungsarbeit, die Verdrahtung in `entry_exapp.py` und die Tests dazu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-audit-log-kern/18-CONTEXT.md
@.planning/phases/18-audit-log-kern/18-RESEARCH.md
@.planning/phases/18-audit-log-kern/18-PATTERNS.md
@.planning/phases/18-audit-log-kern/18-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drei Umgebungswerte und ihre Leser in config.py</name>
  <files>src/mcp_connector/config.py</files>
  <read_first>
    - `src/mcp_connector/config.py` Zeile 47 (`ENV_TALK_SEND` als Namensmuster) und Zeilen 325
      bis 350 (`talk_send_enabled` samt der ausdrücklichen Aussage, dass es die Gegenrichtung
      fährt).
    - `src/mcp_connector/oauth/registry.py` Zeilen 284 bis 308 (`_switch`: leerer Wert gilt als
      ungesetzt, ein Tippfehler behält die Vorgabe und sagt es im Log).
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt "Konfigurationswert mit
      Vorgabe im Code" (Zeilen 1208 bis 1215).
    - `src/mcp_connector/audit/store.py`: `RETENTION_DAYS` und `SIZE_LIMIT_BYTES` als die
      Vorgaben, die hier überschrieben werden können.
  </read_first>
  <action>
    Lege in `config.py` drei Konstanten nach dem Namensmuster von `ENV_TALK_SEND` an:
    `ENV_AUDIT_LOG = "NC_MCP_AUDIT_LOG"`, `ENV_AUDIT_RETENTION_DAYS =
    "NC_MCP_AUDIT_RETENTION_DAYS"`, `ENV_AUDIT_MAX_BYTES = "NC_MCP_AUDIT_MAX_BYTES"`.

    Dazu drei Leser:
    `audit_log_enabled(env) -> bool` mit **Vorgabe False**. Das ist die positive Richtung, nicht
    die Gegenrichtung von `talk_send_enabled`; schreibe den Unterschied als Kommentar hin, damit
    niemand das falsche Vorbild kopiert. Ein leerer Wert gilt als ungesetzt, ein unbekannter Wert
    behält die Vorgabe und schreibt eine WARNING mit dem Namen des Feldes und **nie** dem Wert.
    `audit_retention_days(env) -> int` mit Vorgabe `store.RETENTION_DAYS` (180) und einer
    Untergrenze von 180: ein kleinerer Wert wird abgelehnt und behält die Vorgabe, weil AUDIT-03
    verlangt, dass die Frist mindestens 180 Tage erreichen kann. Ein größerer Wert ist erlaubt.
    `audit_size_limit(env) -> int` mit Vorgabe `store.SIZE_LIMIT_BYTES` (100_000_000) und einer
    Untergrenze von 1_000_000, damit eine Fehleingabe nicht jede Zeile sofort wieder wegräumt.
    Beide Zahlenleser nehmen nur eine reine Ziffernfolge an; alles andere behält die Vorgabe und
    schreibt eine WARNING ohne den Wert.

    Ein Import von `audit.store` in `config.py` ist zulässig, weil `store.py` nur die
    Standardbibliothek benutzt und `config` nicht importiert; prüfe das vor dem Schreiben und
    schreibe die Richtung als Kommentar dazu. Ist die Richtung doch verletzt, wiederhole die zwei
    Zahlen als Konstanten in `config.py` mit einem Kommentar, der auf `audit/store.py` als Ort
    der Begründung zeigt.
  </action>
  <verify>
    <automated>uv run --no-sync python -c "from mcp_connector import config; print(config.audit_log_enabled({})); print(config.audit_log_enabled({'NC_MCP_AUDIT_LOG':'on'})); print(config.audit_retention_days({'NC_MCP_AUDIT_RETENTION_DAYS':'10'})); print(config.audit_retention_days({'NC_MCP_AUDIT_RETENTION_DAYS':'365'}))" | tr -d '\r' | tr '\n' ' ' | grep -q "False True 180 365" &amp;&amp; uv run --no-sync pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `audit_log_enabled({})` ist `False`; `"on"`, `"true"`, `"1"` ergeben `True`; ein
      Tippfehler ergibt `False` und eine WARNING ohne den Wert.
    - `audit_retention_days` gibt nie weniger als 180 zurück.
    - `audit_size_limit` gibt nie weniger als 1_000_000 zurück.
    - Keine der drei Funktionen wirft bei einer beliebigen Zeichenkette.
    - `uv run --no-sync pytest tests/unit -q` grün.
  </acceptance_criteria>
  <done>Die dritte Stufe der Kette (Vorgabe im Code) steht, und sie sagt "aus".</done>
</task>

<task type="auto">
  <name>Task 2: Der siebte Schlüssel in den drei Aufzählungen und im Formular</name>
  <files>src/mcp_connector/exapp/config_values.py, src/mcp_connector/exapp/admin_settings.py, src/mcp_connector/exapp/ui/strings.py, tests/unit/test_exapp_config_values.py, tests/unit/test_exapp_admin_settings.py</files>
  <read_first>
    - `src/mcp_connector/exapp/config_values.py` Zeilen 1 bis 30 (Modul-Docstring und die Kette
      Admin-Wert, Umgebungsvariable, Vorgabe), 103 bis 135 (`CONFIG_KEYS`, `KEY_TO_ENV`,
      `SWITCH_KEYS`), 215 bis 260 (der bekannte 401 beim Startzeit-Lesen und die Regel, dass eine
      unlesbare Hülle nie eine leere ist), 396 bis 423 (`_switch` und `_rejected`).
    - `src/mcp_connector/exapp/admin_settings.py` Zeilen 1 bis 30 (Docstring samt der Zahl
      "six values" und den zwei Fallen) und 117 bis 175 (`form_scheme`, besonders
      `allowlist_field` mit `"default": False`).
    - `tests/unit/test_exapp_admin_settings.py` Zeile 139: der Test, der Formularfelder und
      `CONFIG_KEYS` **in derselben Reihenfolge** gleichsetzt.
    - `src/mcp_connector/exapp/ui/strings.py` Zeilen 600 bis 620 (die zwei Zeichenketten des
      Allowlist-Feldes als Formvorbild).
  </read_first>
  <action>
    Ergänze `config_values.py` an genau drei Stellen um den Schlüssel `"audit_log"`: in
    `CONFIG_KEYS` als letzten Eintrag, in `KEY_TO_ENV` auf `config.ENV_AUDIT_LOG`, und in
    `SWITCH_KEYS`. Zähle in den Kommentaren mit: aus sechs Schlüsseln werden sieben, und die
    Stellen, die "six" oder "sechs" sagen (Modul-Docstring von `admin_settings.py`, die
    Kommentarzeile über `CONFIG_KEYS`), werden mitgezogen.

    Ergänze `admin_settings.form_scheme` um ein siebtes Feld an der Position, die der Reihenfolge
    in `CONFIG_KEYS` entspricht, mit `"type": "checkbox"` und `"default": False`, und einem
    Kommentar, der den Auslieferungszustand begründet: ab Werk aus, weil ein nutzerbezogenes
    Protokoll nicht ungefragt mitlaufen darf (D-14). Kein `sensitive: true`, in keiner
    Schreibweise.

    Lege in `exapp/ui/strings.py` zwei Zeichenketten `ADMIN_FIELD_AUDIT_LOG_LABEL` und
    `ADMIN_FIELD_AUDIT_LOG_DESCRIPTION` an und trage sie in `__all__` ein. Halte beide **kurz und
    sachlich**: was gespeichert wird (Nutzer, Werkzeugname, Zeitpunkt, aufrufender Client,
    Ergebnisstatus) und was nicht (kein Parameterwert, kein Inhalt), und dass die Änderung erst
    nach einem Deaktivieren-Aktivieren-Zyklus wirkt, wie bei allen anderen Werten. Die
    ausführliche Beschriftung samt Mitbestimmungshinweis und Grenzbeschreibung ist AUDIT-05 und
    gehört in Phase 19; sie wird hier ausdrücklich **nicht** vorweggenommen. Keines der vier
    verbotenen Wörter (revisionssicher, AI-Act-konform, DSGVO-konform, SIEM-zertifiziert) und
    kein Werbesatz.

    Ziehe die beiden Tests mit: `tests/unit/test_exapp_config_values.py` bekommt den siebten
    Schlüssel in seinen Gleichheitsfällen, `tests/unit/test_exapp_admin_settings.py` prüft
    weiterhin die Gleichheit von Feldkennungen und `CONFIG_KEYS` und zusätzlich, dass das neue
    Feld `"default": False` trägt und kein `sensitive` kennt.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_config_values.py tests/unit/test_exapp_admin_settings.py -q &amp;&amp; uv run --no-sync python -c "from mcp_connector.exapp import config_values as c; print(len(c.CONFIG_KEYS)); print('audit_log' in c.SWITCH_KEYS)" | tr -d '\r' | tr '\n' ' ' | grep -q "7 True" &amp;&amp; ! grep -rniE "revisionssicher|AI-Act|DSGVO|SIEM" src/mcp_connector/exapp/ui/strings.py</automated>
  </verify>
  <acceptance_criteria>
    - `CONFIG_KEYS` hat sieben Einträge, `KEY_TO_ENV` sieben, `audit_log` steht in
      `SWITCH_KEYS`.
    - `form_scheme` liefert sieben Felder in derselben Reihenfolge wie `CONFIG_KEYS`; der Test in
      `test_exapp_admin_settings.py:139` bleibt grün.
    - Das neue Feld trägt `"type": "checkbox"` und `"default": False` und kein `sensitive`.
    - Kein verbotenes Wort in den neuen Zeichenketten; kein Mitbestimmungshinweis und keine
      Grenzbeschreibung (beides ist AUDIT-05, Phase 19).
    - Jede Stelle, die bisher "six" oder "sechs" Werte sagte, sagt jetzt sieben.
  </acceptance_criteria>
  <done>Der Schalter existiert als Konfigurationswert und als Kästchen, ohne dass die Beschriftungsarbeit aus Phase 19 vorweggenommen wird.</done>
</task>

<task type="auto">
  <name>Task 3: Verdrahtung in entry_exapp.py samt Schaltprotokoll</name>
  <files>src/mcp_connector/entry_exapp.py, tests/unit/test_exapp_entry.py</files>
  <read_first>
    - `src/mcp_connector/entry_exapp.py` Zeilen 85 bis 103 (wo der OAuth-Opener gebaut wird),
      129 bis 183 (der Kommentarblock über die Routen und die Aufzählung, in der sie angehängt
      werden), 239 bis 281 (`_resolved_env`) und Zeile 332 (`config.persistent_storage` beim
      Start).
    - `src/mcp_connector/audit/record.py` und `src/mcp_connector/audit/store.py` (Ergebnisse der
      Pläne 18-06 und 18-01), besonders `Recorder`, `note_switch`, `last_entry` und `sweep`.
    - `.planning/phases/18-audit-log-kern/18-RESEARCH.md` §9 (Zeilen 957 bis 1050): die Kette des
      Admin-Werts, der bekannte 401 beim ersten Start und die Wand, an die D-15 läuft.
    - `.planning/phases/18-audit-log-kern/18-PATTERNS.md` Abschnitt
      "`src/mcp_connector/entry_exapp.py`" (Zeilen 1081 bis 1107).
  </read_first>
  <action>
    Baue den Audit-Opener dort, wo der OAuth-Opener gebaut wird:
    `audit_store = audit.audit_opener(env)`. Ein Opener je Anwendung, kein Modulglobal.

    Baue den Rekorder **nur, wenn der Schalter an ist**:
    `recorder = record.Recorder(audit_store, env=env, retention_days=config.audit_retention_days(env),
    size_limit=config.audit_size_limit(env)) if config.audit_log_enabled(env) else None`, und
    reiche ihn als `audit_recorder=` an `RequireAppApi`. Das `env` ist dasselbe aufgelöste Mapping
    aus `_resolved_env()`, mit dem auch die übrigen Fabriken gebaut werden, und nicht
    `os.environ`: die Kontoprüfung aus D-12 (Plan 18-09) ruft damit Nextcloud auf, und ein
    zweites, nicht aufgelöstes Mapping hätte an dieser Stelle andere Werte. Ist er `None`, hinterlegt die
    Middleware nichts und es wird keine Zeile geschrieben. Schreibe den Kommentar dazu: das ist
    D-14 in einer Zeile, und der bekannte 401 beim ersten Start fällt in dieselbe Richtung, weil
    ein leeres Ergebnis die Vorgabe aus dem Code gelten lässt und die ist "aus".

    Ergänze den Startlauf um das Schaltprotokoll aus D-15 und D-16, als eigene, kurze Funktion,
    die nie wirft und deren Fehlschlag nur eine Logzeile mit dem Ausnahmetyp kostet:
    erstens: existiert die Datei `config.persistent_storage(env) / store.AUDIT_FILENAME` nicht
    **und** ist der Schalter aus, geschieht gar nichts. Kein Anlegen einer Datei für ein Log,
    das nie lief;
    zweitens sonst: `last_entry(CHAIN_INSTANCE, kind=KIND_SWITCH)` lesen. Weicht die Richtung
    vom gelesenen Schalterzustand ab, oder gibt es noch keine Schaltzeile und der Schalter ist
    an, dann `await record.note_switch(store, enabled=..., moment=int(time.time()))`;
    drittens, wenn die Datei existiert: einmal `await store.sweep(...)` mit den Werten aus der
    Umgebung. Das ist die einzige Gelegenheit, an der eine abgeschaltete Ablage ihre abgelaufenen
    Zeilen noch loswird, weil danach kein Schreibpfad mehr läuft; schreibe genau diesen Grund
    als Kommentar hin.
    Der Startlauf hängt an derselben Stelle wie `config.persistent_storage(resolved)` in
    `main`, also nachdem das Volume geprüft ist und bevor die erste Anfrage kommt.

    Der lange Kommentarblock über der Routenaufzählung bekommt später (Plan 18-08) seinen
    fünften Absatz; in diesem Plan wird keine Route angehängt.

    Ergänze `tests/unit/test_exapp_entry.py` um fünf Fälle: mit ausgeschaltetem Schalter
    bekommt die Middleware `audit_recorder=None` und es entsteht keine Datei; mit eingeschaltetem
    Schalter bekommt sie einen `Recorder`; der erste Start mit eingeschaltetem Schalter schreibt
    genau eine Zeile der Art `switch` mit `actor == "unknown"`; ein zweiter Start mit demselben
    Zustand schreibt keine zweite Zeile; und der fünfte Fall greift den in der
    Produktionskonstruktion gebauten `Recorder` ab und behauptet, dass sein Feld `env` genau das
    Mapping ist, mit dem die Anwendung gebaut wurde (Gleichheit gegen das übergebene Mapping,
    nicht `None` und nicht `os.environ`). Dieser fünfte Fall ist die einzige Stelle, an der die
    Verdrahtung der Kontoprüfung aus Plan 18-09 geprüft wird; ohne ihn stünde D-12 im Code und
    wäre in der laufenden Anwendung nicht angeschlossen.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_entry.py -q &amp;&amp; uv run --no-sync pytest tests/unit tests/contract -q &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright &amp;&amp; uv run --no-sync vulture src scripts vulture_whitelist.py &amp;&amp; uv run --no-sync python scripts/check_tool_budget.py | tr -d '\r' | grep -q "15712 bytes, 21 tools"</automated>
  </verify>
  <acceptance_criteria>
    - Mit `NC_MCP_AUDIT_LOG` ungesetzt entsteht keine Datei `audit.sqlite3` und keine Zeile;
      der Test behauptet die Abwesenheit der Datei.
    - Mit eingeschaltetem Schalter steht nach dem Start genau eine Zeile der Art `switch` in der
      Instanzkette, mit `actor == "unknown"` und der Richtung `"on"`.
    - Ein zweiter Start mit unverändertem Zustand schreibt keine zweite Schaltzeile.
    - Ein Fehlschlag des Startlaufs (Ablage nicht schreibbar) kostet den Start nichts: die
      Anwendung baut weiter, eine Logzeile nennt den Ausnahmetyp.
    - Der in `build_exapp_app` gebaute `Recorder` trägt `env` gleich dem aufgelösten Mapping;
      belegt durch den fünften Fall in `tests/unit/test_exapp_entry.py` und nicht nur durch einen
      grep auf `env=env`.
    - Alle sechs Gates grün, `check_tool_budget.py` weiterhin 15712 Bytes über 21 Werkzeuge.
  </acceptance_criteria>
  <done>Das Log läuft genau dann, wenn ein Administrator es eingeschaltet hat, und die Schaltung selbst steht in der Kette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nextcloud-Admin-Wert zu ExApp | Der Wert kommt über OCS und ist Eingabe; ein unlesbares Ergebnis ist nie ein leeres |
| Anwendungsaufbau zu Transportgrenze | Der Rekorder wird einmal gebaut und je Anfrage nur hinterlegt |

## STRIDE Threat Register

| Threat ID | Category | Component | Schweregrad | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-18-11 | Compliance und Mitbestimmung | Schalter audit_log | hoch | mitigate | Vorgabe im Code ist "aus"; der bekannte 401 beim ersten Start ergibt `{}` und fällt in dieselbe Richtung; ohne Schalter wird die Datei nicht einmal angelegt |
| T-18-18 | Repudiation | Schaltung des Logs | mittel | mitigate | D-15: jede Zustandsänderung schreibt eine Zeile in die Instanzkette. D-16: `actor` ist `unknown`, weil AppAPIs `SetValueListener` die Nutzerangabe für Admin-Formulare verwirft; die Quelle steht im Docstring |
| T-18-19 | Tampering | Admin-Formularfeld | mittel | mitigate | Kein `sensitive: true` in keiner Schreibweise, sonst läse die ExApp einen Klumpen zurück, den sie nicht öffnen kann; ein Test hält Feldkennungen und Lesepfad gleich |
| T-18-05 | Denial of Service | abgeschaltete Ablage | mittel | mitigate | Der Startlauf räumt einmal ab, damit eine abgeschaltete Ablage ihre abgelaufenen Zeilen verliert, obwohl kein Schreibpfad mehr läuft |
| T-18-10 | Information Disclosure | Startlauf | mittel | mitigate | Ein Fehlschlag protokolliert nur den Ausnahmetyp, nie einen Pfad |
| T-18-SC | Tampering | pyproject.toml, uv.lock | mittel | mitigate | Kein Paket, keine neue Abhängigkeit |
</threat_model>

<verification>
- `uv run --no-sync pytest tests/unit tests/contract -q` grün.
- `ruff check .`, `ruff format --check .`, `pyright`, `vulture src scripts vulture_whitelist.py`
  grün.
- `uv run --no-sync python scripts/check_tool_budget.py`: 15712 Bytes, 21 Werkzeuge.
- `grep -rniE "revisionssicher|AI-Act-konform|DSGVO-konform|SIEM-zertifiziert" src/ appinfo/`
  ohne Treffer.
- `git status --short appinfo/ pyproject.toml uv.lock` leer.
</verification>

<success_criteria>
Der Kern liegt hinter einem Schalter, der ab Werk aus steht, Frist und Obergrenze sind
einstellbar, und das Ein- und Ausschalten hinterlässt selbst eine Zeile.
</success_criteria>

<output>
Create `.planning/phases/18-audit-log-kern/18-07-SUMMARY.md` when done
</output>
