---
phase: 19-audit-log-bedienung-und-textnachzug
plan: 08
type: execute
wave: 5
depends_on: ["19-03", "19-07"]
files_modified:
  - appinfo/info.xml
  - README.md
  - README.de.md
  - README.fr.md
  - tests/unit/test_exapp_env_setup.py
autonomous: true
requirements: [AUDIT-06]
must_haves:
  truths:
    - "Der Enterprise-Absatz nennt das Audit-Log nicht mehr als geplant, in allen drei Sprachen und an allen sechs Stellen"
    - "Der Absatz sagt, dass das Protokoll Teil dieser App ist, ab Werk aus, und ueber occ gelesen wird, und dass Gruppen-Policies und SSO weiter geplant sind"
    - "Kein Absatz behauptet ein exklusives Merkmal, das die AGPL nicht halten kann"
    - "Ein Test haelt die drei Manifestsprachen und die drei READMEs an gemeinsamen Markern zusammen"
  artifacts:
    - path: "appinfo/info.xml"
      provides: "Enterprise-Absatz EN, DE, FR, ohne Backtick und ohne Tabelle"
      contains: "audit log"
    - path: "tests/unit/test_exapp_env_setup.py"
      provides: "Markertest ueber sechs Stellen in drei Sprachen"
  key_links:
    - from: "appinfo/info.xml"
      to: "README.md, README.de.md, README.fr.md"
      via: "Markertripel je Sprache, per Test gegeneinander gehalten"
      pattern: "off by default|ab Werk|par defaut"
---

<objective>
Der Enterprise-Absatz sagt die Wahrheit. Heute steht an sechs Stellen in drei Sprachen, das
Audit-Log sei als kommerzielles Add-on geplant; mit Phase 18 ist es gebaut.

Purpose: AUDIT-06 und Erfolgskriterium 5. CONTEXT nennt ausdruecklich nur den Enterprise-Absatz,
der Changelog-Eintrag zu 0.1.10 belegt aber, dass dieser Absatz immer in allen sechs Stellen
gepflegt wurde ("of the store description and of all three READMEs"). Wer nur eine Stelle zieht,
laesst Store-Text und READMEs auseinanderdriften (Pitfall 17).
Output: sechs umgeschriebene Absaetze und ein Test, der sie zusammenhaelt.

Die Version in `appinfo/info.xml` bleibt unangetastet, es wird kein Archiv gebaut, kein Tag
gesetzt und nichts hochgeladen. Der Store liest das Manifest nur beim Upload; bis dahin bleibt
der veroeffentlichte Text woertlich korrekt, und die Aenderung wartet im `[Unreleased]`-Block
(D-v1.5-03, Auslieferung ist EXAPP-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-CONTEXT.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-PATTERNS.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-03-SUMMARY.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-05-SUMMARY.md
</context>

<interfaces>
Die sechs Fundstellen (Zeilen Stand 2026-08-31, beim Lesen pruefen):

| Datei | Zeile | Heutiger Kern |
|-------|-------|---------------|
| `appinfo/info.xml` | 79 | "Audit log, group policies and SSO ... are planned as a commercial add-on" |
| `appinfo/info.xml` | 124 | "Audit-Log, Gruppen-Policies und SSO ... sind als kommerzielles Add-on ... geplant" |
| `appinfo/info.xml` | 171 | "Journal d'audit, politiques de groupe et SSO ... sont prevus comme module commercial" |
| `README.md` | 512-516 | Abschnitt "Enterprise", EN-Original |
| `README.de.md` | 527-531 | derselbe Absatz auf Deutsch |
| `README.fr.md` | 545-549 | derselbe Absatz auf Franzoesisch |

Formregeln:
- In einer `description` des Manifests: ein Absatz je Zeile, kein Backtick, keine Tabelle, kein
  HTML, kein Bild, keine horizontale Linie. Die Instanzansicht sanitisiert auf `h1..h6, strong,
  p, a, ul, ol, li, em, del, blockquote`; ein einzelner Zeilenumbruch erzeugt keinen Umbruch,
  Absaetze brauchen eine Leerzeile. Gate: `tests/unit/test_exapp_env_setup.py:1730-1791`.
- In den READMEs ist Markdown mit mehreren Zeilen erlaubt, der Inhalt bleibt derselbe.
- Echte Umlaute und Accents; im Franzoesischen das schmale Leerzeichen vor Fragezeichen und
  Doppelpunkt wie im Original; keine Em-Dashes; keine Emojis.
- Verbotene Ansprueche: die vier Muster aus Plan 19-03. Das Wort "Audit-Log" beziehungsweise
  "audit log" beziehungsweise "journal d'audit" bleibt ausdruecklich erlaubt.
- Der Changelog-Eintrag zu 0.1.10 behauptet ueber die drei genannten Dinge "exist in this version
  in no form and behind no setting". Das ist ein Release-Eintrag ueber 0.1.10 und damit ein Datum:
  er wird NICHT rueckwirkend geaendert (dieselbe Begruendung, mit der `docs/store-submission.md`
  vom Vokabular-Gate ausgenommen ist).
- Kontaktadresse bleibt admin@infranode.dev.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sechs Absaetze in drei Sprachen</name>
  <files>appinfo/info.xml, README.md, README.de.md, README.fr.md</files>
  <read_first>
    - appinfo/info.xml (Zeilen 1-40 die Rendering-Regeln im Kopfkommentar, 70-90 der EN-Absatz, 115-130 der DE-Absatz, 160-180 der FR-Absatz)
    - README.md (Zeilen 500-536: der Abschnitt Enterprise und seine Nachbarn)
    - README.de.md (Zeilen 515-551) und README.fr.md (Zeilen 533-570)
    - src/mcp_connector/exapp/ui/strings.py (die neue Beschriftung aus Plan 19-02, damit der Enterprise-Absatz nicht mehr verspricht als das Formular)
  </read_first>
  <action>
    Alle sechs Absaetze auf denselben Inhalt bringen, je Sprache in ihrer eigenen Formulierung:

    1. Das Protokoll der Werkzeugaufrufe ist Teil DIESER App, nicht eines Add-ons. Es ist ab Werk
       aus, wird in den Admin-Einstellungen eingeschaltet und ueber ein occ-Kommando gelesen. Die
       Eintraege sind hash-verkettet, und ein zweites occ-Kommando prueft die Kette.
    2. Gruppen-Policies und die Anmeldung ueber den Identitaetsanbieter der Organisation bleiben
       als kommerzielles Add-on geplant. Das Wort "geplant" bezieht sich ab jetzt nur noch auf
       diese zwei Dinge.
    3. Das Angebot zur Unterstuetzung bei Einfuehrung und Betrieb bleibt, samt
       admin@infranode.dev.

    Verboten: jeder der vier Ansprueche aus Plan 19-03; jede Behauptung eines exklusiven
    Merkmals, das die AGPL nicht halten kann (der Code liegt unter AGPL-3.0 offen, das Protokoll
    ist Teil davon; die Enterprise-Positionierung traegt Leistung und nicht Zugang); das Wort
    "archiv"; Em-Dashes; Emojis; im Manifest Backticks und Tabellen.

    Damit ein Test die sechs Stellen zusammenhalten kann, wird je Sprache ein Markertripel
    festgelegt und im SUMMARY notiert. Vorschlag, im Text woertlich zu verwenden:
    EN "audit log", "off by default", "group policies";
    DE "Audit-Log", "ab Werk aus", "Gruppen-Policies";
    FR "journal d'audit", "desactive par defaut" (mit Accents geschrieben), "politiques de groupe".
    Wenn eine Formulierung einen Marker unnatuerlich macht, wird der Marker geaendert und nicht
    der Satz verbogen; die endgueltigen Marker stehen im SUMMARY und werden in Task 2 verwendet.

    Die Version, `image-tag` und jede andere Versionszeichenkette bleiben unangetastet. Der
    Changelog wird in diesem Plan NICHT angefasst (das ist Plan 19-09).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "planned as a commercial add-on" README.md` ergibt 1 und die Zeile nennt nicht mehr das Audit-Log
    - `grep -c "Audit" appinfo/info.xml` ergibt mindestens 4 (drei Absaetze plus der Kommentar zum abwesenden Pfad aus Plan 19-07)
    - `uv run python -c "from lxml import etree; r=etree.parse('appinfo/info.xml').getroot(); print([len(d.text or '') for d in r.findall('description')])"` gibt drei Laengen aus, keine davon 0
    - `uv run python -c "from lxml import etree; r=etree.parse('appinfo/info.xml').getroot(); t=''.join(d.text or '' for d in r.findall('description')); print('\`' in t, '|' in t)"` gibt zweimal False aus
    - `grep -c "admin@infranode.dev" README.md README.de.md README.fr.md appinfo/info.xml` zeigt je Datei mindestens 1
    - `uv run pytest tests/unit/test_exapp_env_setup.py -q` gruen, darunter das Rendering-Gate, das Vokabular-Gate und das Gate aus Plan 19-03
    - `git diff 4baacbd HEAD -- appinfo/info.xml | grep -c "^[-+].*<version>"` ergibt 0
    - `git diff --stat 4baacbd HEAD -- CHANGELOG.md` ist leer
  </acceptance_criteria>
  <done>Sechs Absaetze, drei Sprachen, ein Inhalt: das Protokoll ist Teil der App, geplant sind nur noch Gruppen-Policies und SSO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ein Test haelt die sechs Stellen zusammen</name>
  <files>tests/unit/test_exapp_env_setup.py</files>
  <read_first>
    - tests/unit/test_exapp_env_setup.py (der bestehende Test, der ein Markertripel je Sprache ueber die Manifestbeschreibungen haelt, Muster test_every_description_carries_the_answer_of_the_faq; dazu die Helfer PUBLIC_MARKDOWN und element_text_without_comments)
    - appinfo/info.xml und die drei READMEs (die Ergebnisse von Task 1)
    - .planning/phases/19-audit-log-bedienung-und-textnachzug/19-08-SUMMARY.md ist noch nicht geschrieben; die endgueltigen Marker stehen im Ergebnis von Task 1
  </read_first>
  <behavior>
    - Jede der drei Manifestbeschreibungen traegt alle drei Marker ihrer Sprache
    - Jede der drei READMEs traegt alle drei Marker ihrer Sprache
    - Keine der sechs Stellen nennt das Audit-Log als geplant: das Wort fuer geplant steht in keiner Zeile, die das Wort fuer Audit-Log traegt
    - Der Test ist rot, wenn ein Marker in einer der sechs Stellen fehlt
  </behavior>
  <action>
    Einen Test in der Bauart des bestehenden Markertests anlegen: eine Modulkonstante mit drei
    Eintraegen, je Sprache das Markertripel und die zugehoerige Datei beziehungsweise das
    `lang`-Attribut der Beschreibung. Der Test laeuft ueber alle sechs Stellen und sammelt
    fehlende Marker in einer Liste, damit eine rote Zeile alle Luecken auf einmal nennt, mit
    Datei und Marker.

    Ein zweiter, kleiner Test behauptet die Trennung: in keiner der sechs Stellen steht in einer
    Zeile gleichzeitig das Wort fuer Audit-Log und das Wort fuer geplant der jeweiligen Sprache
    (EN "planned", DE "geplant", FR "prevus" beziehungsweise die Form mit Accent). Der Grund
    gehoert als Kommentar dazu: dieser Satz war mit Phase 18 falsch geworden, und ein Test ist
    der einzige Weg, mit dem eine Textwahrheit die naechste Phase uebersteht.

    Der Manifestweg laeuft ueber die Beschreibungen mit ihrem `lang`-Attribut, nicht ueber
    `element_text_without_comments`: hier ist die Sprache die Unterscheidung.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_exapp_env_setup.py -k "enterprise or marker" -q` gruen und meldet mindestens zwei Tests
    - Ein von Hand entfernter Marker in `README.fr.md` macht den Test rot und die Fehlermeldung nennt Datei und Marker; danach zurueckgenommen und im SUMMARY festgehalten
    - `uv run pytest tests/unit tests/contract -q` gruen
    - `uv run ruff check . && uv run ruff format --check .` still
  </acceptance_criteria>
  <done>Zwei Tests halten sechs Stellen an drei Markertripeln zusammen, samt ausgefuehrter Gegenprobe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifesttext zu Store | apps.nextcloud.com liest die Beschreibung nur beim Upload; eine Aenderung hier erreicht die Store-Seite erst mit dem naechsten Release |
| Oeffentlicher Text zu Codewahrheit | Sechs Stellen sagen dasselbe oder sie driften; ein Leser sieht immer nur eine davon |
| Enterprise-Aussage zu Lizenz | Die App liegt unter AGPL-3.0 offen; ein exklusives Merkmal kann sie nicht versprechen |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-19-31 | Repudiation | high | Sechs Enterprise-Absaetze | mitigate | Alle sechs Stellen gemeinsam gezogen, Markertripel je Sprache per Test gehalten, zweiter Test verbietet die Naehe von Audit-Log und geplant in derselben Zeile (Pitfall 17) |
| T-19-32 | Tampering | medium | `description`-Rendering in der Instanzansicht | mitigate | Kein Backtick, keine Tabelle, kein HTML, Absaetze durch Leerzeile; geprueft durch das bestehende Rendering-Gate und durch eine Behauptung ueber die drei Beschreibungstexte |
| T-19-33 | Repudiation | medium | Enterprise-Positionierung gegen AGPL | accept | Der Absatz verspricht Leistung, nicht Zugang: das Protokoll ist Teil der offenen App, geplant bleiben Gruppen-Policies und SSO. Die AGPL-Konsequenz fuer die Positionierung ist Verhandlungssache und steht auf der Fragenliste fuer den ISV-Call (OD-03), nicht in diesem Plan |
| T-19-34 | Tampering | high | Version, Store-Archiv, Tag | mitigate | Keine Versionszeile angefasst, kein Lauf von `scripts/build_store_release.sh`, kein Tag, kein Upload; `git diff` und `git tag --points-at HEAD` als Beleg (D-v1.5-03) |
| T-19-35 | Repudiation | low | Release-Eintraege zu 0.1.10 und 0.1.11 | mitigate | Werden nicht umgeschrieben: ein Release-Eintrag ist ein Datum. `git diff --stat` gegen `CHANGELOG.md` ist in diesem Plan leer |
| T-19-SC | Tampering (Supply Chain) | low | `pyproject.toml`, `uv.lock` | mitigate | Kein Paket: `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer |
</threat_model>

<verification>
1. `uv run pytest tests/unit tests/contract -q` gruen, insbesondere das Rendering-Gate, das
   Vokabular-Gate, das Vier-Woerter-Gate aus Plan 19-03 und die zwei neuen Markertests.
2. `uv run ruff check . && uv run ruff format --check .` still.
3. url-Zahl im Manifest ist weiter 13, Version und `image-tag` unveraendert.
4. `git tag --points-at HEAD` leer, kein Store-Archiv im Baum
   (`git status --short` zeigt keine `.tar.gz`).
5. Nach jedem `gsd-sdk`-Aufruf `.planning/STATE.md` und `.planning/ROADMAP.md` pruefen.
</verification>

<success_criteria>
- Sechs Stellen, drei Sprachen, ein Inhalt, keine Behauptung eines exklusiven Merkmals
- Zwei Tests halten die Stellen zusammen und verbieten die alte Aussage
- Version, Changelog, Tag und Store unberuehrt
</success_criteria>

<output>
Create `.planning/phases/19-audit-log-bedienung-und-textnachzug/19-08-SUMMARY.md` when done.
Die endgueltigen Markertripel je Sprache und den vollen Wortlaut der drei Manifestabsaetze im
SUMMARY zitieren, damit Plan 19-09 den Changelog-Eintrag daraus schreiben kann.
</output>
