---
phase: 19-audit-log-bedienung-und-textnachzug
plan: 09
type: execute
wave: 6
depends_on: ["19-01", "19-02", "19-03", "19-04", "19-05", "19-06", "19-07", "19-08"]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [AUDIT-04, AUDIT-05, AUDIT-06]
must_haves:
  truths:
    - "CHANGELOG.md hat einen neuen [Unreleased]-Block ueber dem Eintrag zu 0.1.11, in der Form von Keep a Changelog 1.1.0"
    - "Der Block nennt das occ-Lesekommando, die neue Beschriftung, den Textnachzug und den Deaktivieren-Aktivieren-Zyklus"
    - "Die Eintraege zu 0.1.10 und 0.1.11 sind unveraendert, weil ein Release-Eintrag ein Datum ist"
    - "Es gibt keinen Tag, kein Store-Archiv und keinen Upload, und keine Versionszeichenkette wurde angefasst"
    - "Die volle Gate-Kette laeuft gruen, und jedes der fuenf Erfolgskriterien der Phase hat eine Nachweiszeile"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[Unreleased]-Block mit Added, Changed und Fixed samt Linkdefinition"
      contains: "## [Unreleased]"
  key_links:
    - from: "CHANGELOG.md"
      to: "appinfo/info.xml"
      via: "Der Block beschreibt die Textaenderung, die der Store erst beim naechsten Upload liest"
      pattern: "## \\[Unreleased\\]"
---

<objective>
Der Abschluss der Phase: der wartende Textrest kommt in einen NEUEN `[Unreleased]`-Block, die
volle Gate-Kette laeuft, und jedes Erfolgskriterium der Phase bekommt eine Nachweiszeile.

Purpose: D-v1.5-03. Alles aus dieser Phase wartet im `[Unreleased]`-Block; die Auslieferung ist
EXAPP-12 und ausdruecklich nicht Teil dieses Meilensteins. `CHANGELOG.md` hat heute KEINEN
solchen Block: die Datei beginnt nach dem Kopf direkt mit dem Eintrag zu 0.1.11 (Zeile 12), weil
Phase 16 den Block dorthin ueberfuehrt hat. Der Block muss also neu angelegt werden, nicht
befuellt (Pitfall 13).
Output: ein Changelog-Block, eine Linkdefinition, ein gruener Gate-Lauf, eine Nachweistabelle.

Kein Tag, kein Store-Upload, kein Release. Milestone-Tags heissen `milestone-v*`, NIE `v*`, weil
`release.yml` auf `v*` triggert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-CONTEXT.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-PATTERNS.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-06-SUMMARY.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-07-SUMMARY.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-08-SUMMARY.md
</context>

<interfaces>
Form der Datei (Zeilen Stand 2026-08-31):
- Zeilen 1-4 SPDX-Kommentar, Zeile 6 der Titel, Zeilen 8-10 der Hinweis auf Keep a Changelog
  1.1.0 und Semantic Versioning, Zeile 12 `## [0.1.11] - 2026-08-28`
- Eintragsform: Ueberschrift mit Version und Datum, ein einleitender Absatz in Prosa, dann
  Abschnitte `### Added` / `### Changed` / `### Fixed`
- Punkte: ein Satz, der sagt, was ein Nutzer merkt, dann die Begruendung; Zeilenlaenge um 100
  Zeichen; Fortsetzungszeilen mit zwei Leerzeichen eingerueckt
- Am Dateiende ein Block mit Linkdefinitionen je Version, Form
  `[0.1.11]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.10...v0.1.11`
- Das Vokabular-Gate erfasst `CHANGELOG.md`: das Wort "archiv" ist verboten, ebenso die vier
  Ansprueche aus Plan 19-03
- Die fuenf Erfolgskriterien der Phase stehen in `.planning/ROADMAP.md:219-225`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Den [Unreleased]-Block neu anlegen</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (Zeilen 1-60: Kopf, der Eintrag zu 0.1.11 und die Form seiner Punkte; dazu die letzten 15 Zeilen mit den Linkdefinitionen)
    - .planning/phases/19-audit-log-bedienung-und-textnachzug/19-06-SUMMARY.md (Kommandoname und Optionen)
    - .planning/phases/19-audit-log-bedienung-und-textnachzug/19-07-SUMMARY.md (Registrierung, Variablen, der Deaktivieren-Aktivieren-Zyklus)
    - .planning/phases/19-audit-log-bedienung-und-textnachzug/19-08-SUMMARY.md (Wortlaut der Enterprise-Absaetze)
    - .planning/phases/19-audit-log-bedienung-und-textnachzug/19-05-SUMMARY.md (die drei Loeschwege in der Nutzerdokumentation)
  </read_first>
  <action>
    Einen Block `## [Unreleased]` zwischen den Kopf (heute Zeile 11) und `## [0.1.11]` (heute
    Zeile 12) setzen, mit einem einleitenden Absatz in Prosa und drei Abschnitten:

    `### Added`
    - Das occ-Kommando `mcp_connector:audit:read`: was ein Administrator damit tut, welche
      Optionen es hat (`--user`, `--since`, `--limit`, `--json`), dass die Ausgabe ohne Option
      gedeckelt ist, und dass fuer diesen Weg keine Route im Manifest deklariert wird.
    - Die drei Umgebungsvariablen `NC_MCP_AUDIT_LOG`, `NC_MCP_AUDIT_RETENTION_DAYS` und
      `NC_MCP_AUDIT_MAX_BYTES` sind jetzt im Manifest deklariert, mit dem Hinweis, dass das
      Admin-Formular der Hauptweg bleibt.
    - Der Pruefschritt fuer `audit.sqlite3` in `docs/uninstall.md`.

    `### Changed`
    - Die Beschriftung des Audit-Schalters in den Admin-Einstellungen: sie nennt jetzt auch die
      Parameternamen, den Ablehnungsgrund und die Dauer, was das Protokoll nicht leistet, dass
      ein nutzerbezogenes Protokoll mitbestimmungsrelevant sein kann, und wie lange aufbewahrt
      wird.
    - `docs/privacy.md`, `docs/uninstall.md` und `docs/faq.md`: zwei Datenbanken statt einer, der
      Purge leert die sieben Tabellen des OAuth-Speichers und nicht das Protokoll, und alle drei
      automatischen Loeschwege sind benannt (180 Tage Vorgabe, 100 MB Obergrenze, Loeschung des
      Kontos in Nextcloud).
    - Der Enterprise-Absatz der Store-Beschreibung und aller drei READMEs nennt das Protokoll
      nicht mehr als geplant; geplant bleiben Gruppen-Policies und SSO.
    - Der SATZ, der in diesen Block gehoert, weil ihn sonst niemand liest: Kommando und
      Beschriftung erreichen eine bestehende Installation erst nach einem Deaktivieren und
      Aktivieren der App, weil beide Registrierungen im Aktivierungszweig laufen.

    `### Fixed`
    - Die drei Namensreiniger sind zu einer Regel zusammengezogen; ein Name mit einem
      Formatzeichen wie U+202E kann in einer Ausgabezeile nicht mehr die Leserichtung drehen.
      Formulierung ohne das Zeichen selbst im Text.
    - Ein `content-length` mit einer Unicode-Ziffer fuehrt nicht mehr zu einem Serverfehler.

    Am Dateiende eine Linkdefinition in der Form der bestehenden:
    `[Unreleased]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.11...HEAD`,
    als erste Zeile des Definitionsblocks, ueber `[0.1.11]`.

    Verboten: die Eintraege zu 0.1.10 und 0.1.11 anfassen (ein Release-Eintrag ist ein Datum);
    eine Versionsnummer 0.1.12 irgendwo behaupten; das Wort "archiv"; die vier Ansprueche aus
    Plan 19-03; Em-Dashes; Emojis.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## \[Unreleased\]" CHANGELOG.md` ergibt 1
    - `grep -n "^## " CHANGELOG.md | head -2` zeigt `[Unreleased]` vor `[0.1.11]`
    - `grep -c "^\[Unreleased\]:" CHANGELOG.md` ergibt 1 und die Zeile endet auf `v0.1.11...HEAD`
    - `git diff 4baacbd HEAD -- CHANGELOG.md | grep -c "^-"` ergibt hoechstens 1 (allein die Kontextzeile des Definitionsblocks darf sich verschieben; kein Satz eines Release-Eintrags wurde entfernt)
    - `grep -c "0.1.12" CHANGELOG.md` ergibt 0
    - `grep -ci "archiv" CHANGELOG.md` ergibt 0
    - `grep -c "disable and enable" CHANGELOG.md` ergibt mindestens 1
    - `uv run pytest tests/unit/test_exapp_env_setup.py -k "vocabulary or forbidden_claim" -q` gruen
  </acceptance_criteria>
  <done>Ein neuer Block ueber 0.1.11, drei Abschnitte, eine Linkdefinition, kein Release-Eintrag angefasst.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-Kette, Lieferverbote und Nachweistabelle je Erfolgskriterium</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (Zeilen 214-227: die fuenf Erfolgskriterien der Phase im Wortlaut)
    - .github/workflows/ci.yml (Zeilen 1-50: die Gate-Kette in ihrer Reihenfolge)
    - .planning/phases/18-audit-log-kern/18-SECURITY.md (Zeilen 60-95: die Form der Belegzeilen und T-18-SC als Vorlage fuer den Lieferkettennachweis)
  </read_first>
  <action>
    Kein weiterer Dateiinhalt, sondern Messen und Belegen. Die volle Gate-Kette in dieser
    Reihenfolge ausfuehren und die Ausgaben im SUMMARY festhalten:
    `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`,
    `uv run vulture src scripts vulture_whitelist.py`,
    `uv run pytest tests/unit tests/contract -q`,
    `uv run python scripts/check_tool_budget.py`,
    `uv run pytest -m matrix`.

    Danach die Lieferverbote belegen, jede Zeile mit ihrem Kommando und ihrer Ausgabe:
    - `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer (T-19-SC)
    - `git diff 4baacbd HEAD -- appinfo/info.xml | grep "<version>"` ist leer, ebenso fuer
      `image-tag`
    - `git tag --points-at HEAD` ist leer, und `git tag --list "v0.1.12"` ist leer
    - `git status --short` zeigt kein `.tar.gz` und kein Store-Archiv
    - Die url-Zahl im Manifest ist 13
    - `uv run python scripts/check_tool_budget.py` meldet 21 Werkzeuge und 15712 Bytes

    Dann eine Nachweistabelle je Erfolgskriterium der Phase (fuenf Zeilen, Wortlaut aus
    ROADMAP.md), mit den Spalten Kriterium, Belegstelle und Urteil. Erlaubte Urteile: `gemessen`
    oder `hergeleitet`. Alles, was ohne laufende Test-Nextcloud nicht gemessen werden kann, wird
    ausdruecklich als `hergeleitet` gefuehrt und nicht als geprueft weitergeschrieben; die
    Praezedenz dafuer ist R-18-05 aus Phase 18. Mindestens diese Punkte sind hergeleitet: das
    Erscheinen des Kommandos in `occ list` nach dem Deaktivieren-Aktivieren-Zyklus, das
    Ausbleiben einer Namenskollision mit globalen occ-Optionen (Assumption A2 der Recherche), und
    die Sichtbarkeit der neuen Beschriftung in den Admin-Einstellungen.

    Zuletzt die Owner-Schrittliste fuer den echten Lauf aufschreiben, damit sie ausfuehrbar ist,
    ohne diesen Plan zu lesen: App deaktivieren und aktivieren, `occ list | grep mcp_connector`,
    `occ list --help`, das Lesekommando ohne Option, mit `--user`, mit `--limit`, mit `--json`,
    und `occ mcp_connector:audit:verify` als Gegenprobe, dass das aeltere Kommando unveraendert
    antwortet.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src scripts vulture_whitelist.py && uv run pytest tests/unit tests/contract -q && uv run python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - Jedes der sieben Gate-Kommandos ist mit seiner Ausgabe im SUMMARY zitiert und keines ist rot
    - `uv run python scripts/check_tool_budget.py` meldet 21 Werkzeuge und 15712 Bytes, byte-gleich zum Stand vor der Phase
    - `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer
    - `git tag --points-at HEAD` ist leer
    - Die Nachweistabelle hat fuenf Zeilen, je Zeile eine Belegstelle mit Datei und Zeile oder ein Kommando mit Ausgabe, und jedes Urteil ist `gemessen` oder `hergeleitet`
    - Die Owner-Schrittliste steht im SUMMARY und nennt mindestens acht Kommandos
  </acceptance_criteria>
  <done>Sieben Gates gruen, sechs Lieferverbote belegt, fuenf Kriterien mit Nachweis und ehrlichem Urteil, eine ausfuehrbare Owner-Schrittliste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo zu Auslieferung | Ein Tag auf `v*` loest `release.yml` aus; diese Phase liefert ausdruecklich nichts aus |
| Changelog zu Historie | Ein Release-Eintrag ist ein Datum; wer ihn umschreibt, faelscht eine Aussage ueber eine veroeffentlichte Fassung |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-19-36 | Tampering | critical | Tag, `release.yml`, Store-Upload | mitigate | Kein Tag, kein Archiv, kein Upload; `git tag --points-at HEAD` und `git tag --list "v0.1.12"` als Beleg, `git status --short` ohne `.tar.gz`. Milestone-Tags heissen `milestone-v*`, nie `v*` (D-v1.5-03) |
| T-19-37 | Repudiation | high | Eintraege zu 0.1.10 und 0.1.11 | mitigate | Werden nicht angefasst; `git diff` gegen `CHANGELOG.md` darf keine entfernte Satzzeile eines Release-Eintrags zeigen |
| T-19-38 | Repudiation | high | Nachweistabelle | mitigate | Jedes Urteil ist `gemessen` oder `hergeleitet`; ohne laufende Test-Nextcloud ist der occ-Lauf hergeleitet und wird so ausgewiesen (Praezedenz R-18-05), damit die naechste Phase ihn nicht als geprueft weiterschreibt |
| T-19-39 | Information Disclosure | low | Changelog-Text | mitigate | Kein Kontoname, kein Pfad, kein Parameterwert und kein Steuerzeichen im Text; das Formatzeichen U+202E wird benannt und nicht eingesetzt |
| T-19-40 | Tampering | medium | Werkzeugoberflaeche | mitigate | `scripts/check_tool_budget.py` meldet unveraendert 21 Werkzeuge und 15712 Bytes; kein Grenzwert wurde angehoben |
| T-19-SC | Tampering (Supply Chain) | low | `pyproject.toml`, `uv.lock` | mitigate | Die ganze Phase installiert kein Paket: `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer (Praezedenz T-18-SC) |
</threat_model>

<verification>
1. Die sieben Gate-Kommandos laufen gruen und stehen mit Ausgabe im SUMMARY.
2. Die sechs Lieferverbote sind mit Kommando und Ausgabe belegt.
3. Die Nachweistabelle deckt alle fuenf Erfolgskriterien der Phase ab, jedes mit Urteil.
4. Alle drei Anforderungen der Phase sind adressiert: AUDIT-04 durch die Plaene 19-01, 19-04,
   19-06 und 19-07, AUDIT-05 durch 19-02, AUDIT-06 durch 19-03, 19-05, 19-08 und diesen Plan.
5. Nach jedem `gsd-sdk`-Aufruf `.planning/STATE.md` und `.planning/ROADMAP.md` pruefen
   (Prozentwert, `last_activity`, Tabellentrenner). Phase 18 hat dort Korruptionen erlebt; eine
   entdeckte Korruption wird im SUMMARY genannt und von Hand korrigiert.
</verification>

<success_criteria>
- Ein neuer `[Unreleased]`-Block mit Added, Changed und Fixed samt Linkdefinition
- Sieben Gates gruen, Werkzeugoberflaeche byte-gleich, keine Abhaengigkeit
- Kein Tag, kein Archiv, kein Upload, keine Versionszeichenkette angefasst
- Fuenf Nachweiszeilen mit ehrlichem Urteil und eine ausfuehrbare Owner-Schrittliste
</success_criteria>

<output>
Create `.planning/phases/19-audit-log-bedienung-und-textnachzug/19-09-SUMMARY.md` when done.
Das SUMMARY traegt die Nachweistabelle, die sechs Lieferverbote mit Ausgabe und die
Owner-Schrittliste; es ist die Vorlage fuer die Phasenverifikation.
</output>
