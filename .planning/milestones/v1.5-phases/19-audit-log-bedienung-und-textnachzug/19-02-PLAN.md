---
phase: 19-audit-log-bedienung-und-textnachzug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/exapp/ui/strings.py
  - tests/unit/test_exapp_admin_settings.py
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "Ein Administrator liest im Admin-Formular, was ein Eintrag enthaelt, einschliesslich Parameternamen, Ablehnungsgrund und Dauer"
    - "Ein Administrator liest, was nie in einem Eintrag steht: Parameterwert, Ergebnisinhalt, Adresse, Fehlermeldung"
    - "Ein Administrator liest, was das Protokoll nicht leistet, inhaltlich gleich mit dem Grenzsatz des Pruefkommandos"
    - "Ein Administrator liest, dass ein nutzerbezogenes Protokoll mitbestimmungsrelevant sein kann"
    - "Ein Administrator liest, wie lange aufbewahrt wird und dass das Log Purge und Deinstallation uebersteht"
    - "Das Formular bietet keine Stufe an: das Wort full kommt in keinem Feldtext und in keinem Feldtyp vor"
  artifacts:
    - path: "src/mcp_connector/exapp/ui/strings.py"
      provides: "ADMIN_FIELD_AUDIT_LOG_LABEL und ADMIN_FIELD_AUDIT_LOG_DESCRIPTION in der langen Fassung"
      contains: "ADMIN_FIELD_AUDIT_LOG_DESCRIPTION"
    - path: "tests/unit/test_exapp_admin_settings.py"
      provides: "Zusagen an die neue Beschriftung, sechs Pflichten und die Verbote"
  key_links:
    - from: "src/mcp_connector/exapp/ui/strings.py"
      to: "src/mcp_connector/exapp/audit_verify.py"
      via: "Grenzsatz inhaltlich gleich mit LIMIT_SENTENCE, per Test gegeneinander gehalten"
      pattern: "changed or removed unnoticed"
    - from: "src/mcp_connector/exapp/admin_settings.py"
      to: "src/mcp_connector/exapp/ui/strings.py"
      via: "form_scheme liest die Beschriftung aus dem Katalog"
      pattern: "ADMIN_FIELD_AUDIT_LOG_DESCRIPTION"
---

<objective>
Die lange Fassung der Beschriftung des Audit-Schalters, die Phase 18 ausdruecklich dieser Phase
ueberlassen hat (`strings.py:640-654` begruendet die Vertagung im Code).

Purpose: AUDIT-05 und die Entscheide D-v1.5-02 (Grenzbeschreibung ist Pflichtbestandteil) und
D-v1.5-04 (Mitbestimmungshinweis) werden hier eingeloest, samt dem Info-Befund IN-06 aus dem
Phase-18-Review: die heutige Beschreibung verschweigt, dass auch Parameternamen,
Ablehnungsgrund und Dauer gespeichert werden.
Output: zwei ersetzte Konstanten in `exapp/ui/strings.py`, erweiterte Zusagetests.

Das Formular selbst wird NICHT angefasst: `admin_settings.form_scheme()` baut seine sieben
Felder unveraendert, `audit_log` bleibt Checkbox mit `"default": False` und ohne `sensitive`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-CONTEXT.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-PATTERNS.md
</context>

<interfaces>
Feste Bezugswerte fuer den Text (am Code gemessen, 2026-08-31):

- Aufbewahrungsfrist: `audit/store.py:93` `RETENTION_DAYS = 180` (Vorgabe, per
  `NC_MCP_AUDIT_RETENTION_DAYS` erhoehbar) -> im Text als "180 days by default", nie als "always".
- Obergrenze: `audit/store.py:98` `SIZE_LIMIT_BYTES = 100_000_000` -> "100 MB".
- Kontoloeschung: `audit/store.py:111` `USER_SILENCE_DAYS = 30`, Ablauf `store.drop_user_chain`.
- Grenzsatz des Pruefkommandos: `exapp/audit_verify.py:117-121` `LIMIT_SENTENCE`, tragende
  Begriffe "changed or removed unnoticed" und "recompute".
- Die 17 Felder einer Zeile: `audit/store.py:220-286`; die drei bisher verschwiegenen sind
  `reason` ("a fixed identifier of a refusal, never a message of an error", `:243-244`),
  `duration_ms` und `params` ("A sorted JSON list of parameter names, never a value", `:246`).
- Aktivierungssatz, wortgleich zu den anderen sechs Feldern: "A change takes effect after you
  disable and enable this app again."
- Formvorlage fuer die lange Fassung: `strings.py:627-638` (`ADMIN_FIELD_TALK_SEND_DESCRIPTION`)
  und `:595-609` (`ADMIN_FIELD_CIMD_DESCRIPTION`).
- Beide Konstantennamen stehen bereits in `__all__` (`strings.py:41-42`); eine dritte neue
  Konstante muesste dort alphabetisch nachgetragen werden (vulture-Gate).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Die lange Fassung der Beschriftung schreiben</name>
  <files>src/mcp_connector/exapp/ui/strings.py</files>
  <read_first>
    - src/mcp_connector/exapp/ui/strings.py (Zeilen 1-30 Katalogregel und __all__, Zeilen 590-660 die drei langen Beschreibungen und die heutige Audit-Fassung samt Vertagungsvermerk)
    - src/mcp_connector/exapp/audit_verify.py (Zeilen 105-125: CHAIN_LIMIT, NO_BREAK, LIMIT_SENTENCE im Wortlaut)
    - src/mcp_connector/audit/store.py (Zeilen 84-120 die vier Grenzwerte, Zeilen 220-286 das kommentierte Schema mit den 17 Feldern)
    - src/mcp_connector/exapp/admin_settings.py (Zeilen 100-200: wie das Feld gebaut wird, damit klar ist, was NICHT geaendert wird)
  </read_first>
  <action>
    `ADMIN_FIELD_AUDIT_LOG_LABEL` bleibt inhaltlich "Keep a record of tool calls"; nur anpassen,
    wenn der Text sonst irrefuehrend wird.

    `ADMIN_FIELD_AUDIT_LOG_DESCRIPTION` wird ersetzt. Sechs Pflichten, jede davon im Text
    nachweisbar, Englisch, implizit verkettete Stringliterale in Klammern, keine Aufzaehlung mit
    Bindestrichen (das Formular rendert Fliesstext), keine Em-Dashes, keine Emojis:

    1. Was eine Zeile enthaelt: Konto, Werkzeugname, Zeit, aufrufende App, Ergebnisstatus, UND
       die drei bisher verschwiegenen: die Namen der uebergebenen Parameter, der Grund einer
       Ablehnung als feste Kennung, und die Dauer des Aufrufs. Die Wendung "the names of the
       parameters, never their values" ist Pflicht.
    2. Was nie enthalten ist: "no parameter value", "no part of a result", keine Adresse, kein
       User-Agent, kein Text einer Fehlermeldung. Die zwei Wendungen "no parameter value" und
       "no part of a result" bleiben wortgleich zur heutigen Fassung, weil ein bestehender Test
       auf sie prueft.
    3. Die Grenzbeschreibung, inhaltlich gleich mit `audit_verify.LIMIT_SENTENCE`: ein Eintrag
       zeigt, dass etwas unbemerkt geaendert oder entfernt wurde, und er zeigt nicht, wer die
       Datei schreiben kann, weil wer sie schreiben kann die Kette neu rechnet. Die tragenden
       Begriffe "changed or removed unnoticed" und "recompute" muessen wortgleich vorkommen,
       damit ein Test die beiden Saetze gegeneinander halten kann.
    4. Der Mitbestimmungssatz (D-v1.5-04): ein Protokoll, das Aufrufe einzelnen Konten
       zuordnet, kann in Deutschland und in Oesterreich der Mitbestimmung des Betriebsrats
       unterliegen; wer es einschaltet, klaert das vorher. Kein Rechtsrat, ein Hinweis. Die
       Wendung "works council" ist Pflicht, weil der Test darauf prueft.
    5. Aufbewahrung und Fortbestand: 180 Tage als Vorgabe, Obergrenze 100 MB bei der die
       aeltesten Eintraege weichen, und dass ein Eintrag `occ mcp_connector:purge` und die
       Deinstallation der App uebersteht, waehrend das Entfernen des Datenvolumes ihn mitnimmt.
       Formulierung ohne "the only automatic deletion": es gibt drei automatische Loeschwege.
    6. Der Aktivierungssatz wortgleich: "A change takes effect after you disable and enable
       this app again."

    Verboten im Text: die vier Woerter revisionssicher, AI-Act-konform, DSGVO-konform,
    SIEM-zertifiziert und jede Entsprechung davon in EN oder FR; das Wort "full"; jede Andeutung
    einer waehlbaren Stufe; das Wort "archiv" in jeder Schreibweise (bestehendes Vokabular-Gate);
    Backticks (das Formular rendert keinen Code).

    Der `#:`-Kommentar ueber der Konstante ersetzt den heutigen Vertagungsvermerk: er zaehlt die
    sechs Pflichten auf, nennt AUDIT-05, D-v1.5-02, D-v1.5-04 und den Review-Befund IN-06, und
    haelt fest, dass die Grenzbeschreibung inhaltlich an `audit_verify.LIMIT_SENTENCE` gebunden
    ist und ein Test das haelt.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_admin_settings.py -q && uv run ruff check src/mcp_connector/exapp/ui/strings.py && uv run ruff format --check src/mcp_connector/exapp/ui/strings.py</automated>
  </verify>
  <acceptance_criteria>
    - Die bestehenden Tests in `tests/unit/test_exapp_admin_settings.py` bleiben gruen (sie pruefen "no parameter value", "no part of a result", "disable and enable this app again" und die vier Verbotswoerter)
    - `grep -c "works council" src/mcp_connector/exapp/ui/strings.py` ergibt mindestens 1
    - `grep -c "changed or removed unnoticed" src/mcp_connector/exapp/ui/strings.py` ergibt mindestens 1
    - `grep -c "recompute" src/mcp_connector/exapp/ui/strings.py` ergibt mindestens 1
    - `grep -c "180 days" src/mcp_connector/exapp/ui/strings.py` ergibt mindestens 1 und `grep -c "100 MB" src/mcp_connector/exapp/ui/strings.py` mindestens 1
    - `grep -ci "full" src/mcp_connector/exapp/ui/strings.py` zeigt keinen Treffer innerhalb von `ADMIN_FIELD_AUDIT_LOG_DESCRIPTION` (Treffer in anderen Konstanten sind erlaubt und werden im SUMMARY benannt)
    - `grep -c '`' src/mcp_connector/exapp/ui/strings.py` zeigt keinen Backtick innerhalb der beiden geaenderten Konstanten
    - `uv run vulture src scripts vulture_whitelist.py` still, `uv run pyright` ohne neue Meldung
  </acceptance_criteria>
  <done>Die Beschreibung traegt alle sechs Pflichten, kein verbotenes Wort, keine Stufe; das Formular selbst ist unveraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zusagen an die Beschriftung als Tests</name>
  <files>tests/unit/test_exapp_admin_settings.py</files>
  <read_first>
    - tests/unit/test_exapp_admin_settings.py (Zeilen 280-360: der Nachbartest mit default/checkbox/sensitive und der heutige Beschreibungstest samt Docstring, der die lange Fassung Phase 19 zuschreibt)
    - src/mcp_connector/exapp/ui/strings.py (das Ergebnis von Task 1)
    - src/mcp_connector/exapp/audit_verify.py (Zeilen 117-121: LIMIT_SENTENCE als Bezug)
  </read_first>
  <behavior>
    - Die Beschreibung des Feldes `audit_log` im gesendeten `formScheme` nennt Parameternamen, Ablehnungsgrund und Dauer
    - Sie enthaelt weiterhin "no parameter value", "no part of a result" und "disable and enable this app again"
    - Sie enthaelt "works council"
    - Sie enthaelt die beiden tragenden Begriffe des Grenzsatzes, und dieselben Begriffe kommen in `audit_verify.LIMIT_SENTENCE` vor
    - Sie nennt 180 Tage und 100 MB und sagt, dass ein Eintrag den Purge uebersteht
    - Sie enthaelt keines der vier Verbotswoerter und keine EN- oder FR-Entsprechung
    - Weder ein Feldtext noch ein Feldtyp des ganzen Formulars enthaelt das Wort "full"
    - Das Feld bleibt Checkbox mit `default is False` und ohne `sensitive` in jeder Schreibweise
  </behavior>
  <action>
    Den bestehenden Test `test_the_audit_log_description_says_what_is_kept_and_what_is_not`
    erweitern statt ersetzen: die bestehenden vier Behauptungen bleiben, dazu kommen die
    Pflichten aus `<behavior>`. Sein Docstring wird angepasst, weil er heute sagt, die lange
    Fassung gehoere Phase 19; jetzt ist sie da.

    Zwei neue Tests daneben, mit `respx` und dem Aufbau der Nachbartests:
    - einer, der die tragenden Begriffe des Grenzsatzes in BEIDEN Zeichenketten behauptet
      (Beschreibung und `audit_verify.LIMIT_SENTENCE`), damit ein spaeteres Auseinanderdriften
      auffaellt (Pitfall 12 der Recherche);
    - einer, der ueber ALLE sieben Felder des gesendeten `formScheme` laeuft und behauptet, dass
      `"full"` in keinem `description`, keinem `title`, keinem `type` und keiner `options`-Liste
      vorkommt (Erfolgskriterium 3, Pitfall 11). Die Pruefung laeuft ueber
      `json.dumps(field).lower()`, in der Form des bestehenden `sensitive`-Tests, und
      beruecksichtigt, dass "full" als Teilwort in einem anderen Feldtext auftauchen koennte:
      geprueft wird als Wortform mit Wortgrenzen, nicht als nackter Substring.

    Der Nachbartest mit `default is False`, `type == "checkbox"` und
    `"sensitive" not in json.dumps(field).lower()` bleibt unveraendert.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_admin_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_exapp_admin_settings.py -q` gruen, mit mindestens zwei Tests mehr als vorher
    - Ein absichtlicher Rueckbau des Wortes "works council" in `strings.py` macht den erweiterten Test rot (Gegenprobe einmal von Hand ausgefuehrt und im SUMMARY genannt, danach zurueckgenommen)
    - `grep -c "LIMIT_SENTENCE" tests/unit/test_exapp_admin_settings.py` ergibt mindestens 1
    - `uv run pytest tests/unit tests/contract -q` gruen
  </acceptance_criteria>
  <done>Jede der sechs Pflichten und jedes der drei Verbote ist als Behauptung im Test hinterlegt, samt einmal ausgefuehrter Gegenprobe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nextcloud-Admin zu Datenerhebung | Der Schalter startet eine nutzerbezogene Protokollierung; die Beschriftung ist die einzige Stelle, an der ein Administrator vorher erfaehrt, was er einschaltet |
| Oeffentlicher Text zu Codewahrheit | Die Beschreibung ist eine Zusage; sie kann mehr behaupten als der Code haelt |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-19-04 | Repudiation | high | `ADMIN_FIELD_AUDIT_LOG_DESCRIPTION` | mitigate | Sechs Pflichten, davon drei (Parameternamen, Ablehnungsgrund, Dauer) als Einloesung des Review-Befunds IN-06; jede Pflicht als Testbehauptung; die vier Verbotswoerter bleiben per bestehendem Test draussen |
| T-19-05 | Information Disclosure | medium | Formularfeld `audit_log` | mitigate | Keine Stufe, kein Auswahlfeld, `full` kommt in keinem Feldtext und keinem Feldtyp vor (neuer Test ueber alle sieben Felder); `sensitive` bleibt in jeder Schreibweise abwesend (bestehender Test) |
| T-19-06 | Repudiation | medium | Grenzsatz gegen `audit_verify.LIMIT_SENTENCE` | mitigate | Test haelt beide Saetze an ihren tragenden Begriffen gegeneinander; zwei Orte sagen nicht unterschiedlich viel ueber dieselbe Grenze (Pitfall 12) |
| T-19-07 | Compliance und Mitbestimmung | high | Neue Datenerhebung bei bestehenden Installationen | accept | Ab Werk aus bleibt unveraendert (`"default": False`, Formular nicht angefasst); die Beschriftung nennt die Mitbestimmungsrelevanz. Die rechtliche Bewertung selbst kann eine App nicht leisten, deshalb Hinweis statt Zusage (D-v1.5-04) |
| T-19-SC | Tampering (Supply Chain) | low | `pyproject.toml`, `uv.lock` | mitigate | Kein Paket: `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer |
</threat_model>

<verification>
1. `uv run pytest tests/unit tests/contract -q` gruen.
2. `uv run ruff check . && uv run ruff format --check .` still, `uv run pyright` ohne neue Meldung,
   `uv run vulture src scripts vulture_whitelist.py` still.
3. `uv run python scripts/check_tool_budget.py`: 21 Werkzeuge, 15712 Bytes, unveraendert.
4. `git diff --stat 4baacbd HEAD -- appinfo/info.xml pyproject.toml uv.lock` leer: dieser Plan
   fasst weder Manifest noch Version noch Abhaengigkeiten an.
5. Hergeleitet und nicht gemessen (ausdruecklich so zu kennzeichnen): eine bestehende
   Installation sieht die neue Beschriftung erst nach einem Deaktivieren-Aktivieren-Zyklus, weil
   `register_admin_form` im `enabled=1`-Zweig laeuft. Keine Test-Nextcloud verfuegbar.
6. Nach jedem `gsd-sdk`-Aufruf `.planning/STATE.md` und `.planning/ROADMAP.md` auf Prozentwert,
   `last_activity` und Tabellentrenner pruefen.
</verification>

<success_criteria>
- Die Beschreibung des Audit-Schalters traegt Leistung, Grenze, Mitbestimmung, Aufbewahrung, Fortbestand und Aktivierungszyklus
- Kein verbotenes Wort, kein `full`, keine Stufe, kein `sensitive`
- Formular, Feldtyp und Vorgabewert unveraendert
</success_criteria>

<output>
Create `.planning/phases/19-audit-log-bedienung-und-textnachzug/19-02-SUMMARY.md` when done.
Im SUMMARY den vollen Wortlaut der neuen Beschreibung zitieren, damit Plan 19-05 und Plan 19-09
daraus zitieren koennen, ohne die Datei erneut zu lesen.
</output>
