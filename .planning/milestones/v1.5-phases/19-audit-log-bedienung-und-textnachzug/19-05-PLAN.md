---
phase: 19-audit-log-bedienung-und-textnachzug
plan: 05
type: execute
wave: 2
depends_on: ["19-03"]
files_modified:
  - docs/privacy.md
  - docs/uninstall.md
  - docs/faq.md
  - tests/unit/test_docs_audit_truth.py
autonomous: true
requirements: [AUDIT-06]
must_haves:
  truths:
    - "docs/privacy.md nennt zwei SQLite-Dateien und beschreibt audit.sqlite3 mit seinen personenbezogenen Feldern"
    - "docs/privacy.md sagt, dass der Purge das Audit-Log nicht leert, und nennt alle drei automatischen Loeschwege mit ihren Zahlen"
    - "docs/uninstall.md nennt das Audit-Log namentlich, hat einen Pruefschritt fuer audit.sqlite3 und sagt, dass --rm-data das Log mitnimmt"
    - "docs/faq.md antwortet auf die completely-Frage so, dass die Antwort am Code stimmt"
    - "Kein Text behauptet, die Aufbewahrungsfrist sei der einzige automatische Loescher"
  artifacts:
    - path: "tests/unit/test_docs_audit_truth.py"
      provides: "Bindung der neuen Doku-Saetze an die Codekonstanten RETENTION_DAYS, SIZE_LIMIT_BYTES, USER_SILENCE_DAYS"
      min_lines: 70
  key_links:
    - from: "docs/privacy.md"
      to: "src/mcp_connector/audit/store.py"
      via: "180 Tage, 100 MB und 30 Tage stehen im Text und als Konstante, im selben Test gegeneinander gehalten"
      pattern: "RETENTION_DAYS|SIZE_LIMIT_BYTES|USER_SILENCE_DAYS"
    - from: "docs/uninstall.md"
      to: "src/mcp_connector/audit/store.py"
      via: "AUDIT_FILENAME steht im Runbook und als Konstante"
      pattern: "audit.sqlite3"
---

<objective>
Die Nutzerdokumentation sagt die Wahrheit ueber Speicherung, Purge und Deinstallation.

Purpose: AUDIT-06 und D-v1.5-01. Heute behauptet `docs/privacy.md` eine Datenbank statt zweier,
"empties every table of its database" und "There is no long lived store of personal data";
`docs/uninstall.md` heisst "Removing this app, and proving that nothing is left" und nennt das
Audit-Log kein einziges Mal (`grep -ri audit docs/uninstall.md` ohne Treffer, der messbare
Ausgangszustand von Restpunkt R-18-04). Mit Phase 18 ist all das falsch.
Output: drei umgeschriebene Dokumente und ein Test, der die neuen Saetze an Codekonstanten
bindet, damit die neue Wahrheit die naechste Phase uebersteht.

KORREKTUR gegenueber dem Wortlaut von D-v1.5-01, ROADMAP-Erfolgskriterium 4 und CONTEXT: der
Code kennt DREI automatische Loeschwege, nicht einen. Die verengte Formulierung "die
Aufbewahrungsfrist ist der einzige automatische Loescher" wird NICHT uebernommen, weil sie am
Code falsch waere. Wie viele automatische Loescher es gibt, ist eine Messung und keine
Entscheidung; die Entscheidung D-v1.5-01 (das Log ueberlebt Purge und Deinstallation, der Text
wird ausdruecklich umgeschrieben) bleibt unberuehrt. Der Verifizierer soll das nicht als
Abweichung lesen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-CONTEXT.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-PATTERNS.md
@.planning/phases/19-audit-log-bedienung-und-textnachzug/19-03-SUMMARY.md
</context>

<interfaces>
Die drei automatischen Loeschwege, am Code gemessen (Zeilen Stand 2026-08-31):

| Weg | Ausloeser | Wirkung | Belegstelle |
|-----|-----------|---------|-------------|
| Aufbewahrungsfrist | 180 Tage Vorgabe, geprueft je 500. Zeile im Schreibpfad | Das abgelaufene Praefix jeder Nutzerkette geht, ein Grabstein erklaert die Luecke | `audit/store.py:93` `RETENTION_DAYS`, `:632-665`, `config.py:99` |
| Obergrenze | `used_bytes > 100 MB`, im selben Aufraeumlauf | Die aeltesten Zeilen ausserhalb der Instanzkette weichen, ein Grabstein erklaert sie | `audit/store.py:98` `SIZE_LIMIT_BYTES`, `:667-702` |
| Konto existiert nicht mehr | jede 20. Aufraeumrunde, Kette 30 Tage stumm und Konto fehlt in der AppAPI-Nutzerliste | Die ganze Kette faellt, ein Grabstein in der Instanzkette nennt sie | `audit/store.py:111` `USER_SILENCE_DAYS`, `:912-989`, `audit/accounts.py` |

Weitere feste Bezuege:
- `AUDIT_FILENAME = "audit.sqlite3"` (`audit/store.py:84`), liegt im ExApp-Volume neben `oauth.sqlite3`
- Der Purge leert die sieben Tabellen des OAuth-Speichers; das Audit-Log ueberlebt (T-18-22, vier
  Faelle in `tests/unit/test_exapp_purge.py:800-860`)
- `occ app_api:app:unregister mcp_connector --rm-data` entfernt das Volume und damit die Datei;
  dasselbe gilt fuer "Delete data on remove" auf Nextcloud 32/33 (D-18, R-18-04)
- Felder einer Zeile: `nc_user`, `tool`, `client_id`, `auth_id`, `client_name`, `outcome`,
  `reason`, `duration_ms`, `params` (Parameternamen, nie Werte); nie IP, nie User-Agent, nie
  Parameterwert, nie Ergebnisinhalt, nie Fehlermeldungstext
- Verbotene Ansprueche: die vier Muster aus Plan 19-03 (siehe 19-03-SUMMARY.md). Das Wort
  "Audit-Log" beziehungsweise "audit log" bleibt ausdruecklich erlaubt.
- Vokabular-Gate: das Wort "archiv" ist in jedem oeffentlichen Text verboten, auch als Teilwort.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: docs/privacy.md sagt zwei Datenbanken, drei Loescher und einen ueberlebenden Purge</name>
  <files>docs/privacy.md</files>
  <read_first>
    - docs/privacy.md (ganz, 191 Zeilen: Ton, Tabellenform, Purge-Abschnitt ab Zeile 155, Retention ab Zeile 186)
    - src/mcp_connector/audit/store.py (Zeilen 84-135 die vier Grenzwerte samt Kommentaren; Zeilen 220-286 das kommentierte Schema mit allen Feldern)
    - tests/unit/test_oauth_store.py (Zeilen 1485-1512: der bestehende Test, der die Zeile "| Client registrations" woertlich haelt)
  </read_first>
  <action>
    Vier Stellen in `docs/privacy.md`, im Ton der Datei (Aussagesaetze, Tabellen- und
    Spaltennamen in Backticks, keine Werbung, keine Beteuerung, Englisch):

    1. Abschnitt "What the app stores", Einleitung (heute Zeile 29): "one SQLite database" wird
       zu zwei, benannt als `oauth.sqlite3` und `audit.sqlite3`, beide im eigenen Container. Der
       Satz sagt, dass die zweite Datei nur existiert, wenn ein Administrator das Protokoll
       eingeschaltet hat oder es einmal eingeschaltet war.
    2. Die Tabelle "What the app stores" bekommt neue Zeilen fuer `audit.sqlite3`: Kontoname,
       Werkzeugname, Zeit, Client-Id und Clientname, Ergebnisstatus, Ablehnungsgrund als feste
       Kennung, Dauer, und die Namen der uebergebenen Parameter. Je Zeile die Form der
       bestehenden Zeilen: `| Data | Where | Form |` mit Spaltenname in Backticks. Eine
       zusaetzliche Zeile sagt, was NICHT darin steht: keine IP, kein User-Agent, kein
       Parameterwert, kein Teil eines Ergebnisses, kein Text einer Fehlermeldung.
       HARTE RANDBEDINGUNG: die bestehende Zeile, die mit `| Client registrations` beginnt,
       bleibt WOERTLICH stehen, samt "hash" und "never in the clear"; ein bestehender Test
       (`test_the_privacy_doc_describes_the_clients_table_as_it_is`) prueft ihren Anfang und
       diese zwei Wendungen. Neue Zeilen daneben stellen, nichts dazwischen umformulieren.
    3. Purge-Abschnitt (heute Zeilen 168-184): "empties every table of its database" wird zu
       "empties the seven tables of its OAuth database". Ein neuer Absatz sagt, dass das
       Protokoll den Purge ueberlebt, warum das so gewollt ist (ein Protokoll, das sich mit einem
       Kommando entfernen laesst, protokolliert nichts), und dass die Datei danach weiter im
       Volume liegt. Die Wendung "how to verify that nothing is left" wird zu einer Formulierung,
       die sagt, was bleibt und wie man es sieht.
    4. Abschnitt "Retention" (heute Zeilen 186-191): der Satz "There is no long lived store of
       personal data beyond the active connections a user has chosen to keep" wird ersetzt. Der
       neue Text nennt die 180 Tage als Vorgabe (nicht als Immer, weil
       `NC_MCP_AUDIT_RETENTION_DAYS` sie verschieben kann) und dann alle DREI automatischen
       Loeschwege in dieser Rangfolge: abgelaufene Frist, erreichte Obergrenze von 100 MB mit
       Verdraengung der aeltesten Eintraege, und die Loeschung des Kontos in Nextcloud nach 30
       Tagen Stille in dieser Kette. Der Text sagt ausserdem, dass in allen drei Faellen eine
       Markierung stehen bleibt, die die Zahl der fehlenden Eintraege nennt, weshalb die
       Kettenpruefung eine erklaerte Luecke meldet und keinen Bruch.

    Verboten: die Wendung "the only automatic deletion" oder eine Entsprechung; jeder der vier
    Ansprueche aus Plan 19-03; das Wort "archiv" in jeder Schreibweise; Em-Dashes; Emojis.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_store.py tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "audit.sqlite3" docs/privacy.md` ergibt mindestens 2
    - `grep -c "180" docs/privacy.md` ergibt mindestens 1, `grep -c "100 MB" docs/privacy.md` mindestens 1, `grep -c "30 days" docs/privacy.md` mindestens 1
    - `grep -c "empties every table" docs/privacy.md` ergibt 0
    - `grep -c "There is no long lived store of personal data" docs/privacy.md` ergibt 0
    - `grep -c "^| Client registrations" docs/privacy.md` ergibt 1 und die Zeile enthaelt weiterhin "hash" und "never in the clear"
    - `grep -ci "the only automatic" docs/privacy.md` ergibt 0
    - `grep -ci "archiv" docs/privacy.md` ergibt 0
    - `uv run pytest tests/unit/test_exapp_env_setup.py -k "vocabulary or forbidden_claim" -q` gruen
  </acceptance_criteria>
  <done>Zwei Datenbanken benannt, das Audit-Log in der Speichertabelle beschrieben, der Purge-Satz korrigiert, drei Loeschwege mit ihren Zahlen im Retention-Abschnitt.</done>
</task>

<task type="auto">
  <name>Task 2: docs/uninstall.md und docs/faq.md nennen das Log und seine Ausnahme</name>
  <files>docs/uninstall.md, docs/faq.md</files>
  <read_first>
    - docs/uninstall.md (ganz, 312 Zeilen: Titel, Versionstabelle Zeile 47, die acht Checks ab Zeile 95, Purge-Feldtabelle ab Zeile 155, Abschnittstitel Zeile 151, Gegenpruefungen ab Zeile 229)
    - docs/faq.md (Zeilen 100-135: der Eintrag zur completely-Frage und die Form der Nachbareintraege)
    - docs/privacy.md (das Ergebnis von Task 1, damit beide Dokumente dasselbe sagen)
  </read_first>
  <action>
    `docs/uninstall.md`, sechs Stellen, in der Messform der Datei (numerierte Shell-Bloecke mit
    echter Ausgabe darunter, dreispaltige Feldtabellen, Englisch):

    1. Titel (Zeile 1) "Removing this app, and proving that nothing is left": umschreiben zu
       einer Formulierung, die beides traegt, das Entfernen und den Nachweis dessen, was bleibt.
    2. Abschnittstitel (Zeile 151) "What the occ way leaves behind: nothing": nennt jetzt, was
       der occ-Weg zuruecklaesst, naemlich `audit.sqlite3` im Volume, wenn das Protokoll
       eingeschaltet war.
    3. Ein neuer Pruefschritt in der Liste der Checks, in der Form von Check 2 (Wegwerf-Container,
       `docker run --rm -v nc_app_mcp_connector_data:/d:ro alpine:3`, `sqlite3`, Ausgabe
       darunter): er liest `audit.sqlite3` und zeigt die Zeilenzahl der Tabelle `entries`. Der
       Text darunter sagt, dass ein Ergebnis ungleich null hier erwartet und kein Fehler ist,
       wenn das Protokoll eingeschaltet war, und dass die Datei fehlt, wenn es nie an war.
       Falls die Checks numeriert sind, wird die Numerierung durchgezogen und keine bestehende
       Nummer doppelt vergeben.
    4. Feldtabelle der Purge-Antwort (Zeilen 155-219): die Zeile zu `tables_cleared` sagt, dass
       es die sieben Tabellen des OAuth-Speichers sind und die Audit-Datei daneben liegen bleibt.
    5. Gegenpruefungen nach `--rm-data` (Zeilen 229-241): ein Satz ergaenzen, dass mit dem Volume
       auch das Protokoll geht, weil die Datei darin liegt; das ist die benannte Ausnahme aus
       D-18 und schliesst Restpunkt R-18-04.
    6. Versionstabelle (Zeile 47): der Eintrag "Delete data on remove" fuer NC 32/33 bekommt den
       Hinweis, dass dieser Weg das Volume und damit das Protokoll ebenso entfernt.

    `docs/faq.md`, eine Stelle: der Eintrag "How do I remove the app and its data completely?"
    (Zeilen 114-122). Die Antwort behaelt ihre Form (zwei Kommandos in dieser Reihenfolge,
    Verweis auf `uninstall.md` am Ende) und sagt zusaetzlich, dass der Purge das Protokoll nicht
    leert und erst `--rm-data` es mitnimmt. Das Wort "completely" in der Ueberschrift ist der
    Grund, diesen Eintrag mitzuziehen; CONTEXT nennt ihn nicht ausdruecklich, die Recherche
    empfiehlt ihn (Assumption A5) und diese Empfehlung wird hier uebernommen. Im SUMMARY
    ausdruecklich als Ergaenzung ueber CONTEXT hinaus ausweisen.

    Verboten in beiden Dateien: die vier Ansprueche aus Plan 19-03, das Wort "archiv", Em-Dashes,
    Emojis. Kein Shell-Kommando erfinden, das nicht der Form der bestehenden Checks folgt.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "audit" docs/uninstall.md` ergibt mindestens 6 (Ausgangszustand war 0)
    - `grep -c "audit.sqlite3" docs/uninstall.md` ergibt mindestens 2
    - `grep -c "leaves behind: nothing" docs/uninstall.md` ergibt 0
    - `grep -c "rm-data" docs/uninstall.md` ergibt mindestens 3 und im Abschnitt der Gegenpruefungen steht das Wort "audit"
    - `grep -ci "audit" docs/faq.md` ergibt mindestens 1
    - `grep -ci "archiv" docs/uninstall.md docs/faq.md` ergibt 0
    - `uv run pytest tests/unit/test_exapp_env_setup.py -k "vocabulary or forbidden_claim" -q` gruen
  </acceptance_criteria>
  <done>Das Log ist im Deinstallations-Runbook namentlich, hat einen eigenen Pruefschritt, und die `--rm-data`-Grenze steht in der Administratordokumentation statt nur im Phasenartefakt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Die neuen Doku-Saetze an Codekonstanten binden</name>
  <files>tests/unit/test_docs_audit_truth.py</files>
  <read_first>
    - tests/unit/test_oauth_store.py (Zeilen 1485-1512: das Muster, eine Doku-Aussage an ein Codefaktum zu binden)
    - docs/privacy.md und docs/uninstall.md und docs/faq.md (die Ergebnisse von Task 1 und 2)
    - src/mcp_connector/audit/store.py (Zeilen 84-135: RETENTION_DAYS, SIZE_LIMIT_BYTES, USER_SILENCE_DAYS, AUDIT_FILENAME)
  </read_first>
  <behavior>
    - `docs/privacy.md` enthaelt die Zahl aus `store.RETENTION_DAYS`, die Groesse aus `store.SIZE_LIMIT_BYTES` in MB und die Zahl aus `store.USER_SILENCE_DAYS`, jede als eigene Behauptung mit der Konstante auf der anderen Seite
    - `docs/privacy.md` und `docs/uninstall.md` enthalten `store.AUDIT_FILENAME` woertlich
    - Keine der drei Dateien enthaelt eine Wendung, die die Aufbewahrungsfrist zum einzigen automatischen Loescher macht
    - Keine der drei Dateien enthaelt "empties every table"
    - `docs/uninstall.md` nennt das Wort audit mindestens einmal, weil sein Fehlen der Ausgangszustand von R-18-04 war
    - Ein Test ist rot, wenn eine der drei Zahlen im Code geaendert und im Text nicht nachgezogen wird
  </behavior>
  <action>
    Neue Testdatei `tests/unit/test_docs_audit_truth.py`. Begruendung im Modul-Docstring: die
    Datei bindet drei Dokumente an drei Konstanten und gehoert zu keinem einzelnen Modul; das
    Muster stammt aus `tests/unit/test_oauth_store.py:1490-1509`, wo eine Doku-Aussage schon
    einmal an ein Codefaktum gehalten wird.

    Aufbau: `ROOT` ueber `Path(__file__).resolve().parents[2]`, drei Modulkonstanten mit den
    Pfaden, ein Helfer, der eine Datei liest. Je Behauptung eine eigene Testfunktion, damit eine
    rote Zeile sagt, welche Aussage gebrochen ist. Die Zahlen werden aus
    `mcp_connector.audit.store` importiert und in die erwartete Zeichenkette formatiert, NICHT
    als Literal in den Test geschrieben: das ist der ganze Zweck der Bindung.
    `SIZE_LIMIT_BYTES` wird fuer die Textform durch 1_000_000 geteilt und als "100 MB" erwartet,
    mit einem Kommentar, dass der Text die dezimale Form nennt, weil `used_bytes` in Bytes
    zaehlt und ein Administrator MB liest.

    Ein Negativtest ueber alle drei Dateien fuer die Wendungen, die verschwinden mussten
    ("empties every table", "the only automatic", "leaves behind: nothing",
    "There is no long lived store of personal data"), in der Meldungsform des Vokabular-Gates
    (Datei und Zeilennummer zuerst).

    Kein Duplikat des Vier-Woerter-Gates aus Plan 19-03: dieses Modul prueft Wahrheit, nicht
    Vokabular.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_docs_audit_truth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_docs_audit_truth.py -q` gruen und meldet mindestens acht Tests
    - `grep -c "RETENTION_DAYS\|SIZE_LIMIT_BYTES\|USER_SILENCE_DAYS\|AUDIT_FILENAME" tests/unit/test_docs_audit_truth.py` ergibt mindestens 4
    - `grep -vc "^\s*#" tests/unit/test_docs_audit_truth.py` zeigt keine Zeile, die "180" oder "100_000_000" oder "30" als Literal in einer Behauptung fuehrt (die Zahlen kommen aus dem Import)
    - Eine von Hand auf 90 gesetzte `RETENTION_DAYS` macht mindestens einen Test rot; danach zurueckgenommen und im SUMMARY festgehalten
    - `uv run pytest tests/unit tests/contract -q` gruen
  </acceptance_criteria>
  <done>Drei Dokumente haengen an drei Konstanten, vier verschwundene Wendungen sind als Negativfall belegt, die Gegenprobe ist einmal gelaufen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oeffentlicher Text zu Codewahrheit | `docs/privacy.md`, `docs/uninstall.md` und `docs/faq.md` sind die Zusage an einen Datenschutzbeauftragten; jede Aussage darin ist pruefbar und heute an fuenf Stellen falsch |
| Administrator zu Daten | Wer das Volume entfernt, entfernt das Log; das ist eine Handlung und kein Angriff, muss aber dokumentiert sein (D-18) |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-19-15 | Repudiation | high | `docs/privacy.md` Retention und Purge | mitigate | Alle drei automatischen Loeschwege mit ihren Zahlen benannt, jede Zahl per Test an `RETENTION_DAYS`, `SIZE_LIMIT_BYTES` und `USER_SILENCE_DAYS` gebunden; die Wendung "the only automatic" ist als Negativfall verboten |
| T-19-16 | Repudiation | high | `docs/uninstall.md` Titel, Abschnitt, Checks | mitigate | Das Log namentlich, ein eigener Pruefschritt fuer `audit.sqlite3`, `tables_cleared` praezisiert; schliesst Restpunkt R-18-04, dessen Ausgangszustand `grep -ri audit docs/uninstall.md` ohne Treffer war |
| T-19-17 | Information Disclosure | medium | Neue Beschreibung der gespeicherten Felder | mitigate | Der Text nennt die Felder, die es gibt, und ausdruecklich die, die es nicht gibt (IP, User-Agent, Parameterwert, Ergebnisinhalt, Fehlermeldung); kein Beispiel eines echten Parameterwerts im Text |
| T-19-18 | Repudiation | medium | `docs/faq.md` completely-Frage | mitigate | Die kuerzeste oeffentliche Antwort auf "wie werde ich alle Daten los" nennt beide Kommandos und die Ausnahme; als Ergaenzung ueber CONTEXT hinaus im SUMMARY ausgewiesen (Assumption A5) |
| T-19-19 | Repudiation | low | Verbotene Ansprueche im neuen Text | mitigate | Das Gate aus Plan 19-03 laeuft ueber `docs/**/*.md` und ist Teil der Verifikation dieses Plans |
| T-19-SC | Tampering (Supply Chain) | low | `pyproject.toml`, `uv.lock` | mitigate | Kein Paket: `git diff --stat 4baacbd HEAD -- pyproject.toml uv.lock` ist leer |
</threat_model>

<verification>
1. `uv run pytest tests/unit tests/contract -q` gruen, insbesondere
   `test_the_privacy_doc_describes_the_clients_table_as_it_is` und die zwei Gates aus Plan 19-03.
2. `uv run ruff check . && uv run ruff format --check .` still.
3. `git diff --stat 4baacbd HEAD -- appinfo/info.xml README.md README.de.md README.fr.md CHANGELOG.md pyproject.toml uv.lock`
   ist leer: dieser Plan fasst Manifest, READMEs und Changelog nicht an (das gehoert zu 19-08 und 19-09).
4. Kein Tag, kein Store-Upload, keine Version: `git tag --points-at HEAD` leer,
   `grep -c "0.1.12" appinfo/info.xml pyproject.toml` ergibt 0.
5. Nach jedem `gsd-sdk`-Aufruf `.planning/STATE.md` und `.planning/ROADMAP.md` pruefen.
</verification>

<success_criteria>
- Zwei Datenbanken, drei Loescher, ein ueberlebender Purge, eine benannte Ausnahme: alles in den Nutzertexten
- `docs/uninstall.md` nennt das Audit-Log und hat einen Pruefschritt dafuer
- Acht Behauptungen binden die neuen Saetze an vier Codekonstanten
</success_criteria>

<output>
Create `.planning/phases/19-audit-log-bedienung-und-textnachzug/19-05-SUMMARY.md` when done.
Im SUMMARY zwei Dinge ausdruecklich festhalten: (1) dass die verengte Formulierung aus D-v1.5-01
bewusst nicht uebernommen wurde und warum (drei Loescher am Code belegt, Messung statt
Entscheidung); (2) dass `docs/faq.md` eine Ergaenzung ueber CONTEXT hinaus ist.
</output>
