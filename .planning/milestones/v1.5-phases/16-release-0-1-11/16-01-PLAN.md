---
phase: 16-release-0-1-11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_connector/__init__.py
  - appinfo/info.xml
  - uv.lock
  - README.md
  - README.de.md
  - README.fr.md
  - CHANGELOG.md
autonomous: true
requirements: [EXAPP-11]

must_haves:
  truths:
    - "Die Zeichenkette 0.1.11 steht als dieselbe Version an allen sechs Versionsstellen"
    - "Der Changelog-Block 0.1.11 nennt den gekürzten Trifecta-Absatz samt der Teilen-Formulierung und den Wechsel des Autorenkontakts auf admin@infranode.dev"
    - "Die Linkdefinition [0.1.11] existiert und referenziert einen Abschnitt, der existiert"
    - "Der [Unreleased]-Block und die [Unreleased]-Linkdefinition sind nach diesem Plan verschwunden, beide zusammen"
    - "Die Nutzlast in appinfo/info.xml wird nicht neu formuliert; die vier bereits fertigen Zeilen bleiben Wort für Wort stehen"
    - "Nach diesem Plan existiert kein Tag v0.1.11"
  artifacts:
    - path: "pyproject.toml"
      provides: "Paketversion 0.1.11"
      contains: "version = \"0.1.11\""
    - path: "src/mcp_connector/__init__.py"
      provides: "__version__ 0.1.11"
      contains: "__version__ = \"0.1.11\""
    - path: "appinfo/info.xml"
      provides: "Manifest-Version und image-tag 0.1.11"
      contains: "<version>0.1.11</version>"
    - path: "README.md"
      provides: "Statuszeile 0.1.11, die fünfte Stelle, die im signierten Tarball mitreist und die kein Gate hält"
      contains: "Version 0.1.11."
    - path: "uv.lock"
      provides: "Selbsteintrag 0.1.11, die sechste Stelle"
      contains: "version = \"0.1.11\""
    - path: "CHANGELOG.md"
      provides: "Release-Notes-Block 0.1.11 plus seine Linkdefinition, an der Stelle des bisherigen [Unreleased]-Blocks"
      contains: "## [0.1.11]"
  key_links:
    - from: "appinfo/info.xml"
      to: "src/mcp_connector/__init__.py"
      via: "Versions-Gleichheit, gehalten von tests/unit/test_exapp_env_setup.py"
      pattern: "0\\.1\\.11"
    - from: "appinfo/info.xml <image-tag>"
      to: "appinfo/info.xml <version>"
      via: "image-tag folgt der Version, gehalten vom selben Gate und ein zweites Mal von release.yml beim Tag"
      pattern: "<image-tag>0\\.1\\.11</image-tag>"
    - from: "CHANGELOG.md Linkdefinition [0.1.11]"
      to: "GitHub-Compare-Bereich v0.1.10...v0.1.11"
      via: "Markdown-Referenzdefinition am Dateiende, gepaart mit dem Abschnitt ## [0.1.11]"
      pattern: "compare/v0\\.1\\.10\\.\\.\\.v0\\.1\\.11"
---

<objective>
Dieser Plan hebt die Version des Repositories auf 0.1.11 und macht aus dem wartenden
`[Unreleased]`-Block den Release-Notes-Block 0.1.11. Er erzeugt keinen Tag und stößt nichts an.

Purpose: Runbook Schritt 1 und 2 aus `docs/store-submission.md`. Ohne diesen Plan gibt es
keinen 0.1.11-Kandidaten: `scripts/build_store_release.sh` liest die Version aus
`appinfo/info.xml` und benennt das Archiv danach, und `release.yml` verweigert einen Tag,
dessen Name nicht der Manifest-Version gleicht.

0.1.11 existiert als Release, weil der Store das Manifest ausschließlich zur Uploadzeit
liest. Die Nutzlast liegt seit den Commits `b3267cd`, `901b294` und `deafbf4` fertig im
Repository und erreicht bis zu diesem Release keinen einzigen Leser der Store-Seite. Genau
diese Begründung tragen die Blöcke 0.1.5, 0.1.6, 0.1.9 und 0.1.10 schon.

Der Umfang der Nutzlast ist gemessen und nicht geschätzt: `git diff v0.1.10..HEAD` berührt
genau eine ausgelieferte Datei, `appinfo/info.xml`, und darin genau vier Zeilen. Drei davon
sind derselbe gekürzte Trifecta-Absatz in den `<description>`-Blöcken Englisch, Deutsch und
Französisch, die vierte ist das Attribut `<author mail>`. Die drei READMEs tragen diesen
Absatz nicht und sind seit dem Tag `v0.1.10` unverändert: sie bekommen in dieser Phase
ausschließlich ihre Statuszeile gehoben und keine Nutzlast. Das ist der Unterschied zu 0.1.10,
wo die READMEs mit im Text standen.

Output: sechs geänderte Versionsstellen, ein Changelog-Block anstelle des `[Unreleased]`-Blocks,
eine getauschte Link-Referenz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4-phases/15-release-0-1-10/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sechs Versionsstellen auf 0.1.11</name>
  <files>pyproject.toml, src/mcp_connector/__init__.py, appinfo/info.xml, uv.lock, README.md, README.de.md, README.fr.md</files>
  <read_first>
    - pyproject.toml, Zeile 1-5 (`version = "0.1.10"` steht auf Zeile 3)
    - src/mcp_connector/__init__.py, Zeile 1-9 (`__version__ = "0.1.10"` steht auf Zeile 7)
    - appinfo/info.xml, Zeile 183 (`<version>`) und Zeile 258 (`<image-tag>`)
    - appinfo/info.xml, Zeile 185 (`<author mail="admin@infranode.dev">street1983nk</author>`): die Nutzlast dieses Releases, die in diesem Task nicht angefasst wird
    - uv.lock, Zeile 468-476 (der Block mit `name = "nextcloud-mcp-connector"`, die Versionszeile ist 472)
    - README.md, Zeile 25-29 (Abschnitt `## Status`, Statuszeile auf 27)
    - README.de.md, Zeile 27-31 (Statuszeile auf 29)
    - README.fr.md, Zeile 29-33 (Statuszeile auf 31)
    - docs/store-submission.md, Runbook Schritt 1 (Zeile 197-210): dort stehen die Stellen wörtlich, samt der Begründung, warum die drei Statuszeilen von Hand gehoben werden
    - docs/store-submission.md, Zeile 146 (die 0.1.10-Proof-Zeile zu Schritt 1: sie nennt die sechs Stellen, die uv.lock-Begründung und die vier CRLF-Dateien mit ihren Zeilenzahlen)
    - tests/unit/test_exapp_env_setup.py, der Manifest-Gate, der `<version>` gegen die Paketversion und `<image-tag>` gegen `<version>` hält
    - .planning/milestones/v1.4-phases/15-release-0-1-10/15-01-PLAN.md, Task 1 (dasselbe Vorgehen für 0.1.10, inklusive der uv.lock-Begründung und der CRLF-Regel)
  </read_first>
  <action>
    Ersetze die Zeichenkette 0.1.10 durch 0.1.11 an genau sechs Stellen und an keiner
    weiteren. Ein Grep über die sieben Dateien nennt heute genau acht Zeilen mit 0.1.10, und
    genau diese acht sind die Arbeitsmenge; die drei README-Statuszeilen zählen als eine
    Stelle, die fünfte, und `appinfo/info.xml` trägt zwei Zeilen für eine Stelle plus eine
    zweite Stelle:

    `pyproject.toml:3` zu `version = "0.1.11"`, `src/mcp_connector/__init__.py:7` zu
    `__version__ = "0.1.11"`, `appinfo/info.xml:183` zu `<version>0.1.11</version>`,
    `appinfo/info.xml:258` zu `<image-tag>0.1.11</image-tag>`, die drei Statuszeilen
    (`Version 0.1.11.` als Zeilenanfang in README.md:27, README.de.md:29 und
    README.fr.md:31) und `uv.lock:472` zu `version = "0.1.11"`.

    Die uv.lock-Zeile ist die sechste Stelle und wird bewusst mitgezogen: die Bumps auf
    0.1.8, 0.1.9 und 0.1.10 sind ihr genauso gefahren, obwohl kein Gate und kein
    Runbook-Schritt sie hält. Fasse sie per Texteditierung an, rufe NICHT `uv lock` und NICHT
    `uv sync` auf: ein Lock-Lauf würde Dependency-Zeilen bewegen. Kein `uv add`, kein
    `pip install`, kein Dependency-Block in `pyproject.toml` oder `uv.lock`.

    README.md, README.de.md, README.fr.md und appinfo/info.xml liegen mit CRLF im
    Arbeitsbaum (536, 551, 570 und 540 CRLF-Zeilen, keine reine LF-Zeile). Patche diese vier
    byte-exakt, per Python im Binärmodus (`rb` lesen, ersetzen, `wb` schreiben), wie die
    Phasen 14 und 15 es getan haben. Sonst entsteht ein Massen-Diff aus Zeilenenden und der
    Diff verliert seine Aussage. `pyproject.toml`, `src/mcp_connector/__init__.py` und
    `uv.lock` liegen mit LF und werden normal editiert.

    In den drei READMEs ändert sich ausschließlich die Versionszahl der Statuszeile. Die Zahl
    21 in "All 21 tools", "Alle 21 Tools" und "Les 21 outils" bleibt unangetastet: sie steht
    unter `tests/contract/test_tool_surface.py`, und in dieser Phase wird kein Werkzeug
    angefasst.

    In `appinfo/info.xml` werden `<summary>`, die drei `<description>`-Blöcke, die
    `<author>`-Zeile und `<environment-variables>` in diesem Task NICHT angefasst. Ein leer
    gemachtes Element lässt den Store-Upload später mit HTTP 500 sterben, und die vier
    fertigen Zeilen sind die Nutzlast dieses Releases: der gekürzte Trifecta-Absatz in den
    drei Sprachen und `<author mail="admin@infranode.dev">`. Sie dürfen nicht neu formuliert
    und nicht verschoben werden.

    Fasse `CHANGELOG.md` in diesem Task nicht an. Die Linkdefinitionen am Dateiende enthalten
    die Zeichenkette 0.1.10 in Compare-URLs, die zur Historie gehören und bleiben.

    Erzeuge keinen Tag. `git tag` bleibt in diesem Plan unbenutzt.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.1.11"' pyproject.toml` gibt 1
    - `grep -c '^__version__ = "0.1.11"' src/mcp_connector/__init__.py` gibt 1
    - `grep -o '<version>[^<]*' appinfo/info.xml` gibt genau `<version>0.1.11`
    - `grep -o '<image-tag>[^<]*' appinfo/info.xml` gibt genau `<image-tag>0.1.11`
    - `grep -n '^Version 0\.1\.11\.' README.md README.de.md README.fr.md` gibt drei Treffer, einen je Datei
    - `grep -n '^version = "0.1.11"' uv.lock` nennt Zeile 472
    - `grep -rn '0\.1\.10' pyproject.toml src/mcp_connector/__init__.py uv.lock README.md README.de.md README.fr.md appinfo/info.xml` gibt keinen Treffer
    - `git diff --numstat` nennt für die sechs Nicht-XML-Dateien je `1	1` und für `appinfo/info.xml` genau `2	2`, also keine Zeilenenden-Umschreibung
    - `git diff -- pyproject.toml uv.lock` enthält keine Zeile mit `dependencies`, `requires-dist` oder einem Paketnamen außer `nextcloud-mcp-connector`
    - `git diff -- appinfo/info.xml` enthält keine Zeile mit `<summary`, `<description`, `<author`, `admin@infranode.dev` oder `<default`
    - `python -c "d=open('appinfo/info.xml','rb').read(); print(d.count(b'\r\n'))"` gibt 540, und dieselbe Zählung gibt 536, 551 und 570 für README.md, README.de.md und README.fr.md
    - `python -c "import xml.etree.ElementTree as E; E.parse('appinfo/info.xml')"` läuft ohne Fehler
    - `grep -c 'author mail="admin@infranode.dev"' appinfo/info.xml` gibt 1, die Nutzlast steht also unverändert
    - `git status --short CHANGELOG.md` gibt keine Zeile aus
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
    - `git tag --list v0.1.11` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Stellen tragen dieselbe Zeichenkette 0.1.11, kein Zeilenenden-Massen-Diff, die Nutzlast in info.xml ist unberührt, das Manifest-Gate ist grün, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Aus dem [Unreleased]-Block wird der Changelog-Block 0.1.11</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md, Zeile 1-20 (Kopf mit SPDX-Kommentar, Keep-a-Changelog-Absatz, dann `## [Unreleased]` auf Zeile 12, sein Einleitungsabsatz auf 14 und die zwei `### Changed`-Punkte auf 18 und 19)
    - CHANGELOG.md, Zeile 21-48 (der 0.1.10-Block als Ton- und Formvorbild; sein Einleitungsabsatz und die Begründung über die Uploadzeit)
    - CHANGELOG.md, Zeile 516-528 (die Linkdefinitionen; `[Unreleased]:` steht als erste Zeile des Blocks und zeigt auf `compare/v0.1.10...HEAD`, `[0.1.10]:` direkt darunter)
    - .planning/milestones/v1.4-phases/14-doku-reste-und-gate-entscheid/14-01-SUMMARY.md, Abschnitt "DOC-01b: gewählter Weg und Regel für Phase 15" (die verbindliche Paarungsregel: eine Linkdefinition existiert nur zusammen mit dem Abschnitt, der sie referenziert)
    - .planning/milestones/v1.4-phases/15-release-0-1-10/15-01-PLAN.md, Task 2 (Form und Ja/Nein-Disziplin des Vorgänger-Blocks)
    - docs/store-submission.md, Zeile 155 (die Proof-Zeile vom 2026-08-28 06:05Z: sie zählt die Nutzlast dieses Releases vollständig auf, drei Commits an `appinfo/info.xml` und einer an `CHANGELOG.md`)
    - `git diff v0.1.10..HEAD -- appinfo/info.xml` (die vier geänderten Zeilen im Wortlaut, also die Nutzlast, über die der Block schreibt)
    - `git show 901b294 -- CHANGELOG.md` (die nach dem Tag geschriebene Korrektur am 0.1.10-Block, die mit diesem Asset erstmals veröffentlicht wird und KEINEN eigenen Eintrag bekommt)
  </read_first>
  <action>
    Mache aus dem bestehenden `## [Unreleased]`-Block den Block `## [0.1.11] - 2026-08-28`.
    Der Inhalt ist bereits geschrieben und wird nicht neu erfunden: die zwei Punkte unter
    `### Changed` bleiben in ihrer Aussage stehen. Das Datum ist der Kalendertag des
    Schreibens; Plan 16-03 prüft es vor dem Tag noch einmal nach und korrigiert es, falls der
    Tag an einem anderen Tag entsteht.

    Schreibe den Einleitungsabsatz um. Er sagt heute, die Änderungen "warten auf das nächste
    Release", und das ist in einem Release-Block falsch. Er sagt danach im Ton des
    0.1.10-Blocks: dieses Release trägt keinen Codewechsel, dieselben einundzwanzig Werkzeuge
    beantworten dieselben Fragen, eine Instanz, die aktualisiert, behält jedes Verhalten, und
    was sich ändert, ist Text im Manifest, den nur ein Release zu den Lesern der Store-Seite
    bringt, weil der Store das Manifest ausschließlich zur Uploadzeit liest.

    Der erste Punkt unter `### Changed` betrifft den gekürzten Trifecta-Absatz in den drei
    `<description>`-Blöcken von `appinfo/info.xml`, samt der geweiteten Teilen-Formulierung
    (nicht mehr ein geteilter Ordner, sondern überall dort, wo das Konto teilt). Er nennt
    weiter dieselben vier Dinge, die der Absatz benennt. Schreibe ihn NICHT auf die READMEs
    aus: die drei READMEs tragen diesen Absatz nicht und sind seit dem Tag `v0.1.10`
    unverändert; `git diff v0.1.10..HEAD -- README.md README.de.md README.fr.md` ist leer.
    "Store descriptions" ist die richtige und geprüfte Benennung.

    Der zweite Punkt unter `### Changed` betrifft den Autorenkontakt: das Attribut
    `<author mail>` im Manifest ist von k.cherif@outlook.de auf admin@infranode.dev gewechselt,
    damit die private Adresse den öffentlichen Store-Eintrag verlässt. Der Enterprise-Kontakt
    innerhalb der Beschreibung ist schon mit 0.1.10 gewechselt; dies ist das Feld, das der
    Store daneben anzeigt.

    Lege keine weitere Rubrik an. `### Changed` ist die einzige Rubrik dieses Blocks: es gibt
    in dieser Version nichts Hinzugefügtes, nichts Repariertes und nichts Entferntes.

    Nenne die Korrektur, die `901b294` am 0.1.10-Block geschrieben hat, NICHT als eigenen
    Eintrag. Sie ist Prosa im Block der Vorversion, sie fährt mit diesem Asset zum ersten Mal
    mit, und ein Eintrag darüber wäre ein Changelog-Eintrag über den Changelog. Genau so ist
    die WR-02-Korrektur bei 0.1.10 behandelt worden.

    Nimm ebenfalls nicht in den Block auf, weil nicht nutzersichtbar: die Archivierung der
    v1.4-Phasendokumente, die Anforderungen und die Roadmap des Meilensteins v1.5, die
    Recherchedokumente, der Meilenstein-Audit.

    Am Dateiende: entferne die Zeile
    `[Unreleased]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.10...HEAD`
    und setze an ihre Stelle
    `[0.1.11]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.10...v0.1.11`.
    Abschnitt und Definition wandern damit gemeinsam von `Unreleased` nach `0.1.11`, und die
    Paarungsregel aus Phase 14 bleibt gewahrt: keine hängende Definition, kein Abschnitt ohne
    Definition. Keine weitere Definition, keine entfernte Definition.

    Sprache Englisch wie die übrigen Blöcke. Keine Em-Dashes. `CHANGELOG.md` liegt im
    Vokabular-Gate und mit LF im Arbeitsbaum: das Wort `archiv` darf im neuen Block in keiner
    Sprache und in keinem Kompositum vorkommen, und die Zeilenenden bleiben LF.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## \[' CHANGELOG.md | head -2` zeigt `## [0.1.11] - 2026-08-28` vor `## [0.1.10] - 2026-08-28`
    - `grep -c 'Unreleased' CHANGELOG.md` gibt 0, der Abschnitt und seine Linkdefinition sind also beide verschwunden
    - `grep -c 'compare/v0.1.10...v0.1.11' CHANGELOG.md` gibt 1
    - Die Mengen aus `grep -oE '^## \[[0-9.]+\]' CHANGELOG.md` und `grep -oE '^\[[0-9.]+\]:' CHANGELOG.md` sind nach Versionen sortiert identisch, 12 gegen 12
    - Der 0.1.11-Block trägt genau eine Rubrik; `awk` zwischen `^## \[0\.1\.11\]` und `^## \[0\.1\.10\]` findet genau eine Zeile, die mit `### ` beginnt, und diese lautet `### Changed`
    - Der 0.1.11-Block enthält genau zwei Aufzählungspunkte
    - Der 0.1.11-Block nennt `admin@infranode.dev` und `k.cherif@outlook.de`
    - Der 0.1.11-Block nennt "upload time" oder eine gleichbedeutende Aussage über den Zeitpunkt, zu dem der Store das Manifest liest
    - Der 0.1.11-Block nennt weder `README.md` noch `README.de.md` noch `README.fr.md` noch `Unreleased` noch `901b294` noch `WR-02`
    - `git diff v0.1.10..HEAD --numstat -- README.md README.de.md README.fr.md` gibt keine Zeile aus, was die Aussage des vorigen Kriteriums belegt
    - `grep -in 'archiv' CHANGELOG.md` gibt keinen Treffer
    - Eine Zählung der Gedankenstrich-Zeichen U+2014 und U+2013 in CHANGELOG.md gibt jeweils 0, geprüft mit `python -c "d=open('CHANGELOG.md',encoding='utf-8').read(); print(d.count(chr(0x2014)), d.count(chr(0x2013)))"`
    - `python -c "d=open('CHANGELOG.md','rb').read(); print(d.count(b'\r\n'))"` gibt 0
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
    - `git tag --list v0.1.11` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Der 0.1.11-Block steht an der Stelle des bisherigen [Unreleased]-Blocks, nennt den gekürzten Trifecta-Absatz samt Teilen-Formulierung und den Wechsel des Autorenkontakts, die Linkdefinition ist mitgewandert, kein [Unreleased] ist übrig, das Vokabular-Gate ist grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu Git-Tag | Ein Tag ist der Auslöser des Release-Workflows und irreversibel; dieser Plan darf die Grenze nicht überschreiten |
| Repo-Text zu veröffentlichtem Artefakt | Was hier als Zeichenkette steht, reist unveränderlich im signierten Tarball und in den Store-Metadaten mit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Die sechs Versionsstellen | mitigate | Ein Akzeptanzkriterium je Stelle mit exaktem grep-Muster; `tests/unit/test_exapp_env_setup.py` hält Manifest gegen `__version__` und `image-tag` gegen `<version>`; `release.yml` prüft die Gleichheit beim Tag ein zweites Mal |
| T-16-02 | Denial of Service | Git-Tag v0.1.11 | mitigate | Dieser Plan erzeugt keinen Tag und behauptet in beiden Tasks als Akzeptanzkriterium, dass `git tag --list v0.1.11` leer ist; der Tag entsteht in Plan 16-03 nach ausdrücklicher Owner-Freigabe |
| T-16-03 | Tampering | Nutzlast des Releases in appinfo/info.xml | mitigate | Die vier fertigen Zeilen aus `b3267cd`, `901b294` und `deafbf4` werden nicht neu formuliert; `git diff -- appinfo/info.xml` darf keine Zeile mit `<summary`, `<description`, `<author` oder `<default` enthalten, und der Diff dieser Datei ist auf `2	2` begrenzt |
| T-16-04 | Denial of Service | appinfo/info.xml, leeres Element | mitigate | Beschreibungs-, Autoren- und Variablenblöcke bleiben unangetastet; zusätzlich parst ein Akzeptanzkriterium die Datei mit ElementTree, denn ein leeres Element beantwortet der Store-Upload mit HTTP 500 |
| T-16-05 | Repudiation | CHANGELOG.md, überzogene Reichweite | mitigate | Der Block nennt die READMEs ausdrücklich nicht, und ein Akzeptanzkriterium belegt mit `git diff v0.1.10..HEAD -- README*`, dass sie keine Nutzlast tragen; die Rubrik ist auf genau `### Changed` mit genau zwei Punkten begrenzt |
| T-16-06 | Repudiation | Hängende Linkdefinition | mitigate | `[Unreleased]:` wird durch `[0.1.11]:` ersetzt, nicht ergänzt; die Paarung wird als Mengengleichheit 12 gegen 12 geprüft, und `grep -c 'Unreleased'` muss 0 geben |
| T-16-07 | Tampering | Zeilenenden der vier CRLF-Dateien | mitigate | Byte-exaktes Patchen per Python rb/wb; die CRLF-Zählungen 536, 551, 570 und 540 werden nach dem Patch als Akzeptanzkriterium erneut geprüft |
| T-16-SC | Tampering | uv.lock, pyproject.toml Dependencies | mitigate | Kein `uv add`, `uv lock`, `uv sync`, `pip install` oder `npm install`; der Diff darf keinen Fremdpaketnamen und keine Dependency-Zeile enthalten; alle Prüfläufe mit `uv run --no-sync` |
</threat_model>

<verification>
- Alle sechs Versionsstellen tragen dieselbe Zeichenkette, kein 0.1.10-Rest in den sieben Dateien
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` grün (Manifest-Gate, Beschreibungs-Gate, Variablen-Gate, Vokabular-Gate)
- `git diff --numstat` nennt genau acht Dateien, je `1	1` außer `appinfo/info.xml` mit `2	2` und `CHANGELOG.md`
- `grep -c 'Unreleased' CHANGELOG.md` gibt 0, Abschnitte und Linkdefinitionen stehen 12 gegen 12
- `git tag --list v0.1.11` leer
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 2 ist geschrieben: die Version 0.1.11 steht als derselbe String an
allen sechs Stellen, der Changelog trägt einen Block 0.1.11 mit seiner Linkdefinition, und der
`[Unreleased]`-Block ist verschwunden. Kein Tag existiert.
</success_criteria>

<output>
Create `.planning/phases/16-release-0-1-11/16-01-SUMMARY.md` when done.
Nenne darin ausdrücklich, welches Datum die Kopfzeile des 0.1.11-Blocks trägt, damit Plan
16-03 es vor dem Tag prüfen kann, und halte fest, dass mit diesem Block auch die Korrektur aus
`901b294` am 0.1.10-Block erstmals in ein Asset fährt, ohne dass sie einen eigenen
Changelog-Eintrag bekommen hat.
</output>

