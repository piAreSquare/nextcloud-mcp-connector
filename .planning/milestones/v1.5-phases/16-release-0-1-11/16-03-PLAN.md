---
phase: 16-release-0-1-11
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - CHANGELOG.md
  - docs/store-submission.md
autonomous: false
requirements: [EXAPP-11]

must_haves:
  truths:
    - "Der Branch main ist gepusht, bevor irgendein Tag existiert"
    - "Der Tag v0.1.11 entsteht erst nach einer ausdrücklichen Owner-Freigabe"
    - "Der Tag heißt genau v0.1.11 und entspricht der Version des Manifests"
    - "Der Release-Workflow ist in jedem Schritt grün und hängt das Store-Archiv an das GitHub-Release"
    - "Die Runbook-Schritte 4 und 5 tragen eine Proof-Zeile mit Datum, Behauptung und Befehl"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeilen zu Push, Tag und Workflow-Lauf"
      contains: "v0.1.11"
  key_links:
    - from: "Tag v0.1.11"
      to: "appinfo/info.xml <version>"
      via: "Versions-Gleichheitsprüfung in .github/workflows/release.yml (Zeile 44-59), Exit 1 bei Ungleichheit"
      pattern: "v0\\.1\\.11"
    - from: ".github/workflows/release.yml"
      to: "GitHub Release Asset mcp_connector-0.1.11.tar.gz"
      via: "scripts/build_store_release.sh im Job publish, dann action-gh-release"
      pattern: "mcp_connector-0\\.1\\.11\\.tar\\.gz"
---

<objective>
Runbook Schritt 4 und 5: der Push vor dem Tag, das menschliche Tor, der Tag und der Workflow.
Dieser Plan überschreitet die einzige irreversible Grenze der Phase und ist deshalb nicht
autonom.

Purpose: Der Tag löst den Release-Workflow aus, ein Asset ist danach unveränderlich und darf
nie gelöscht werden, weil AppAPI von unserer URL installiert und nicht aus dem Store. Der Push
kommt zuerst: die Store-Beschreibung verlinkt `blob/main/...`-Dokumente und die Screenshots
laden von `raw.githubusercontent.com/.../main/...`, ein Tag auf einem nicht gepushten Stand
veröffentlicht also ein Release, dessen verlinkte Seiten einen älteren Stand zeigen (das
0.1.8-Release fand an genau dieser Stelle 42 ungepushte Commits).

Output: gepushter Branch, festgehaltene Owner-Antwort, Tag `v0.1.11`, grüner Workflow-Lauf,
eine Proof-Zeile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-release-0-1-11/16-01-SUMMARY.md
@.planning/phases/16-release-0-1-11/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch pushen, Changelog-Datum prüfen, Abwesenheit des Tags belegen</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 232-242 (Runbook Schritt 4, der Push vor dem Tag samt Begründung)
    - docs/store-submission.md, Zeile 149 (die 0.1.10-Zeile zur ersten Hälfte von Schritt 4 als Formvorbild: sie steht bewusst allein, behauptet die Abwesenheit des Tags und erklärt die Datumsprüfung zwischen lokaler Zeit und UTC)
    - .github/workflows/release.yml, Zeile 22 (Trigger `tags: v*`) und Zeile 41-59 (die Versions-Gleichheitsprüfung, die bei Ungleichheit mit Exit 1 abbricht)
    - CHANGELOG.md, die Kopfzeile des 0.1.11-Blocks (`## [0.1.11] - ...`)
    - .planning/phases/16-release-0-1-11/16-01-SUMMARY.md (welches Datum Plan 16-01 in die Kopfzeile geschrieben hat)
  </read_first>
  <action>
    Prüfe zuerst die Datumszeile des 0.1.11-Blocks in `CHANGELOG.md` gegen den heutigen
    Kalendertag. Stimmt sie nicht, korrigiere ausschließlich diese eine Zeile, damit das
    Release-Datum nicht lügt. Achte dabei auf die Falle, die die 0.1.10-Zeile beschreibt: der
    Kalendertag in Europe/Berlin und der in UTC können auseinanderfallen, und maßgeblich ist,
    dass der Tag nicht an einem früheren Kalendertag entstehen kann, als der Block ihn nennt.
    Ändere keinen anderen Teil des Blocks, und lasse die Zeilenenden von `CHANGELOG.md` bei LF.

    Pushe dann alle Commits der Phase: `git push origin main`. Belege danach mit
    `git log origin/main..HEAD`, dass nichts mehr ungepusht ist, und mit `git status --short`,
    dass der Arbeitsbaum sauber ist.

    Belege außerdem, dass noch kein Tag existiert: `git tag --list v0.1.11` und
    `git ls-remote --tags origin v0.1.11` sind beide leer, und
    `gh run list --workflow release.yml --limit 5` nennt keinen Lauf zu `v0.1.11`. Erzeuge in
    diesem Task keinen Tag.

    Setze keinen Milestone-Tag. Ein Tag `v1.5` würde `release.yml` auslösen und an der
    Versions-Gleichheitsprüfung mit Exit 1 abbrechen; Milestone-Tags heißen `milestone-v*` und
    triggern nichts.
  </action>
  <verify>
    <automated>git log origin/main..HEAD --oneline | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..HEAD --oneline | wc -l` gibt 0
    - `git status --short` gibt keine Zeile aus
    - Die Kopfzeile des 0.1.11-Blocks in `CHANGELOG.md` trägt den heutigen Kalendertag in der Form `## [0.1.11] - YYYY-MM-DD`, und die Ausgaben von `date` und `date -u` sind zusammen mit der Entscheidung notiert
    - Falls die Datumszeile korrigiert wurde, nennt `git diff --numstat CHANGELOG.md` im zugehörigen Commit genau `1	1`
    - `git tag --list v0.1.11` gibt keine Zeile aus
    - `git ls-remote --tags origin v0.1.11` gibt keine Zeile aus
    - `gh run list --workflow release.yml --limit 5` nennt keinen Lauf zu `v0.1.11`
  </acceptance_criteria>
  <done>main ist gepusht, der Arbeitsbaum ist sauber, das Changelog-Datum stimmt, kein Tag und kein Workflow-Lauf existieren.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner-Freigabe für den Tag v0.1.11 und die Store-Sitzung</name>
  <files>keine</files>
  <read_first>
    - .planning/phases/16-release-0-1-11/16-01-SUMMARY.md und 16-02-SUMMARY.md (was bis Runbook-Schritt 3 geliefert und gemessen wurde)
    - docs/store-submission.md, Zeile 258-288 (was die Store-Sitzung in Schritt 7 verlangt und welche drei Wege es gibt)
    - docs/store-submission.md, Zeile 155 (die Drift-Zeile vom 06:05Z: sie beschreibt genau den Zustand, den dieses Release auflöst)
  </read_first>
  <action>
    Halte hier an und lege dem Owner den Stand aus <what-built> vor, zusammen mit den fünf
    Schritten aus <how-to-verify>. Führe in diesem Task kein Kommando aus, das einen Tag
    erzeugt oder einen Push auslöst: `git tag`, `git push origin v0.1.11` und jede
    Store-Einreichung sind hier verboten. Nimm die Antwort wörtlich auf (`freigeben` oder
    `warten`) samt dem genannten Weg für Schritt 7 und halte sie mit Zeitpunkt fest. Ohne die
    Antwort `freigeben` endet der Plan hier, Task 3 bleibt ungestartet und Plan 16-04 bleibt
    ungestartet.
  </action>
  <what-built>
    Alles bis Runbook-Schritt 3 ist erledigt und belegt: sechs Versionsstellen auf 0.1.11, aus
    dem `[Unreleased]`-Block ist der Changelog-Block 0.1.11 geworden, mit `### Changed` als
    einziger Rubrik und zwei Punkten, dem gekürzten Trifecta-Absatz samt der geweiteten
    Teilen-Formulierung in den drei Store-Beschreibungen und dem Wechsel des Attributs
    `<author mail>` von k.cherif@outlook.de auf admin@infranode.dev. Die `[Unreleased]`-Definition
    ist mit ihrem Abschnitt zusammen verschwunden. Sechs Gates lokal grün ohne Anhebung, der
    Archiv-Probelauf bestanden mit Statuszeile 0.1.11, dem gekürzten Absatz und der neuen
    Autorenadresse im mitreisenden Manifest und dem Block 0.1.11 im mitreisenden CHANGELOG.md,
    drei datierte Proof-Zeilen, `main` gepusht, kein Tag vorhanden.

    Dieses Release trägt keinen Codewechsel: dieselben einundzwanzig Werkzeuge, dieselbe
    Werkzeugoberfläche von 15712 Bytes. Die gesamte Nutzlast sind vier Zeilen in einer Datei,
    `appinfo/info.xml`. Die drei READMEs tragen diesmal keine Nutzlast.
  </what-built>
  <how-to-verify>
    1. `git log origin/main..HEAD` ist leer und `git status --short` gibt nichts aus.
    2. `git tag --list v0.1.11` ist leer: es existiert noch kein Tag.
    3. Lies den 0.1.11-Block in `CHANGELOG.md` und die vier geänderten Zeilen aus
       `git diff v0.1.10..HEAD -- appinfo/info.xml`: sagen beide dasselbe, und ist
       admin@infranode.dev die Adresse, die als Autorenkontakt im Store stehen soll?
    4. Lies die drei neuen Zeilen am Ende der Nachweistabelle von `docs/store-submission.md`.
    5. Entscheide dann zwei Dinge:
       Erstens die Freigabe. `freigeben` bedeutet: `git tag v0.1.11` und
       `git push origin v0.1.11` starten `release.yml`, bauen das Multi-Arch-Image nach
       ghcr.io und hängen das Store-Archiv an ein GitHub-Release. Das ist irreversibel; ein
       Asset darf danach nie gelöscht und ein Tag nie umgeschrieben werden, weil AppAPI von
       dieser URL installiert. Eine spätere Korrektur kostet eine neue Patch-Version.
       `warten` bedeutet: nichts ist veröffentlicht, die Phase bleibt bis Schritt 3
       vollständig und in diesem Zustand prüfbar, die Erfolgskriterien 1, 3 und 4 bleiben
       offen, und der `[Unreleased]`-Block bleibt geleert, obwohl kein Release ihn abgeholt
       hat, was Phase 19 blockiert.
       Zweitens den Weg für Schritt 7 in Plan 16-04: eine angemeldete Store-Sitzung im
       Browser, ein `NC_STORE_TOKEN` in der Umgebung, oder das Formular unter
       `https://apps.nextcloud.com/developer/apps/releases/new`. Ohne einen dieser Wege endet
       die Phase nach Schritt 6.
  </how-to-verify>
  <acceptance_criteria>
    - Die Antwort lautet wörtlich `freigeben` oder `warten`
    - Bei `freigeben` ist außerdem benannt, welcher der drei Wege für Schritt 7 zur Verfügung steht
    - Die Antwort ist im SUMMARY dieses Plans festgehalten, mit Zeitpunkt
    - Ohne die Antwort `freigeben` wird Task 3 nicht ausgeführt und kein Tag erzeugt
    - `git tag --list v0.1.11` ist am Ende dieses Tasks leer
  </acceptance_criteria>
  <resume-signal>Antworte mit `freigeben` oder `warten`. Nenne bei `freigeben` außerdem, ob eine angemeldete Store-Sitzung, ein `NC_STORE_TOKEN` oder das Formular für Schritt 7 der Weg ist.</resume-signal>
  <verify>
    <automated>test -z "$(git tag --list v0.1.11)" && echo "kein Tag, korrekt"</automated>
  </verify>
  <done>Die Antwort des Owners liegt wörtlich vor und ist mit Zeitpunkt festgehalten; kein Tag ist entstanden.</done>
</task>

<task type="auto">
  <name>Task 3: Tag v0.1.11, Workflow beobachten, Proof-Zeile der Schritte 4 und 5</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 232-249 (Runbook Schritte 4 und 5) und Zeile 150 (die 0.1.10-Zeile als Formvorbild: sie nennt Run-Id, Job-Name, Laufzeit, Bytegröße des Assets, den Zeitpunkt der Freigabe und die Warnung, dass `createdAt` das Commit-Datum ist und nicht der Moment der Veröffentlichung)
    - .github/workflows/release.yml, Zeile 37-89 (checkout, die Versions-Gleichheitsprüfung, qemu und buildx, der Login, der Build für beide Plattformen, der Aufruf von `scripts/build_store_release.sh` und das Anhängen des Assets)
    - .planning/phases/16-release-0-1-11/16-02-SUMMARY.md (die Bytegröße des lokal gebauten Archivs, die Plan 16-04 gegen das veröffentlichte Asset stellt)
  </read_first>
  <action>
    Nur ausführen, wenn die Antwort aus Task 2 `freigeben` lautet.

    Setze den Tag exakt `v0.1.11`, kein anderer Name: `git tag v0.1.11`, dann
    `git push origin v0.1.11`. Die Tippfehler dieser Version sind besonders tückisch, weil
    zwei Nachbarn schon existieren: `v0.1.1` gibt es seit Monaten und ein Tag darauf schlüge
    fehl, `v0.1.10` gibt es seit gestern und trägt ein anderes Asset. Prüfe den Namen vor dem
    Push Zeichen für Zeichen.

    Beobachte den Lauf bis zum Ende: `gh run list --workflow release.yml --limit 1` für die
    Run-Id, dann `gh run watch <run-id> --exit-status`. Erwartet werden ein grüner Job
    `publish`, das nach ghcr.io gepushte Multi-Arch-Image und ein an das GitHub-Release
    angehängtes `mcp_connector-0.1.11.tar.gz`. Der 0.1.10-Lauf `33142956284` war grün und lief
    1m44s; ein roter Lauf ist deshalb ein neuer Befund und keine bekannte Altlast.

    Schreibe danach eine Proof-Zeile in die Nachweistabelle von `docs/store-submission.md`,
    hinter die drei Zeilen aus Plan 16-02, also an das Tabellenende. Sie deckt die Schritte 4
    und 5 zusammen ab, wie die 0.1.10-Zeile es tat: Datum in UTC, die Behauptung, dass der
    Workflow des Tags `v0.1.11` in jedem Schritt grün ist, mit Run-Id, Job-Name und Laufzeit,
    die Bytegröße des angehängten Assets, die Behauptung, dass die Commits der Phase
    unmittelbar vor dem Tag nach `main` gepusht wurden, samt dem Commit-Hash, auf den der Tag
    zeigt, und der Zeitpunkt der Owner-Freigabe. Dritte Spalte: `git push origin main`,
    `git tag v0.1.11`, `git push origin v0.1.11`, `gh run watch <run-id> --exit-status` mit dem
    Exit-Code und `gh release view v0.1.11`.

    Schreibe die Zeile erst NACH dem grünen Lauf. Keine Zeile für die Schritte 6 bis 8: die
    entstehen in Plan 16-04, nachdem sie passiert sind.

    Lösche kein Release-Asset und schreibe keinen Tag um, auch nicht bei einem Fehlerbefund.
    Ein Fehler kostet eine neue Patch-Version und wird als Befund gemeldet.
  </action>
  <verify>
    <automated>gh release view v0.1.11 --json assets --jq '.assets[].name' | grep -c '^mcp_connector-0.1.11.tar.gz$'</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v0.1.11` gibt genau `v0.1.11`
    - `git ls-remote --tags origin v0.1.11` nennt den Tag samt Commit-Hash
    - `gh run watch <run-id> --exit-status` endete mit Exit 0, und `gh run view <run-id> --json conclusion --jq .conclusion` gibt `success`
    - `gh release view v0.1.11 --json assets --jq '.assets[].name'` nennt `mcp_connector-0.1.11.tar.gz`
    - `docs/store-submission.md` trägt eine neue Zeile am Tabellenende mit Datum, Run-Id, Job-Name, Laufzeit, Assetgröße, Commit-Hash des Tags, Freigabezeitpunkt und den Kommandos in der dritten Spalte
    - `git diff --numstat docs/store-submission.md` nennt 1 hinzugefügte und 0 entfernte Zeilen
    - `docs/store-submission.md` trägt weiterhin keine Zeile zu den Schritten 6 bis 8 für 0.1.11
    - `git tag --list | grep -cx 'v0.1.11'` gibt 1, `git tag --list 'v0.1.*' | wc -l` gibt 12, und kein weiterer neuer Tag ist entstanden
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Der Tag v0.1.11 existiert lokal und remote, der Workflow-Lauf ist grün, das Asset hängt am Release, und eine datierte Proof-Zeile belegt die Schritte 4 und 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu öffentlichem Tag | Der Tag ist irreversibel und startet den Release-Workflow |
| Automat zu Mensch | Die Freigabe ist die eine Entscheidung dieser Phase, die kein Kommando ersetzen darf |
| GitHub Release Asset zu AppAPI-Installation | Administratoren installieren von dieser URL, nicht aus dem Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-15 | Denial of Service | Tag v0.1.11 vor der Freigabe | mitigate | Der Tag entsteht ausschließlich in Task 3 und nur, wenn die Antwort des blockierenden Checkpoints `freigeben` lautet; der Plan ist `autonomous: false`, und `gate="blocking-human"` heißt, dass `auto_advance` hier nicht gilt; Tasks 1 und 2 behaupten die Leere von `git tag --list v0.1.11` als Akzeptanzkriterium |
| T-16-16 | Tampering | Tag-Name | mitigate | Der Name ist wörtlich `v0.1.11`; die Nachbarn `v0.1.1` und `v0.1.10` existieren bereits, deshalb wird der Name vor dem Push Zeichen für Zeichen geprüft und die Tagzahl danach als 12 gezählt; `release.yml` triggert auf `v*` und prüft die Gleichheit gegen `<version>` mit Exit 1; `v1.5` wird ausdrücklich nicht gesetzt, Milestone-Tags heißen `milestone-v*` |
| T-16-17 | Denial of Service | Release-Asset und Tag-Historie | mitigate | Kein `gh release delete-asset`, kein `git tag -f`, kein Force-Push; ein Fehlerbefund wird gemeldet und kostet eine neue Patch-Version |
| T-16-18 | Tampering | Nicht gepushter Stand unter einem Tag | mitigate | Task 1 belegt mit `git log origin/main..HEAD` die Zahl 0 und die Leere von `git ls-remote --tags`, bevor der Tag überhaupt zur Entscheidung steht (Erfahrung 0.1.8 mit 42 ungepushten Commits) |
| T-16-19 | Repudiation | Proof-Zeile zu Schritt 4 und 5 | mitigate | Die Zeile entsteht erst nach dem grünen Lauf und trägt Run-Id, Job-Name, Laufzeit, Exit-Code, Commit-Hash und den Freigabezeitpunkt; ein Akzeptanzkriterium verbietet Zeilen zu den Schritten 6 bis 8 und erlaubt nur eine hinzugefügte Zeile |
| T-16-20 | Repudiation | Changelog-Datum | mitigate | Task 1 prüft die Datumszeile des 0.1.11-Blocks gegen den heutigen Tag in lokaler Zeit und in UTC und korrigiert nur diese eine Zeile, bevor gepusht wird |
| T-16-SC | Tampering | Build im Workflow | transfer | Image und Archiv entstehen im GitHub-Workflow aus dem getaggten Quellstand, nicht lokal hochgeladen; die Lieferkette liegt bei GitHub Actions und ghcr.io, ihre Nachweise erbringt Plan 16-04 mit OCI-Index und Tagliste; die Actions sind auf ihre node24-Majors gepinnt |
</threat_model>

<verification>
- `git log origin/main..HEAD` leer, Arbeitsbaum sauber, Changelog-Datum stimmt
- Antwort des Checkpoints wörtlich und mit Zeitpunkt im SUMMARY festgehalten
- `git tag --list v0.1.11` genau ein Treffer, remote vorhanden, insgesamt 12 `v0.1.*`-Tags
- Workflow-Lauf `success`, Asset am Release
- Eine neue Proof-Zeile, keine für die Schritte 6 bis 8
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 3 ist erfüllt und die erste Hälfte von Erfolgskriterium 4: der Branch
war auf GitHub, bevor irgendein Tag existierte, und der Tag `v0.1.11` entstand erst nach
ausdrücklicher Owner-Freigabe. Der Workflow hat das Multi-Arch-Image und das Store-Archiv
erzeugt.
</success_criteria>

<output>
Create `.planning/phases/16-release-0-1-11/16-03-SUMMARY.md` when done.
Nenne darin die Antwort des Checkpoints mit Zeitpunkt, die Run-Id des Workflow-Laufs, die
Bytegröße und die Download-URL des Assets, damit Plan 16-04 ohne Suche signieren kann.
Lautete die Antwort `warten`, halte das als Ergebnis fest und lasse Plan 16-04 ungestartet.
</output>
