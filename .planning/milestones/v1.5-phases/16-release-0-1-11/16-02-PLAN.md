---
phase: 16-release-0-1-11
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docs/store-submission.md
autonomous: true
requirements: [EXAPP-11]

must_haves:
  truths:
    - "Alle sechs Gates aus Runbook-Schritt 3 laufen lokal grün, ohne Anhebung eines Grenzwerts"
    - "Die Werkzeugoberfläche misst weiter 15712 Bytes über 21 Werkzeuge gegen ein Budget von 18000"
    - "Das gebaute Store-Archiv hat genau einen Top-Level-Ordner und das darin liegende README.md trägt die Statuszeile 0.1.11"
    - "Das darin liegende appinfo/info.xml trägt den gekürzten Trifecta-Absatz und das Attribut author mail admin@infranode.dev"
    - "Das darin liegende CHANGELOG.md trägt den Block 0.1.11 und kein Unreleased"
    - "Der Archiv-Probelauf ist im Nachweis ausdrücklich als Strukturprüfung benannt und die Skript-Signatur wird nicht eingereicht"
    - "Die Runbook-Schritte 1 bis 3 tragen je eine Proof-Zeile mit Datum, Behauptung und Befehl"
    - "Kein Tag v0.1.11 existiert"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeilen der Schritte 1 bis 3 für 0.1.11"
      contains: "0.1.11"
  key_links:
    - from: "dist/mcp_connector-0.1.11.tar.gz"
      to: "README.md Statuszeile"
      via: "das Archiv trägt README.md mit, die Statuszeile reist unveränderlich mit und kein Gate hält sie"
      pattern: "^Version 0\\.1\\.11\\."
    - from: "dist/mcp_connector-0.1.11.tar.gz"
      to: "appinfo/info.xml author mail"
      via: "das Archiv trägt das Manifest mit; dieses Attribut ist die Nutzlast, die dem Release seinen Grund gibt"
      pattern: "author mail=\"admin@infranode\\.dev\""
    - from: "dist/mcp_connector-0.1.11.tar.gz"
      to: "CHANGELOG.md Block 0.1.11"
      via: "das Archiv trägt CHANGELOG.md mit, damit fährt auch die Korrektur aus 901b294 am 0.1.10-Block erstmals mit"
      pattern: "## \\[0\\.1\\.11\\]"
---

<objective>
Runbook Schritt 3: die sechs Gates lokal grün, der Archiv-Probelauf als Strukturprüfung, und
die Proof-Zeilen der Schritte 1 bis 3. Dieser Plan ist der letzte, der ohne Owner-Freigabe
vollständig abschließbar ist.

Purpose: Die Gates bleiben auf der v1.2-Messung (BUDGET_BYTES 18000, MAX_TOOL_BYTES 1400,
21 Werkzeuge); eine Anhebung ist laut ROADMAP ausdrücklich Out of Scope. Der Archiv-Probelauf
belegt die fünfte Versionsstelle VOR dem Tag: bei 0.1.8 lag der Fix hinter dem Tag, und weil
ein Asset unveränderlich ist, blieb im veröffentlichten Tarball für immer "Version 0.1.7"
stehen.

Der Probelauf trägt diesmal einen zweiten und einen dritten Beleg. Das `appinfo/info.xml` im
Archiv muss den gekürzten Trifecta-Absatz und das Attribut `author mail="admin@infranode.dev"`
tragen: das ist die vollständige Nutzlast dieses Releases, vier Zeilen in einer Datei, und
nichts davon hält ein Gate. Das `CHANGELOG.md` im Archiv muss den 0.1.11-Block tragen und
keine Zeichenkette `Unreleased`, denn darüber fährt auch die Korrektur aus `901b294` am
0.1.10-Block erstmals in ein veröffentlichtes Asset.

Eine CIMD-Nachmessung gehört ausdrücklich nicht in diese Phase, aus demselben Grund wie bei
0.1.10: dieses Release ändert keine Zeile Code.

Output: sechs grüne Gates, ein geprüftes Archiv in `dist/` (nicht committet), drei
Proof-Zeilen in `docs/store-submission.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-release-0-1-11/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die sechs Gates in der Runbook-Reihenfolge und der Archiv-Probelauf</name>
  <files>dist/ (gitignored, nicht committen)</files>
  <read_first>
    - docs/store-submission.md, Zeile 214-231 (Runbook Schritt 3: die sechs Kommandos wörtlich und die Aufzählung der Manifest-Gates)
    - docs/store-submission.md, Zeile 148 (die 0.1.10-Proof-Zeile zu Schritt 3: sie ist das Formvorbild und nennt Herkunft und Bedeutung der 15712 Bytes, die Testzahlen und die Bytegröße des lokal gebauten Archivs)
    - scripts/build_store_release.sh, Zeile 5-23 (der Kommentar "diagnosis only") und Zeile 42-50 (die vier Mitglieder des Archivs: appinfo/info.xml, CHANGELOG.md, LICENSE, README.md)
    - scripts/check_tool_budget.py (der Gate der Werkzeugoberfläche und seine Grenzwerte BUDGET_BYTES und MAX_TOOL_BYTES)
    - .planning/milestones/v1.4-phases/15-release-0-1-10/15-02-PLAN.md, Task 1 (derselbe Lauf für 0.1.10, inklusive der Regel, bei Abweichung zu stoppen statt anzuheben)
    - .planning/phases/16-release-0-1-11/16-01-SUMMARY.md (welche Zeilen Plan 16-01 geändert hat und welches Datum die Kopfzeile des 0.1.11-Blocks trägt)
  </read_first>
  <action>
    Führe die sechs Gates in genau der Reihenfolge aus, die Runbook Schritt 3 nennt, jedes mit
    dem Präfix `uv run --no-sync`: `pytest -q`, `ruff check .`, `ruff format --check .`,
    `pyright`, `vulture src scripts vulture_whitelist.py`, und
    `python scripts/check_tool_budget.py`. Halte jede Ausgabe fest, die die Proof-Zeile
    braucht: die Zahl der bestandenen und deselektierten Tests, die Zahl der formatierten
    Dateien, und die Messung der Werkzeugoberfläche. Lies die Testzahl aus einem Lauf ohne das
    zusätzliche `-q`: `addopts` trägt bereits eines, und zwei davon unterdrücken die
    Summenzeile.

    Erwartungswert der Werkzeugoberfläche: 15712 Bytes über 21 Werkzeuge gegen ein Budget von
    18000, dieselbe Zahl, die der 0.1.10-Lauf gemessen hat. Diesmal darf sie sich nicht einmal
    um das eine Byte bewegen, das der 0.1.10-Lauf hatte: die `tools/list`-Hülle trägt die
    Versionszeichenkette in `serverInfo`, und der Sprung von `0.1.9` auf `0.1.10` machte sie um
    ein Zeichen länger. `0.1.10` und `0.1.11` sind beide sechs Zeichen lang, die Messung bleibt
    deshalb exakt gleich. Weicht sie ab, ist das ein Befund und kein Rundungsfehler: dann
    stoppe und melde die Abweichung, statt einen Grenzwert anzuheben. Eine Anhebung von
    BUDGET_BYTES oder MAX_TOOL_BYTES ist ausdrücklich Out of Scope.

    Erwartungswert der Testzahl: der 0.1.10-Lauf zählte 2813 bestandene und 163 deselektierte
    Tests, und seither ist kein Test hinzugekommen; diese Phase fügt keinen hinzu. Notiere die
    tatsächliche Zahl, rate sie nicht.

    Baue danach das Store-Archiv als Strukturprüfung: `bash scripts/build_store_release.sh`,
    dann `tar -tzf dist/mcp_connector-0.1.11.tar.gz`. Erwartet wird genau ein Top-Level-Ordner
    `mcp_connector/` mit den vier Mitgliedern `appinfo/info.xml`, `CHANGELOG.md`, `LICENSE`
    und `README.md`. Die vom Skript ausgegebene Signatur ist Diagnose und wird nirgends
    eingereicht: signiert wird in Plan 16-04 ausschließlich das heruntergeladene Asset, weil
    `tar.gz` nicht bytereproduzierbar ist (bei 0.1.10: 47299 Bytes lokal gegen 46973
    veröffentlicht, verschiedene sha256).

    Prüfe im gebauten Archiv drei Inhalte ausdrücklich, denn alle drei reisen unveränderlich
    mit und kein Gate hält sie:

    Erstens die fünfte Versionsstelle: `mcp_connector/README.md` im Archiv trägt genau eine
    Zeile, die mit `Version 0.1.11.` beginnt.

    Zweitens die Nutzlast: `mcp_connector/appinfo/info.xml` im Archiv trägt
    `author mail="admin@infranode.dev"` genau einmal, `k.cherif@outlook.de` kein einziges Mal,
    und den gekürzten Trifecta-Absatz statt des langen. Der kurze ist am Satzanfang "An
    assistant also reads text that strangers wrote" erkennbar, der lange am Satzanfang
    "Reading text written by strangers next to private data would be a chain". Der lange darf
    im Archiv nicht mehr vorkommen. Das ist der Beleg, dass die vier Zeilen, für die dieses
    Release existiert, tatsächlich im Kandidaten liegen.

    Drittens den Changelog-Stand: `mcp_connector/CHANGELOG.md` im Archiv enthält den Block
    `## [0.1.11]` und die Zeichenkette `Unreleased` kein einziges Mal.

    Committe nichts aus `dist/`; das Verzeichnis ist gitignored und hält bereits die Archive
    früherer Versionen. Erzeuge keinen Tag.
  </action>
  <verify>
    <automated>uv run --no-sync pytest -q && uv run --no-sync ruff check . && uv run --no-sync ruff format --check . && uv run --no-sync pyright && uv run --no-sync vulture src scripts vulture_whitelist.py && uv run --no-sync python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - Jedes der sechs Kommandos endet mit Exit 0, und die Ausgaben von `pytest`, `ruff format --check .` und `check_tool_budget.py` sind mit ihren Zahlen notiert
    - `uv run --no-sync python scripts/check_tool_budget.py` nennt 21 Werkzeuge, 15712 Bytes und ein Budget von 18000; jede Abweichung ist als Befund gemeldet und kein Grenzwert wurde angehoben
    - `git diff -- scripts/check_tool_budget.py tests/contract/test_tool_surface.py` ist leer
    - `tar -tzf dist/mcp_connector-0.1.11.tar.gz | cut -d/ -f1 | sort -u` gibt genau eine Zeile, `mcp_connector`
    - `tar -tzf dist/mcp_connector-0.1.11.tar.gz` nennt `mcp_connector/appinfo/info.xml`, `mcp_connector/CHANGELOG.md`, `mcp_connector/LICENSE` und `mcp_connector/README.md`
    - `tar -xzOf dist/mcp_connector-0.1.11.tar.gz mcp_connector/README.md | grep -c '^Version 0\.1\.11\.'` gibt 1
    - `tar -xzOf dist/mcp_connector-0.1.11.tar.gz mcp_connector/appinfo/info.xml | grep -c 'author mail="admin@infranode.dev"'` gibt 1
    - `tar -xzOf dist/mcp_connector-0.1.11.tar.gz mcp_connector/appinfo/info.xml | grep -c 'k.cherif@outlook.de'` gibt 0
    - `tar -xzOf dist/mcp_connector-0.1.11.tar.gz mcp_connector/appinfo/info.xml | grep -c 'An assistant also reads text that strangers wrote'` gibt 1, und dieselbe Zählung gegen `would be a chain` gibt 0
    - `tar -xzOf dist/mcp_connector-0.1.11.tar.gz mcp_connector/CHANGELOG.md | grep -c '^## \[0\.1\.11\]'` gibt 1, und dieselbe Zählung gegen `Unreleased` gibt 0
    - Die Bytegröße des lokal gebauten Archivs ist notiert
    - `git status --short dist` gibt keine Zeile aus
    - `git tag --list v0.1.11` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Gates grün ohne Anhebung, das Archiv hat einen Top-Level-Ordner und trägt Statuszeile 0.1.11, den gekürzten Absatz, die neue Autorenadresse und den 0.1.11-Changelog-Block, nichts aus dist ist committet, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Proof-Zeilen der Runbook-Schritte 1 bis 3</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 76-82 (der Satz "Every line was measured, not assumed. No fact without its check." und der Tabellenkopf `| Date | Fact | Checked with |`)
    - docs/store-submission.md, Zeile 146-148 (die drei 0.1.10-Zeilen zu den Schritten 1, 2 und 3 als Formvorbild)
    - docs/store-submission.md, Zeile 154-155 (die zwei Drift-Zeilen vom 2026-08-28: sie benennen die Nutzlast dieses Releases vollständig und sind der Beleg, gegen den die neuen Zeilen zählen) und Zeile 157 (`### The update keeps the connections`, die Grenze, vor der die neuen Zeilen stehen)
    - docs/store-submission.md, Zeile 197-231 (Runbook Schritte 1 bis 3, damit jede Behauptung ihren Schritt trifft)
    - .planning/milestones/v1.4-phases/15-release-0-1-10/15-02-PLAN.md, Task 2 (die Regel, dass die Tabelle in der Uhrzeit vorwärts läuft und keine Zeile ihrem Ereignis vorauslaufen darf)
    - tests/unit/test_exapp_env_setup.py, die Zeile mit `VOCABULARY_EXCEPTION` samt Gate-Docstring: warum genau diese Datei ausgenommen ist
  </read_first>
  <action>
    Schreibe drei neue Zeilen in die Nachweistabelle, direkt nach der bisher letzten Zeile
    (heute Zeile 155, 2026-08-28 06:05Z) und vor `### The update keeps the connections`. Form
    wie die Vorbilder: Datum in UTC mit `Z`, dann die Behauptung im Präsens, dann der Befehl,
    mit dem sie geprüft wurde. Sprache Englisch wie die übrige Datei. Die Tabelle läuft in der
    Uhrzeit vorwärts, deshalb stehen die neuen Zeilen am Ende und in der Reihenfolge ihrer
    Uhrzeit; bewege keine bestehende Zeile.

    Zeile zu Schritt 1: die sechs Versionsstellen tragen 0.1.11, mit den drei
    README-Statuszeilen ausdrücklich genannt, geprüft per grep je Stelle plus dem
    Manifest-Gate; nenne auch, dass `uv.lock` als sechste Stelle per Texteditierung mitgezogen
    wurde, ohne Lock-Lauf, und dass die vier CRLF-Dateien byte-exakt gepatcht wurden, sodass
    der Diff eine geänderte Zeile je Datei ist und zwei für `appinfo/info.xml` mit seinen zwei
    Stellen.

    Zeile zu Schritt 2: der Changelog-Block 0.1.11 ist aus dem bisherigen `[Unreleased]`-Block
    entstanden und nennt unter `### Changed` den gekürzten Trifecta-Absatz samt der geweiteten
    Teilen-Formulierung in den drei Store-Beschreibungen und den Wechsel des Attributs
    `<author mail>` von k.cherif@outlook.de auf admin@infranode.dev. `### Changed` ist die
    einzige Rubrik des Blocks. Halte ausdrücklich fest, dass die drei READMEs seit dem Tag
    `v0.1.10` keine Nutzlast tragen und deshalb im Block nicht genannt werden, anders als im
    0.1.10-Block; das ist gegen `git diff v0.1.10..HEAD -- README.md README.de.md README.fr.md`
    geprüft und nicht angenommen. Die Linkdefinition `[0.1.11]` steht und ist mit ihrem
    Abschnitt gepaart, und die `[Unreleased]`-Definition ist mit ihrem Abschnitt zusammen
    verschwunden, sodass die Zeichenkette `Unreleased` in der Datei nicht mehr vorkommt.

    Zeile zu Schritt 3: alle sechs Gates sind lokal grün, mit den gemessenen Zahlen aus Task 1
    (bestandene und deselektierte Tests, formatierte Dateien, keine Lint- und Typbefunde, kein
    toter Code, Werkzeugoberfläche in Bytes über 21 Werkzeuge gegen 18000, ohne Anhebung).
    Nenne, dass die Messung diesmal byte-gleich zum 0.1.10-Lauf ist und warum: die
    Versionszeichenkette in `serverInfo` behält beim Schritt von `0.1.10` auf `0.1.11` ihre
    Länge, während sie beim Schritt von `0.1.9` auf `0.1.10` um ein Zeichen wuchs. Nenne
    weiter, dass das Store-Archiv genau einen Top-Level-Ordner hat, die Statuszeile 0.1.11 in
    seinem README.md trägt, in seinem `appinfo/info.xml` den gekürzten Absatz und
    `admin@infranode.dev` als `author mail` führt und die alte Adresse nicht mehr, und in
    seinem CHANGELOG.md den Block 0.1.11 ohne jedes `Unreleased`. Nenne ausdrücklich, dass mit
    dem mitreisenden CHANGELOG.md auch die nach dem 0.1.10-Tag geschriebene Korrektur am
    0.1.10-Block erstmals veröffentlicht wird, und dass der Archiv-Probelauf eine
    Strukturprüfung ist: das lokal gebaute Archiv ist nicht das Artefakt, das signiert wird.
    Nenne keine Signatur und keine Bytezahl als eingereichten Wert; die Bytegröße des lokalen
    Archivs darf als Messwert der Strukturprüfung stehen.

    Schreibe keine Zeile für ein Ereignis, das noch nicht eingetreten ist. Die Schritte 4 bis 8
    bekommen ihre Zeilen in den Plänen 16-03 und 16-04, nachdem sie passiert sind: eine vorab
    geschriebene Proof-Zeile dreht die Beweisrichtung um und macht aus einem Protokoll eine
    Behauptung. Diese Datei ist die einzige mit einer Vokabular-Gate-Ausnahme, und genau diese
    Beweisrichtung ist ihre Begründung.

    `docs/store-submission.md` liegt mit LF im Arbeitsbaum; die Zeilenenden bleiben LF. Keine
    Em-Dashes. Behaupte am Ende dieses Tasks ausdrücklich, dass noch kein Tag existiert.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/store-submission.md` enthält drei neue Tabellenzeilen, jede beginnend mit einem Datum der Form `| YYYY-MM-DD HH:MMZ |`
    - Die drei Zeilen stehen zwischen der bisher letzten Tabellenzeile (06:05Z vom 2026-08-28) und der Überschrift `### The update keeps the connections`, belegt per `grep -n`
    - Die Datumsspalte der Tabelle ist von der ersten bis zur letzten Zeile aufsteigend; ein sortierter Vergleich der extrahierten Datumsspalte mit ihrer Originalreihenfolge zeigt keine Abweichung
    - `git diff --numstat docs/store-submission.md` nennt 3 hinzugefügte und 0 entfernte Zeilen
    - Jede der drei Zeilen nennt `0.1.11` und trägt in der dritten Spalte mindestens ein Kommando
    - Die Zeile zu Schritt 2 sagt ausdrücklich, dass die drei READMEs keine Nutzlast dieses Releases tragen, und nennt das Kommando, das es belegt
    - Die Zeile zu Schritt 3 enthält die Worte structure check, sagt, dass das lokal gebaute Archiv nicht das signierte Artefakt ist, nennt das mitreisende CHANGELOG.md und begründet die unveränderte Bytezahl der Werkzeugoberfläche mit der gleich langen Versionszeichenkette
    - `docs/store-submission.md` enthält keine Zeile zu den Schritten 4 bis 8 für 0.1.11
    - `python -c "d=open('docs/store-submission.md','rb').read(); print(d.count(b'\r\n'))"` gibt 0
    - `git tag --list v0.1.11` gibt keine Zeile aus
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Drei datierte Proof-Zeilen belegen die Schritte 1 bis 3, der Probelauf ist als Strukturprüfung benannt, die Tabelle läuft weiter vorwärts, keine Zeile läuft ihrem Ereignis voraus, kein Tag existiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokal gebautes Archiv zu veröffentlichtem Asset | Zwei verschiedene Bytefolgen, die leicht verwechselt werden; nur die zweite darf signiert werden |
| Nachweistabelle zu Leser | Die Tabelle ist der einzige Beleg für die Sorgfalt eines Releases; ihre Richtung ist Ereignis, dann Zeile |
| Gate-Grenzwerte zu Release-Kandidat | Ein angehobener Grenzwert macht einen roten Lauf grün, ohne etwas zu verbessern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | dist/mcp_connector-0.1.11.tar.gz | mitigate | Der Probelauf ist in der Proof-Zeile ausdrücklich als Strukturprüfung benannt; die Skript-Signatur ist Diagnose (Kommentar in `build_store_release.sh`); signiert wird ausschließlich in Plan 16-04 über das heruntergeladene Asset |
| T-16-09 | Tampering | Gate-Grenzwerte | mitigate | Grenzwerte bleiben auf der v1.2-Messung; bei Abweichung der 15712 Bytes stoppt der Task und meldet den Befund; ein Akzeptanzkriterium verlangt einen leeren Diff über `check_tool_budget.py` und `test_tool_surface.py` |
| T-16-10 | Tampering | Nutzlast fehlt im Kandidaten | mitigate | Der Probelauf zählt im Archiv `author mail="admin@infranode.dev"` gegen 1, `k.cherif@outlook.de` gegen 0, den kurzen Absatz gegen 1 und den langen gegen 0; ohne diese vier Zählungen ließe sich ein Release bauen, das seinen eigenen Grund nicht enthält |
| T-16-11 | Repudiation | docs/store-submission.md | mitigate | Nur Zeilen für eingetretene Ereignisse; Form ist Datum in UTC plus Behauptung plus Befehl; ein Akzeptanzkriterium verbietet Zeilen zu den Schritten 4 bis 8 und verlangt 3 hinzugefügte und 0 entfernte Zeilen, sodass keine bestehende Zeile umgeschrieben wird |
| T-16-12 | Denial of Service | Git-Tag v0.1.11 | mitigate | Dieser Plan erzeugt keinen Tag und behauptet in beiden Tasks als Akzeptanzkriterium, dass `git tag --list v0.1.11` leer ist |
| T-16-13 | Tampering | Die fünfte Versionsstelle im Tarball | mitigate | Der Probelauf prüft `README.md` im Archiv gegen `^Version 0\.1\.11\.` VOR dem Tag; bei 0.1.8 lag dieser Fix hinter dem Tag und blieb als 0.1.7 für immer veröffentlicht |
| T-16-14 | Information Disclosure | dist/ im Arbeitsbaum | accept | `dist/` ist gitignored und hält nur gebaute Archive aus öffentlichem Quellcode; ein Akzeptanzkriterium prüft, dass nichts daraus committet wird |
| T-16-SC | Tampering | Paketinstallation | mitigate | Alle Gate-Läufe mit `uv run --no-sync`, also ohne Sync und ohne Auflösung; kein `uv add`, `pip install` oder `npm install` |
</threat_model>

<verification>
- Sechs Gates mit Exit 0, Zahlen notiert, kein Grenzwert angehoben, Werkzeugoberfläche bei 15712 über 21 Werkzeuge
- Archiv mit genau einem Top-Level-Ordner, Statuszeile 0.1.11, Nutzlast im Manifest, Block 0.1.11 ohne Unreleased
- Drei Proof-Zeilen am Tabellenende, Datumsspalte weiter aufsteigend, keine Zeile für die Schritte 4 bis 8
- `git tag --list v0.1.11` leer
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 5 ist erfüllt: alle Gates laufen lokal grün auf dem
Release-Kandidaten, ohne Anhebung, bei 18000 und 1400 über 21 Werkzeuge. Die Phase ist damit
ohne Owner-Freigabe bis Runbook-Schritt 3 vollständig abgeschlossen und in diesem Zustand
prüfbar.
</success_criteria>

<output>
Create `.planning/phases/16-release-0-1-11/16-02-SUMMARY.md` when done.
Nenne darin die gemessenen Gate-Zahlen und die Bytegröße des lokal gebauten Archivs, damit
Plan 16-04 die Differenz zum veröffentlichten Asset belegen kann.
</output>
